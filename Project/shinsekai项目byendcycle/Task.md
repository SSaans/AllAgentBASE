# shinsekai项目byendcycle 任务清单

> 📅 产出：规划 Agent（2026-09-07）
> 📋 状态总览：部署与识屏已有验收记录；心跳完整验收及后台实例退出仍有待办，见任务 17、18。

## 部署与说话（已完成）

- [x] 任务 1：确认本地整合包完整（`H:\Program\新世界\Shinsekai`，v2.3.1，自带 runtime）
- [x] 任务 2：验证程序可启动常驻（shinsekai.exe + runtime\pythonw.exe 双进程运行中）
- [x] 任务 3：验证 LLM 对话可用（OpenAI 兼容端点，`data/config/api.yaml`）
- [x] 任务 4：验证 TTS 开口说话（GPT-SoVITS 服务自动拉起，丛雨语音模型切换成功）

## 心跳自动检查（已完成）

- [x] 任务 5：启用 `shinsekai_heartbeat` 插件（plugins.yaml enabled: true）
- [x] 任务 6：验证检查节点自动触发（日志多次 heartbeat.emitted，间隔 5–60 分钟随机）
- [x] 任务 7：验证不插队逻辑（用户发言/回复中/TTS 播放中重置计时）

## 自动识屏（已有历史验收记录，见任务 16）

- [x] 任务 8：启用 `screen_state_companion-BYGPT` 插件（提供 capture_screen 工具）
- [x] 任务 9：心跳提示词注入「调用 capture_screen」指令（识屏/自言自语/提问三种模式均生效）
- [x] 任务 10：验证检查节点识屏闭环（心跳触发 → 截图生成 → 主模型基于屏幕内容发言）
- [x] 任务 11：验证用户主动请求识屏（对话中说「看看我的屏幕」可触发）
- [x] 任务 12：新增自动清理旧截图（retention_days=3，启动+每次截图后自动清理，已验证释放约 78MB）

> ✅ **遗留缺陷已解决（2026-09-08 回填）**：识屏误判根因为**流式 JSON 解析缺陷**（原始回答含未转义引号时只交付最后一段，两次远程格式修复也非法），已新增本地 dialogue JSON 规范化修复，端到端验收通过（日语教材实时截图、数学对照图双案例，详见 CHANGELOG 2026-09-08「Codex 端到端验收结果」）。此前的 PNG optimize、detail 参数两次诊断均被验证 Agent 纠正为非根因。

## 可选迭代（用户已决策：不做，2026-09-07）

> 用户确认：不做迭代，仅维护现有功能。以下任务不再执行，留档备查。

- ~~任务 12：启用学习监督模式~~（用户放弃）
- ~~任务 13：启用 Moondream Vision 本地识屏~~（用户放弃）
- ~~任务 14：评估心跳调度日志降噪~~（用户放弃）
- ~~任务 15：识屏频率与模式权重调优~~（用户放弃）

## 维护记录（Bug 修复看板，2026-09-08 起按活看板规则运行）

> 按根 BRD 协作规则第 5/6/7 条：Bug 一律建任务、状态实时流转、仓库外代码改动摘要落库。
> 以下为历史修复回填；测试数量不替代端到端验收，发现反例须重新打开。

- [x] 任务 16：修复识屏误判（日语教材被识别成"数学题"）—— 根因：流式 JSON 解析只交付最后一段 + 原始回答含未转义引号；修复：新增本地受限 dialogue JSON 规范化，流式解析/格式修复/历史恢复共用。端到端验收通过（日语实时截图、数学对照图双案例）。排障过程中的两次误诊（PNG optimize、detail 参数）已被验证纠正并留档。改动目录：`H:\Program\新世界\Shinsekai`（仓库外，摘要见 CHANGELOG 与 `HANDOFF_识屏误判修复.md`）
- [ ] 任务 17：修复心跳陪伴关闭学习监督后不再主动说话（待复验）—— 调度重置修复及 47 项回归通过；日志证明真实会话已触发。尚缺从正常启动入口验证 UI、语音、历史同轮及正常退出，不应据此宣布全部通过。运行目录：`H:\Program\新世界\Shinsekai`。
- [ ] 任务 18：排查仅停在设置页仍自言自语，完成测试实例退出验收（待办）—— 2026-09-08 用户反例；凌晨直接启动的测试 PID 42464 对应会话在 03:22–09:35 多次 `heartbeat.emitted`，脱离当前设置窗口继续运行。当前核对该 PID 已不存在，退出原因未确认；没有关闭用户设置窗口或更改插件配置。需按 TESTING.md 验证正常入口启动、退出后停止调度/TTS、历史保存及再启动单实例。本轮仅完成证据定位与工作流补全，未做产品代码修复。

## 2026-09-12 规划 Agent 信息同步

- 本轮仅阅读本地文档并校正文档状态；未重新验证运行环境，保留任务 17 待复验、任务 18 待办。
- 后续由开发 Agent 排查任务 18，测试 Agent 完成任务 17、18 的正常入口、设置页、UI/语音/历史、退出与再启动验收；仅测试 Agent 可关闭任务。
- 当前资料库为 D:\Program\AllAgentBASE；旧 H: 运行路径为历史记录，本轮未确认迁移位置。
- 远端同步待恢复：github.com DNS 解析失败，未拉取到最新远端内容；本轮文档需推送后才完成跨 Agent 交接。
