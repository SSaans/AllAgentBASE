# 万有引力 任务清单

> 状态流转：`待办 → 修复中 → 待复验 → 已关闭`
> 勾选（`[x]`）**仅测试 Agent** 验收通过后可改。
> 需求依据：本目录 `BRD.md`；边界与保密要求见 `BRD.md` §1.3 与 §七。

---

## 一、当前阶段（S0 收尾 / 流程归位）

- [ ] 任务 1：**越界产物认领与正式化**（开发 Agent）
  - 背景：立项前规划 Agent 越界写下了两个功能脚本（环境自检 / 解密·列出·导出 CLI），并自行在真机上跑过一遍。依 `BRD.md` §6.2，这属于开发 Agent 职责。
  - 要做：① 认领或判定弃用重写；② 补注释与用法文档；③ 按 `BRD.md` §8.3 自查（无敏感信息、无未说明 TODO、依赖已声明）；④ 在子项目 CHANGELOG 写清"改了什么、验没验"。
  - ⛔ 交付前**必须**确认：脚本与文档里**不出现**私有仓库名/地址、账号标识、本机数据目录（见 `BRD.md` §七 C1–C3）。
  - 证据：文件清单 + `git diff --cached --name-only` 输出 + 敏感词自查结果。
  - 去向：开发 Agent → 测试 Agent。

- [ ] 任务 2：**补齐未验证的导出能力**（开发 Agent）
  - 依 `BRD.md` §8.1，以下**均未实测**，需实现或修复后交付：
    - `F6` 批量导出全部会话（会话数量多时不得中断、不得内存溢出）
    - `F7` CSV / TXT / Markdown / DOCX / XLSX / JSON 六种格式导出
    - `F8` 语音消息转码为可播放音频
    - `F9` 微信 4.x 链路（含取密钥）
    - `F10` 只读校验：解密前后**原始数据库哈希必须一致**
  - ⛔ 输出目录**必须在仓库之外**（`BRD.md` §七 C4）。
  - 证据：每种格式产出一个样例文件路径 + 打开检查结果；`F10` 的哈希前后对比。
  - 去向：开发 Agent → 测试 Agent。

- [ ] 任务 3：**微信 3.x 全链路正式复验**（测试 Agent）
  - 依 `BRD.md` §8.1 `F1–F5` 与 §8.3 逐项核对。
  - 🔴 口径：现有"链路已跑通"**只是规划 Agent 的待核实记录**，不构成验收通过；须重新跑一遍并留证据。
  - 必测边界：① 微信**未运行**时取密钥应给出明确提示而非崩溃；② 拿不到密钥时不产生半成品产物；③ 单会话记录为空时不报错不中断；④ 超大记录不中断。
  - 证据：命令、输出、产物路径、浏览器截图或渲染检查。
  - 结论：在子项目 CHANGELOG 明确签字"通过"或给出 Bug 清单。

- [ ] 任务 4：**保密巡检**（任意 Agent 发现问题即建；收尾由测试 Agent 复核）
  - 逐条核对 `BRD.md` §七 `C1–C5`：公开仓库内**不得**出现私有仓库名/地址、账号标识、本机路径。
  - ⚠️ 已知风险点：`SSaans/AllAgentBASE` 是**公开**仓库；提交前务必自查提交信息与文档正文。
  - 证据：检索命令 + 命中结果（应为 0 命中）。

---

## 二、下一步（S1，需用户放行）

- [ ] 任务 5：**S1 架构整改——抽统一消息模型与引擎接口**（开发 Agent）
  - 依 `BRD.md` §4.3 五步执行。
  - 关键动作：定义 `UnifiedMessage` / `Contact` / `Conversation` / `Attachment`；定义引擎接口 `detect() → collect() → Iterable[UnifiedMessage]`；把现有微信引擎改为实现该接口；把导出层从采集层剥离，使其**只依赖统一模型**。
  - 🔴 **硬判据**：改造后用微信引擎导出的 HTML，**与改造前等价**（可逐字节或逐节点比对）；导出层代码中**不得出现**任何平台分支（如 `if platform == 'wechat'`）。
  - 证据：改造前后产物比对结果 + 导出层不含平台分支的检索输出（0 命中）。
  - ⚠️ 前置：需用户放行（`BRD.md` §十 第 2 条）。**建议在做 QQ 之前完成**，否则第二个引擎会把架构带歪。

---

## 三、后续平台（S2 / S3，未启动）

- [ ] 任务 6：**QQ / TIM 引擎**（开发 Agent）
  - 依 `BRD.md` §5.2：TIM 与 QQ 同 NT 内核，**同批实现**。
  - 🔴 调研结论已定：协议端（NapCat / Lagrange 之类）只能拿到**服务端保留的历史，拿不全**；要完整历史**必须解密本机 NT 数据库**，不要在这条路上绕弯。
  - 交付：能用本机数据库导出文本与图片历史。
  - ⚠️ 严格只读；动手前先备份，备份失败即中止。

- [ ] 任务 7：**B 站私信提取**（开发 Agent）
  - 依 `BRD.md` §5.2：私信在服务端、可翻页拉取，**不需要本地逆向**；难点是接口非公开 + 风控。
  - 交付：能翻页拉取完整私信并导出。
  - ⛔ 账号凭据**不得写入仓库**；必须限速、可暂停、默认关闭。

- [ ] 任务 8：**抖音可行性结论**（开发 Agent）
  - 目标**不是实现**，而是产出一份明确的**"能做 / 不能做"结论**，含技术路径、风控与合规风险评估。
  - 🔴 依 `BRD.md` §5.2，抖音**不作为任何阶段的交付承诺**；结论如实记录即可。

---

## 四、待用户拍板（阻塞项）

| # | 事项 | 阻塞 |
|---|---|---|
| A | 保密口径：AllAgentBASE 是公开仓库 → ①只留脱敏版 ②转私有 ③项目挪去私有仓库 | 任务 1、4 |
| B | 是否启动 S1（`BRD.md` §十 第 2 条） | 任务 5、6 |
| C | 越界产物处置：认领纳入正式交付，还是弃用重写 | 任务 1 |
| D | 首批平台范围是否按 P0→P3 推进 | 任务 6–8 |
