# 表情包同步（StickerSync）任务清单

> 状态流转：`待办 → 修复中 → 待复验 → 已关闭`（**只有测试 Agent 能勾 `[x]`**）
> 验收标准见 `BRD.md`「十、验收标准」。

---

## A. 待用户拍板（规划阶段，本轮产出）

- [ ] 任务 1：用户确认 TIM 侧的三条结论（待复验）
  - 依据：`BRD.md` §1.3。
  - 要点：① 数据源认 `CustomFace.db`、**不走 TIM 那套会坏的 eif 导出**；② 写入按 **W3（面板导入，零风险）→ W2（直写 db，高价值高风险）→ W1（生成 eif）** 推进；③ **动 db 必先备份**。
  - 归属：用户拍板。

- [ ] 任务 2：用户确认能力边界（待复验）
  - 要点：**TIM 导出全自动 / TIM 导入半自动（一次多选）/ 微信仅备料** —— 能否接受？这是整个项目的预期基准。
  - 归属：用户拍板。

- [ ] 任务 3：用户确认 §5 边界清单（34 条）是否有遗漏（待复验）
  - 归属：用户拍板。

- [ ] 任务 4：用户确认云端仓库开放范围（待复验）
  - 选项：① **私有**（建议，规避版权风险）；② 公开。
  - 归属：用户拍板。

---

## B. 攻坚（前置，只读，不改任何文件）

- [x] 任务 5：阶段 0.5 — `CustomFace.db` 结构实测（待复验）✅ 2026-09-20 完成
  - **目标**：摸清 TIM 表情库的内部结构（CFB 容器里的流清单、各流语义、分组与图片如何组织）。
  - **方式**：**只读**；操作对象一律是**拷贝副本**，绝不直接打开用户的原始文件做实验。
  - **产出**：结构说明文档（含样本解析结果、未知字段标注）。
  - **判据**：能完整列出所有分组与表情，且**数量与 TIM 界面一致**。
  - ✅ **实测结果**：
    - 文件位置（**旧机**）：`D:\SystemFiles\Files\Tencent Files\2403232967\CustomFace.db`（194 MB）
    - 🔴 **换机后（2026-09-21 实测）**：本机 TIM 数据根为 `D:\我的文档\Documents\Tencent Files\`，本账号 db = **`D:\我的文档\Documents\Tencent Files\2403232967\CustomFace.db`（仅 34.6 MB）**
      - ⚠️ **与旧机的 194.9 MB 差距巨大 → 新机数据很可能不完整，动手前必须与用户核对**
      - 同目录下另有 4 个账号：`1392949867`（0 MB）、`1940231445`（7 MB）、`2307165194`（9.8 MB）、`2820478544`（13.9 MB）
      - ⚠️ 旧机的 `CustomFace.db.backup`（194.2 MB，现位于 `H:\Program\stickersync\`）**属旧机数据，不得用于新机回滚**
      - 详见 `CHANGELOG.md`「换机勘误补遗：TIM 数据源实测定位」
    - 总计 897 个表情：893 个云端分组（目录 8213）+ 4 个本地分组（目录 1）
    - 格式分布：BMP (448) + GIF (194) + JPG (242) + PNG (13)
    - CFB 结构清晰：图片直接放在分组目录下，文件名为 TIM 内部命名（乱码）
    - ✅ 导出完全可行、写入技术可行但需谨慎
  - 📄 详见：`H:\Program\stickersync\CustomFace.db_结构分析.md`

---

## C. 待办（放行后执行）

- [x] 任务 6：阶段 1 — 核心骨架与 Pack 规范（待复验）✅ 2026-09-20 部分完成
  - 产出：`core/`（models / manifest / probe / naming / errors）+ 适配器基类；**魔数识别优先做**（TIM 与微信两侧的共同地基）。
  - 判据：`probe` 对 GIF87a / GIF89a / PNG / JPEG / WEBP / APNG 与 3 种异常输入判定正确；manifest 往返零丢失。
  - ✅ **已完成部分**：魔数识别（10/10 测试通过）、数据模型、manifest 读写、异常体系、适配器基类、文件名规范化
  - ⏳ **未完成部分**：CFB 读写模块（等待阶段 0.5 完成后实现）、CLI 骨架（阶段 2 配套）
  - ⚠️ 待确认：本机 **ffmpeg** 可用性（阶段 3 需要）；**可写的 CFB 库**待选型（参考 `cfb-reader` MIT 实现思路）

- [ ] 任务 7：阶段 2 — 读取侧（导出）（待办）
  - 产出：`TimAdapter.export()`（解析 CFB 库）+ `WeChatAdapter.export()`（多路径探测 + 魔数识别）。
  - 判据：从用户真实 TIM 与微信目录各导出 ≥ 1 个合法 Pack。
  - 覆盖边界：§5 第 5–8、19–20 条。

- [ ] 任务 8：阶段 3-W3 — 面板导入（**先做，零风险**）（待办）
  - 产出：标准化图片批次 + 图文引导（TIM 表情面板多选导入 / 微信「添加整套表情」）。
  - 判据：用户按指引一次性批量导入成功。

- [ ] 任务 9：阶段 3-W2 — 直写 `CustomFace.db`（**高价值，高风险**）（待办）
  - 前置：任务 5 完成且结构已知；**未知结构一律不猜、不写**。
  - 必需配套：强制备份 + 副本改写 + 写入后重新解析校验 + 失败自动回滚 + 一键回滚命令。
  - 判据：**经用户实机导入成功**，且反复写入后原始数据始终完好；§5 第 14–15 条有用例覆盖。
  - ⚠️ **验证通过前，不得发布任何会写 db 的功能**。

- [ ] 任务 10：阶段 3-W1 — 生成 `.eif`（可选，视 W2 结果）（待办）
  - 前置：需补齐 `Face.dat` 未公开字段的结构（加密算法已知且可逆，见 `BRD.md` §2.1.3）。
  - 判据：生成的 eif 能**被 TIM 成功导入**。若判定不可行，须在 `BRD.md` 与本节明确记录结论与依据。

- [ ] 任务 11：阶段 4 — 同步引擎与健壮性（待办）
  - 产出：五阶段引擎（预检 / 计划 / 备份 / 执行校验 / 报告）+ `SyncReport` + 备份回滚 + 文件锁 + 原子替换 + 中断恢复。
  - 判据：§5「通用」8 条全部有用例；**模拟中断与模拟写入失败后，数据必须完好**。

- [ ] 任务 12：阶段 5 — 前端（待办）
  - 产出：本地 Web UI（浏览 / 预览 / 打标签 / 选包 → 生成产物 → 显示导入指引 + 备份回滚入口）。
  - 判据：不碰命令行即可完成「导出 → 选包 → 生成 → 按指引导入」。

- [ ] 任务 13：阶段 6 — 云端同步（待办）
  - 产出：Pack 仓库目录规范、索引生成、拉取 / 推送流程。
  - 判据：A 机器上传 → B 机器拉取 → 可用。
  - ⚠️ 禁止 `git add .`，逐路径显式 add。

---

## D. 待办（需用户或后续 Agent 决策）

- [ ] 任务 14：是否纳入 NTQQ 轨（待办）
  - 现状：用户目前用 TIM；NTQQ 无本地分组、无 eif，只能走"图片批次 + 面板导入"。
  - 建议：先做好 TIM；NTQQ 轨作为**独立适配器**后补，架构已预留。
  - 归属：用户决策。

- [ ] 任务 15：表情包版权与分享策略（待办）
  - 建议：默认私有仓库；不提供公开订阅入口；Pack 内标注来源。
  - 归属：用户决策。

- [ ] 任务 16：是否引入 JS 参考实现（待办）
  - 现状：`cfb-reader` 为 **MIT**（可参考），其他上游多为 GPLv3。
  - 建议：**只参考思路，不复制代码**；如确需引用须评估许可证。
  - 归属：规划 Agent 评估 → 用户决策。

---

## E. 巡检发现（规划 Agent，2026-09-20 03:35）

- [ ] 任务 17：**写 db 前必须重新备份**（待办）
  - 现状：现有备份 `H:\Program\stickersync\CustomFace.db.backup`（194.2 MB @ 02:30:41）**已落后于原文件**（194.9 MB @ 02:34:49）—— 原文件在备份之后被再次写入（推测为 TIM 运行所致）。
  - 影响：若日后用该备份回滚，会**丢失 02:34 之后新增的数据**。
  - 要求：任何写 db 操作前，**必须基于当时的原文件重新备份**；写前校验「备份字节数 == 原文件字节数」，不一致则重新备份。
  - 归属：开发 Agent。

- [ ] 任务 18：确认被过滤的 448 个 BMP 确为缩略图（待办）
  - 现状：阶段 0.5 统计 897 个文件，实际导出 447 个真表情，448 个 BMP 被过滤。
  - 证据：BMP 与主图**同名配对**（如 `xxx.gif` 与 `xxxfix.bmp`），数量近乎 **1:1** → **高度疑似缩略图**（过滤大概率正确）。
  - 要求：抽样读取若干 BMP 的尺寸头确认（缩略图尺寸应显著小于主图）；若发现含真实表情，须调整规则并重新导出。
  - 另：447 + 448 = 895，与总数 897 差 **2**，需查明是否有 2 个文件因格式无法识别被跳过。
  - 归属：开发 Agent。

- [ ] 任务 19：清理残留的 Web 服务进程（待办）
  - 现状：巡检发现 `127.0.0.1:8899` 有服务在监听（Web UI 自测遗留），且 `server.log` 为 **0 字节**（无日志输出）。
  - 依据：`AGENTS.md`「运行环境收尾」—— 测试后应正常退出自己启动的实例，不留无人管理的后台进程。
  - 要求：确认是否为自测遗留并正常退出；建议为服务补上日志输出。
  - 归属：开发 Agent。

---

## F. 维护记录

- 2026-09-20：子项目立项（R1：QQ + Telegram + 微信）。
- 2026-09-20：**R2 范围修订** —— 按用户要求移除 Telegram，聚焦 **QQ/TIM + 微信**；并按用户澄清「用的全是 TIM」，将 TIM 定为**主战场**，实测出「TIM 的 eif 导出是坏的」「表情存于未加密的 `CustomFace.db`」两条关键结论，写入路径改为 **W3 → W2 → W1** 三级推进，并确立 **db 写入备份铁律**。本轮仍**只规划不写代码**。
- 2026-09-20 03:35：规划 Agent 巡检 —— 新增 E 节，记录 3 项发现（**备份落后于原文件** / BMP 过滤待确认 / 残留 Web 服务），对应任务 17–19，交开发 Agent 处理。
