# bilisum部署 任务清单

> 状态流转：`待办 → 修复中 → 待复验 → 已关闭`（**只有测试 Agent 能勾 `[x]`**）
> 验收标准见 `BRD.md`「五、验收标准」。

---

## A. 待验收（2026-09-17 部署产出，等测试 Agent 复验）

- [ ] 任务 1：网页版入口可用（待复验）
  - 双击 `D:\Program\bilisum\start-web.bat` → 浏览器自动打开 `http://127.0.0.1:3838` 并正常渲染界面（非空白 / 非 404）。
  - 部署侧自测：`GET /` 与 `GET /settings` 实测均返回 200 `text/html`（1,685 B）。

- [ ] 任务 2：健康接口与版本号正确（待复验）
  - `http://127.0.0.1:3838/health` 返回 200，JSON 中 `service=BiliSum`、`version=1.21.1`。
  - 部署侧自测：实测 200 且字段一致。

- [ ] 任务 3：桌面版窗口能连上后端（待复验）
  - 双击 `start-desktop.bat` → Electron 窗口打开，且界面不再是「连接中 / 后端未就绪」状态。
  - ⚠️ 本轮**未验证**：部署时只验证了前端可构建（`npm run build` 全绿）与 Electron 二进制到位（42.3.3），**没有真正拉起过窗口**。此项必须实机复验，不能用构建通过代替。

- [ ] 任务 4：配置持久化（待复验）
  - 在设置页保存一组 LLM / ASR 配置 → 重启服务 → 配置仍在。
  - ⚠️ 本轮**未验证**：需要真实 Key，属用户侧操作。

- [ ] 任务 5：端到端跑通一条真实视频（待复验）
  - 导入一条 B 站视频，走完「转写 → 摘要 → 笔记」。
  - ⚠️ 前提：用户先配好 LLM + ASR。本轮**未验证**。

---

## B. 待办（需用户或后续 Agent 决策）

- [ ] 任务 6：决定本地 ASR 运行时是否安装（待办）
  - 现状：`.venv` 内无 torch（服务日志 `torch=False funasr=False`）→ 本地 Whisper / FunASR / 本地 Embedding 均不可用。
  - 选项：① 只用在线 ASR + 在线 Embedding，不装；② 按需安装本地运行时（数 GB，首次很慢）。
  - 归属：用户决策 → 如选 ② 交开发 Agent 执行。

- [ ] 任务 7：评估是否需要开机自启 / 桌面快捷方式（待办）
  - 现状：每次需手动双击脚本。
  - 归属：用户决策后交开发 Agent。

- [ ] 任务 8：上游版本跟进策略（待办）
  - 现状：本地锁定 v1.21.1（`fc693b1`），上游活跃（593 star，2026-09 仍在更新）。
  - 建议：不自动跟随；用户提出要新功能时再评估，升级前先备份 `AppData\Local\bilisum\data`。
  - 归属：规划 Agent 评估 → 开发 Agent 执行。

---

## C. 维护记录

- 2026-09-17：子项目立项并完成部署（网页版 + 桌面版双入口跑通构建与服务启动）。
