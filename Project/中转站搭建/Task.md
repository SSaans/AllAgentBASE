# 中转站搭建 任务清单

> 上游需求见 `BRD.md`。状态流转：`待办 → 修复中 → 待复验 → 已关闭`。
> **`[x]` 关闭仅测试 Agent 可勾选**，开发 Agent 自测完成后写「（待复验）」。
> ⚠️ 凭据（站点地址 / API Key / 令牌 / 密码）**只在本机程序面板里填**，不得写进本文件或任何仓库文件。

---

## 一、需用户提供（阻塞开工）

- [ ] **P1**：手上在用的中转站「站点地址」与「API Key」
- [ ] **P2**：该站支持的模型名列表
- [ ] **D1**：部署方式拍板（规划建议：二进制）
- [ ] **D2**：本期是否给朋友用（规划建议：本期只自用）
- [ ] **D3**：是否接 Claude Code（规划建议：用就接）

> P1 / P2 未提供之前，阶段 2 及之后的任务无法开工；阶段 0 可以先做。

---

## 二、阶段 0：环境确认（无依赖，可立即开工）

- [ ] 任务 1：**复核**规划 Agent 的只读预检结果（见 BRD §5.3），补齐两项
  - 已预检、无需重复：Windows 10 22H2 / AMD64；**Docker 未安装**；无历史 new-api 目录；3000 已被 `node.exe`(PID 33316) 占用
  - 待补：① 该 node 进程是否会长期存在（影响端口结论的稳定性）② WSL2 是否启用（⚠️ `wsl.exe` 被安全策略拦截，**禁重试**，必要时请用户手工确认）
  - **端口结论已定：本项目用 `3001`**（备选顺序 3002 → 3003 → 3005 → 3100）
  - 🔴 不得强杀占用 3000 的 node 进程（属用户既有服务）
  - 证据：复核命令原文 + 返回结果

- [ ] 任务 2：确认 new-api 的 Windows 可用发行包
  - 在上游 Releases 找到 Windows amd64 包，记录**版本号、下载地址、校验值**
  - 同时记录 Docker 镜像名与当前 tag（供 D1 选 Docker 时使用）
  - 证据：Release 页面信息与校验值

---

## 三、阶段 1：把面板跑起来

- [ ] 任务 3：部署 new-api 并完成首次初始化
  - 程序目录落在**仓库外的固定位置**（路径写进 README，不得入库）
  - 首次访问进初始化向导：设管理员账号密码（强密码）、**使用模式选「自用」**
  - 证据：面板可登录（截图或可复现的访问方式）+ 进程/容器存活状态

- [ ] 任务 4：确认数据持久化
  - 记录数据目录实际位置（含 SQLite 文件）
  - 正常停止进程 → 重新启动 → 确认配置与日志仍在
  - 证据：重启前后各一次状态对比

- [ ] 任务 5：确认端口只监听本机
  - 验证 `127.0.0.1:3001` 可访问、局域网内其他设备访问不通
  - 证据：两个方向的访问结果

---

## 四、阶段 2：接入上游（依赖 P1 / P2）

- [ ] 任务 6：接入第一家上游渠道（用户手头那家）
  - 渠道类型选「自定义（OpenAI 兼容）」或对应厂商类型；Base URL 用该站的 `/v1` 地址
  - 保存后**必须点「测试」**，记录连通性与延迟（ms）
  - 证据：测试返回原文

- [ ] 任务 7：登记模型并完成定价配置（**防 R1 拦截**）
  - 把客户端要用的模型名登记进模型管理
  - 同步在「模型定价」里填单价；**实测确认自用模式下不填价格是否真会被拦截**，把结论记进 CHANGELOG
  - 证据：客户端或 curl 打一次请求，返回正常 `choices`（而不是余额/价格类报错）

- [ ] 任务 8：接入第二家上游，验证故障转移
  - 同模型挂两个渠道
  - 停用其中一家 → 再次请求仍成功；恢复启用 → 渠道归属回到预期
  - 证据：两次请求的日志中渠道归属不同

---

## 五、阶段 3：客户端打通

- [ ] 任务 9：创建令牌并完成 Cherry Studio 首次真实对话
  - 令牌在「令牌管理」里创建（**不是渠道 key**，别混，见 BRD §九 R3）
  - Base URL 填 `http://127.0.0.1:3001/v1`
  - 证据：一次真实对话成功，**流式输出正常**

- [ ] 任务 10：Claude Code / Codex 接入（若 D3 为「接」）
  - 通过面板的 Anthropic 格式入口接入，确认协议互转生效
  - 证据：该客户端一次真实调用成功

---

## 六、阶段 4：文档与收尾

- [ ] 任务 11：补全 README 的实际值（写进**已有文件**，不新建文档）
  - 程序目录、数据目录、端口、启动/停止方式、加渠道步骤、备份方式、升级方式

- [ ] 任务 12：可运维性自检
  - 按 README 步骤，从「已停止」状态独立走一遍启动 → 可用，确认步骤无遗漏
  - 备份一次数据目录，确认备份可用

- [ ] 任务 13：凭据与仓库卫生检查
  - 全仓扫描：无站点地址、无 API Key、无令牌、无管理员密码
  - `git status` 干净；数据目录与程序目录均未被跟踪

---

## 七、遗留与去向

- 到期未决问题**当场**记入本节（附现象与已试办法），不要攒到收工口头交接
- 收工三件套：① 子项目 `CHANGELOG.md` 记录 ② 本文件状态更新 ③ `git commit + push`（以 `git ls-remote` 核实）
