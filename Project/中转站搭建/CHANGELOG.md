# 中转站搭建 变更日志

> 子项目细节写这里，**不进根 log**（依 `AGENTS.md` 规则 9）。
> 保持 15 条上限，超出部分归档进同目录 `CHANGELOG.archive.md`。

---

## [2026-09-22] 开发 Agent — 阶段0完成：环境复核 + 发行包信息记录（待复验）

**任务范围**：Task.md 阶段0的任务1、任务2

**完成项**：
1. **环境复核**（任务1）
   - PID 33316 node进程状态：持续运行，CPU 114.91s，属用户环境常驻服务
   - 3000端口：持续被占用，**本项目使用3001端口的决策正确**
   - 3001端口：确认空闲可用
   - WSL2状态：wsl.exe被安全策略拦截无法探测，不影响本期二进制部署方案
   - **结论**：规划Agent预检结果全部复核确认，端口冲突为长期状态，3001端口可用

2. **new-api发行包信息**（任务2）
   - Docker镜像：calciumion/new-api:v1.0.0-rc.38（linux/amd64 72.63MB, linux/arm64 70.43MB）
   - Windows二进制包：因GitHub访问受限，已记录项目地址和获取方式，待用户协助下载
   - 证据文件：`temp/env_check_result.md`、`temp/newapi_release_info.md`

**阻塞项**：
- 需获取new-api Windows amd64二进制包才能进入阶段1（部署）
- 仍需用户提供P1（上游站点地址+API Key）和P2（模型名列表）才能进入阶段2（接入上游）

**改动文件**：
- `Task.md`：任务1、2标记为待复验，补充实测证据和结论
- 本文件：本条记录

**下一步**：
- 获取Windows二进制包后开始阶段1部署
- 测试Agent按BRD §七验收阶段0完成情况

---

## [2026-09-22] 规划 Agent — 环境预检（只读）+ 端口改 3001 + 决策项按默认值预置

**起因**：用户指示「继续推进」。为了让开发 Agent 不必从零摸环境，先做一次**只读**预检（未安装、未下载、未改动任何东西）。

**预检结论（已并入 BRD §5.3）**

| 项 | 结果 | 影响 |
|---|---|---|
| 系统 | Windows 10 22H2（`10.0.19045`）AMD64 | 满足要求 |
| **Docker** | **未安装** | 二进制方式由「推荐」升为**本期唯一现实选择**；Docker 路线需先装 Docker Desktop + WSL2 |
| WSL2 | **探测失败** —— `wsl.exe` 被本机安全策略列入黑名单，禁启动 | 已写明「禁重试」，需确认时请用户手工查看 |
| **3000 端口** | **已被占用**（`node.exe` PID 33316 LISTENING） | 🔴 **本项目端口改为 3001**；不碰该进程（用户既有服务，红线） |
| 候选端口 | 3001/3002/3003/3005/3100/8000/8080 均空闲 | 以 3001 为准，备选按序 |
| 历史部署 | 无 new-api / sub2api 目录 | 全新部署，无迁移 |

**决策项预置（BRD §10.2）**：用户说「继续推进」但未逐项表态，故按规划建议预置为执行口径，并注明可随时推翻——D1 部署方式＝**二进制**；D2 本期**只自用**；D3 **接** Claude Code。目的是不让开发 Agent 停在「等拍板」上空等。

**文档改动**
- `BRD.md`：新增 §5.3「本机环境预检结果」；端口 3000→3001（含架构图、参数表、客户端表、验收项）；R6 端口冲突由「已知解法」改为「**已实际发生**·已处置」；§10.2 由「需要拍板」改为「预置取值 + 依据」
- `Task.md`：任务 1 由「从零确认环境」改为「**复核**预检结果 + 补齐 2 项」，并写明禁重试 `wsl.exe`、禁强杀占用端口的进程
- `README.md`：端口同步；补一条「不装 Docker」的说明
- 本文件：本条

**阻塞项（唯一）**：仍等用户提供 **P1**（站点地址 + API Key）与 **P2**（模型名）。阶段 0、1 不依赖它，开发 Agent 可立即开工。

## [2026-09-22] 规划 Agent — 立项：搭 new-api 面板聚合上游中转站（规划完成，待用户提供坐标）

**背景**：用户先让另一 Agent 调研了「sub2api 面板中转站」（22 站横向对比），随后指派规划 Agent「新建 sub2api 中转站搭建 project 并作出详细规划」。

**本轮澄清（关键，避免立项立歪）**：经与用户确认，真实需求是——**在本机 Windows 上搭一个 new-api 面板，把自己在用的别家中转站接到上游，先自用**。原表述里的「sub2api」是调研对象（22 家站的底座），**不是**用户要装的东西。

**① 选型结论：底座用 new-api（BRD §二）**
- 本项目上游 = 别家中转站的 API Key → new-api 的渠道管理正是为此设计
- sub2api 的主场是「订阅账号池 + 拼车分发」，用户手上**无任何 AI 账号**，用它等于放弃其唯一长处
- sub2api 未废弃，转为**延期方案**（BRD §六 6.7）：等有订阅账号时再单独立项

**② 调研报告的定位（BRD §三）**
定位为**上游选站参考**，不是搭建文档。三条可复用价值：候选上游清单、面板识别方法（`.well-known/ai-transit.json` / 快照接口 / `x-new-api-version` 头）、价格与渠道基线（0.02x–0.5x、1 元=1 美元、逆向渠道风险）。
⚠️ 引用边界已写明：报告自述「不构成推荐」，数据时效 2026-09-19。

**③ 交付物**
| 文件 | 内容 |
|---|---|
| `BRD.md` | 目标 G1–G4、选型论证、报告用法、架构图、部署形态（二进制优先）、6 类功能需求、四类验收标准、边界红线、9 条风险、待用户提供/拍板 |
| `Task.md` | 5 阶段 13 项任务 + 需用户提供项（P1/P2、D1–D3） |
| `README.md` | 通俗说明：解决什么、怎么做、能干什么、不做什么、注意事项 |

**④ 立项即定的三条硬边界**
- 只监听本机，不对外、不开公网（本期定位自用）
- 对外收费**不写进规划**——用户询问过合规风险，已按「自用 / 朋友白用 / 对外收费」三层留档于 BRD §九 R5，结论：收费运营风险等级显著上升，不建议
- 凭据（站点地址、key、令牌、密码）**一律不入库**

**⑤ 目录命名说明**
目录取用户给的**简称** `Project/中转站搭建/`，未将「sub2api」写进目录名——底座最终为 new-api，目录名绑死某开源项目会使后续 Agent 误判。

**状态**：规划完成，**卡在用户提供 P1（站点地址 + key）与 P2（模型名）**，并需对 D1–D3 拍板。阶段 0（环境确认）不依赖以上，开发 Agent 可先开工。

**下一步**：用户提供坐标与拍板 → 开发 Agent 按 `Task.md` 开工 → 测试 Agent 按 BRD §七 验收。
