# 子项目 BRD：ALLBot 部署（AstrBot QQ 机器人「丛雨」调教）

> 📅 立项时间：2026-09-15
> 👤 发起人：用户（Edi）
> 👤 维护者：规划 Agent
> 🎯 状态：群分析、长回复自动转发、群漫画均已真实群内成功；⚠️ 2026-09-15 15:46 后实测**所有 `/` 命令失效**（唤醒前缀丢失 `/`，见「已知风险」#8 与 Task.md 任务 20），待用户修复配置后交测试 Agent 正式验收

---

## 项目目标

在已能正常聊天的 AstrBot QQ 机器人（人设：丛雨）基础上完成三项调教，并为其后续衍生程序（插件、辅助工具）建立开发规范：

1. **插件命令正常调用**：打通 AstrBot 插件命令链路，使群内可用 `/` 前缀正常触发插件指令（含第三方插件）；
2. **自动群总结（仅指令触发）**：配置群聊分析插件，群内发指令即可生成当日群聊总结报告（话题 / 成员称号 / 金句 / 质量锐评），并以丛雨人设口吻呈现；
3. **说话通过转发聊天记录实现（长短分流）**：按用户自定义标准自动选择消息形式——短内容直接发送，长内容以 QQ「合并转发聊天记录」卡片发送。

---

## 现状盘点（规划 Agent 实地核查，2026-09-15）

| 项 | 内容 |
|---|---|
| 管理程序 | AstrBot Launcher 0.3.9（安装包 `D:\Program\AstrBot\AstrBot Launcher`） |
| 核心版本 | **AstrBot v4.26.8**（`core/main.py`，`pyproject.toml` 确认） |
| 实例目录 | `C:\Users\WindoseII\.astrbot_launcher\instances\4450a298-f4c2-43fa-b7f7-bd645b753fc3\core` |
| 数据目录 | 上述 `core\data`（cmd_config.json、plugins、logs、data_v4.db） |
| LLM | OpenAI 兼容端点 `https://rkapi.com/v1`；模型 `gpt-5.6-terra`（默认）、`claude-opus-5`；`max_context_tokens=1000000` |
| 人设 | `default_personality = "丛雨"`，`persona_pool = ["*"]`（人设内容存于 `data_v4.db`） |
| QQ 接入 | `aiocqhttp`（OneBot v11）反向 WS `0.0.0.0:6199`，平台名「丛雨v1」← **SnowLuma** 客户端连接 |
| 唤醒前缀 | QQ 档「丛雨丸」**实测** `["丛雨", "丛雨酱"]`（**不含 `/`**）；默认档 `cmd_config.json` = `[]`。⚠️ 勘误：Codex 轮曾保留原前缀并补 `/`，其后被覆盖回退（配置档 mtime `2026-09-15 15:46`）→ 全部 `/` 命令静默失效，见风险 #8 / 功能 4.6 |
| 管理员 | 默认档与 QQ 档均保留昵称 Edi，并按用户提供的 QQ 号添加管理员（号码不落库） |
| 已装插件 | `astrbot_plugin_qq_group_daily_analysis`（群分析）、`astrbot_plugin_repeater`（复读）、`astrbot_plugin_limited_repeat`、`astrbot_plugin_listen_music`（听歌） |
| 长消息转发 | QQ 档 `streaming_response = false`、`segmented_reply.enable = false`；`forward_threshold` **实测 = 50**（勘误：原记 30 已被后续改动覆盖）→ 当前真实分界「恰好 50 字直发、51 字起转卡片」 |
| WebUI | 仪表盘 `localhost:17163`（以 Launcher 当前入口为准，账号 Edi） |
| 运行状态 | astrbot 双 python 进程常驻（venv + py312），可正常聊天 |

⚠️ **敏感信息红线**：`cmd_config.json` 内含 LLM API Key、WS token、仪表盘密码，**严禁写入本仓库**，文档只引用位置。

---

## 📌 开发进度快照（2026-09-15 交接轮，按 Codex 实测证据勘误）

> 前序开发 Agent（Codex）因 5h 限额中断。证据来源：Codex 本地克隆 `D:\Program\AllAgentBASE` 的 Task.md/README/CHANGELOG 实测记录（规划 Agent 已合并入库）+ 运行时核查。**测试 Agent 正式验收前，Task.md 状态不勾选关闭。**

> ⚠️ **勘误（2026-09-15 规划 Agent）**：上述克隆副本已于本日移入回收站，本机仅保留唯一权威工作区 `D:\Project\AllAgentBASE`。此段引用副本路径仅为历史表述，**勿再按该路径查找文件**。

| 项 | 状态 | 说明 |
|---|---|---|
| 配置修复 | ⚠️ 部分回退 | 管理员按 QQ 号添加 ✅（实测 `admins_id=["Edi","2403232967"]`）；插件 `keep_original_persona=true` ✅；**但唤醒前缀的 `/` 与 `log_file_enable` 均被后续覆盖回退**（实测不含 `/` / `false`）——见风险 #6、#8 |
| `/群分析` | ✅ 已实测 | 00:52 真实执行成功（话题 2 / 称号 2 / 金句 5 / 锐评 1，trace=succeeded，报告图片生成落盘），**用户已确认除漫画外正常**；手动触发不受 200 条/日限制 |
| `/群漫画` | ✅ 已实测 | `openai_images` / `gpt-image-2` 供应商独立生图成功；11:45 真实指令完成话题、分镜、生图和落盘，11:47:08 QQ 群消息历史确认机器人发送 `text,image` |
| 转发卡片 | ✅ 超阈值自动链路已实测；边界值待复验 | QQ 档已非流式、关闭自动分段；配置更新后群消息历史确认 11:23:15、11:23:46、11:27:23 三次普通 LLM 长回复均为 `forward`。⚠️ **勘误（2026-09-15 第二轮实测）**：阈值**当前 = 50**（原记 30 已被后续改动覆盖），真实分界为「恰好 50 字直发、51 字起转卡片」；边界复验请按实测 50 判定 |
| 日志落盘 | ⚠️ 未成立（实测回退） | **勘误：`log_file_enable` 当前实测 = `false`**，`core/data/logs/astrbot.log` 最后写入停在 `11:42:51`、仅 1200 字节；需先重新开启并保存，再经 Launcher 正常重启核对 |
| 隔离自测 | ✅ 10/10 通过 | `tests/check_installed_source.py`：白名单 / 人设回归 / 模型回退 / 定时关闭 / 29、30、31 字转发边界 / 实际 QQ 配置 / 转发节点到 OneBot 群合并转发动作；另有 >30 字普通 LLM 自动链路群内实证 |
| 远端同步 | ✅ 已恢复 | 本快照已随合并轮提交推送（此前 GitHub 网络不可达导致 Codex 未推送） |

---

## 核心功能与技术方案

### 功能 1：插件命令正常调用

**现状**：`plugin_set = ["*"]`（全部启用）、唤醒前缀 `/`（QQ 实际生效档「丛雨丸」为 `["丛雨", "/"]`）、`disable_builtin_commands = false`。群分析插件指令已确认存在：`/群分析`、`/群漫画`、`/设置格式`、`/设置模板`、`/查看模板`、`/分析设置`、`/增量状态`。

**已验证结论（原为「待确认疑点」，2026-09-15 规划 Agent 按实测证据勘误）**：
- ✅ `platform_settings.enable_id_white_list = true` 且 `id_whitelist = []`——**空白名单直接放行、不拦截**；插件命令声明为管理员命令，普通群成员收到明确无权限提示，符合预期（详见风险表 #1）；
- ✅ `log_file_enable` 已由 false 改为 true（全局配置）；文件日志 sink 只在启动阶段配置，需经 Launcher 正常重启后生效（详见风险表 #6）；
- ✅ 三条 `/` 指令已真实进入事件总线，`/分析设置`、`/群分析` 均正常响应（00:52 实测）。

**方案**：按「群内实际调用 → 抓日志/回复 → 定位拦截点（白名单/权限/前缀/插件加载）」逐层验证修复，保证至少管理员（Edi）与目标群普通成员能正常触发插件指令。

### 功能 2：自动群总结（仅指令触发）

**方案**：直接使用已安装的 `astrbot_plugin_qq_group_daily_analysis`：

- 群内发 `/群分析` 触发当日（`analysis_days=1`）群聊分析，输出格式 `image + text + html`（模板 scrapbook）；
- 分析维度：话题（≤5 个）、成员称号+MBTI（≤8 人）、金句（≤5 条）、聊天质量锐评；另有 `/群漫画` 生成多格漫画；
- 人设联动：`keep_original_persona=true`（已修正，原 false 导致 `_build_system_prompt` 返回 None、丛雨人设不加载）、`use_plugin_specific_persona=false`——报告以丛雨口吻撰写（00:52 实测已出报告，口吻听感待测试 Agent 正式验收）；
- **已确认**：`llm.llm_provider_id = ""` 时回退当前会话模型（claude-opus-5/claude-opus-5），LLM 调用成功；
- **已确认**：`group_list_mode = "none"` 不限群；`min_messages_threshold = 200` 仅限**定时分析**（手动触发不受限，已实测且用户确认）；
- 定时自动分析（`auto_analysis_time=["23:00"]`）按用户决策**不启用**，保留为可选迭代。

### 功能 3：说话通过转发聊天记录实现（长短分流）

**机制（已确认存在）**：AstrBot 内置逻辑——非流式 LLM 回复的 Plain 文本总字符数**超过 `forward_threshold`（实测 50，见 4.2 勘误）**时，转为 QQ「合并转发聊天记录」卡片。流式结果会提前发送并跳过该判断，因此 QQ 档须保持 `provider_settings.streaming_response=false`。

**方案**：
- 用户确认标准：短内容直接发送 1–2 条普通消息，超过阈值发送合并转发卡片。当前关闭 `segmented_reply`，使短回复稳定为 1 条普通消息；
  - 📌 勘误（2026-09-15 第二轮实测）：QQ 档 `forward_threshold` **实测 = 50**，非原记 30——当前真实分界为「恰好 50 字直发、51 字起转卡片」。若用户期望仍为 30，按 4.2 改法保存即热生效。
- 验证 OneBot v11（SnowLuma）下合并转发卡片在群内正常展示；
- **可选衍生程序（用户确认后立项）**：编写小型插件，将转发节点的 `uin/name` 呈现为「丛雨」人设身份，并支持按丛雨口吻为长内容分段组织转发节点，使转发记录更贴近人设；
- 可选调优项：`segmented_reply`（长回复拆多条间隔发送）当前关闭，如用户需要短消息也按句拆分可另行开启。

### 功能 4：新增需求（2026-09-15 用户提出；规划 Agent 源码级核查 + 用户口径已确认）

> 📌 用户已于 2026-09-15 回复全部口径。本节为**规划结论**，对应 Task.md 任务 15–20；除 4.5 已由用户本人执行外，**其余均未实施、未启用**。

**4.1 指定时间段的群漫画**——用户口径：**不需要**指定历史某一天，「最近几天够用」
- 现状：`/群漫画 [天数]`（`main.py:809`，参数 `days`）语义即**最近 N 天**；底层 `onebot_adapter.py:147` 的 `since_ts` 只提供下界，过滤条件 `start_timestamp <= msg_time <= now`（`:264`）**无上界**。
- 结论：**降级为零代码**。「改造 `until_ts` + 命令层日期参数」**不再实施**；唯一阻塞是功能 4.6 的 `/` 命令失效。
- 修复 4.6 后，`/群漫画 N` 直接可用（N = 最近 N 天）；注意 `basic.max_messages=1000` 会截断回溯量。
- 备选技术事实（仅将来若确需「锁定某一天」时参考）：改造点 = ① `fetch_messages` 增 `until_ts` 并透传（域接口 `message_repository.py:24`）② `onebot_adapter.py:264` 补上界 ③ `analysis_application_service.py:1169` 手动漫画入口透传 ④ `main.py:810` 命令层解析日期；另需同步 discord / telegram / qq_official 适配器，或明确限定仅 OneBot 支持。

**4.2 转发阈值修改位置（答疑项，无开发量；含本轮实测勘误）**
- 字段：`platform_settings.forward_threshold`（QQ 实际档「丛雨丸」）；**实测 = 50**（勘误：原记 30 已被后续改动覆盖）。
- 生效判断：`astrbot/core/pipeline/result_decorate/stage.py:414`（`if word_cnt > self.forward_threshold`——**恰好等于阈值仍直发**）；全局默认 1500 位于 `astrbot/core/config/default.py:66`，被档内值覆盖，**改它无用**。
- 改法：WebUI → 平台设置 → 选中「丛雨丸」档 → 改数值 → 保存即热生效；**直接编辑磁盘 JSON 不会更新运行对象**。
- 硬前提：`provider_settings.streaming_response = false`（本轮实测 ✓），否则流式结果提前发送、跳过长度判断。

**4.3 存图功能（群友图片库）——用户口径已明确，正式立项，交开发 Agent**
- **用途**：把群友发送的图片按「关键词」归档，之后用「关键词 + `.jpg`」检索并发出。
- **存图规则**：发送一张图片，并在**同一条消息**或**对该图片的回复**中带 `/c <关键词>.jpg`（例：`/c 大肥鱼.jpg`）→ 该图片归档到关键词「大肥鱼」。
- **发图规则**（**必须匹配到 `<关键词>.jpg` 才触发**；只有关键词、不带 `.jpg` 时**完全无反应**）：

| 输入示例 | 期望行为 |
|---|---|
| `大肥鱼.jpg` | 发 1 张 |
| `大肥鱼.jpgx3` | 发 3 张 |
| `大肥鱼.jpgx15`（库存仅 10 张） | 发全部 10 张，随后补一句「**一张也没有了...**」 |
| `哦鲸鲸.jpg`（库中无此关键词） | 回「**换个关键词逝世吧...**」；**发送者是主人**时回「**主人换个关键词逝世吧...**」 |
| `大肥鱼`（无 `.jpg`） | **无任何反应** |

- **技术要点 / 待开发 Agent 确认**：
  1. **避开唤醒陷阱**：发图触发应为**消息文本正则匹配**，建议用 `filter.event_message_type(GROUP_MESSAGE)` + 正则，不注册为命令，避免与 `/` 前缀机制耦合（否则同样会「没反应」）；存图用的 `/c` 仍受 `wake_prefix` 制约，见 4.6。
  2. **存储结构**：建议 `core/data/meme_library/<关键词>/` 存图 + 轻量索引（JSON 或复用 `data_v4.db` 建表）；**明确不得入库仓库**（放运行目录）。
  3. **同关键词多图**：用户示例「库里有 10 张 `大肥鱼.jpg`」→ 同关键词**允许多张**；命名 / 去重规则待定（如 `大肥鱼_001.jpg` 递增，或按内容 hash 去重）。
  4. **`xN` 语法**：`x` + 整数（`x3` / `x15`）；非法写法（`x0` / `xabc` / `x-1`）的降级行为待定义。
  5. **多图发送方式**：✅ **已定——逐条发**（用户 2026-09-15 口径）。一次发 10 张即 10 条图片消息，**不走合并转发**（`forward_threshold` 只管纯文本字数，不套用图片）；需留意 QQ 风控与发送频率。
  6. **主人判定**：按 `admins_id`（实测 `["Edi","2403232967"]`）区分话术分叉。
  7. **管理 / 删除功能**：用户本轮**未提**，**不擅自加**；如需可另立任务。
- ✅ **口径已全部确认（2026-09-15 用户回复）**：多图**逐条发**；同关键词多图的**命名 / 去重方案由开发 Agent 自行决定**——用户只关注功能可用，但须守住「不覆盖已有图、可维护、不误删」三条底线。
- **去向：交开发 Agent（新插件）**。

**4.4 被 @ 或叫「丛雨」/「丛雨酱」时立即回复「吾辈在！」——需小型插件；含一处设计冲突待用户拍板**
- **用户口径**：话术固定为 **「吾辈在！」**（三字 + 感叹号，无其他内容）；**@ 也触发**；说 **「丛雨」「丛雨酱」也触发**；**触发词用户可自行编辑**。
- **触发词在哪改（答复用户）**：触发词**就是核心配置项 `wake_prefix`**——`astrbot/core/pipeline/waking_check/stage.py:103-120` 读取它判定唤醒。**WebUI「配置 → 唤醒前缀」即编辑位置**，日后加词 / 删词都在这里改。⚠️ **`/` 必须留在列表中**（否则全部 `/` 命令失效，见 4.6），最终建议形如 `["丛雨","丛雨酱","/"]`。
- ✅ **行为已定：方案 A**（用户 2026-09-15 确认「本来就是 A」）——纯唤醒才回「吾辈在！」，带内容则放行 LLM，**不干扰正常对话**。以下三方案表保留备查：

| 方案 | 行为 | 后果 |
|---|---|---|
| **A（推荐）** | 唤醒后**消息无其他内容**才回「吾辈在！」并 `stop_event()`；有内容则放行 LLM | 「@丛雨」→「吾辈在！」；「@丛雨 今天天气」→ 正常回答 |
| B | 任何唤醒都回「吾辈在！」且阻止 LLM | **无法正常对话**（不推荐） |
| C | 回「吾辈在！」但**不**阻止 LLM | 会「吾辈在！」+ 正经回答两条，略啰嗦 |

- **技术要点**：监听 `EventType.AdapterMessageEvent`（配 `filter.event_message_type(GROUP_MESSAGE)`），判断 `event.is_at_or_wake_command`，`yield event.plain_result("吾辈在！")`，再按**方案 A** 决定是否 `event.stop_event()`；**处理顺序须先于 LLM 对话阶段**；实现时须与 4.6 的前缀修复共存。
- **去向：交开发 Agent**（规格已定稿，可开工）。

**4.5 关闭 Markdown 输出——用户已自行以人设约束解决**
- **核实结果（2026-09-15 规划 Agent 只读 `core\data\data_v4.db` → `personas` 表）**：丛雨人设 `system_prompt` 已于 **2026-09-15 07:28** 加入约束，原文：「特别注意：回复时不要使用Markdown格式，禁用星号、md格式的"- "列表，其他不变，就和普通说话一样，用纯文本回复」。即**方案 A 已由用户本人执行完毕**，本轮**未改动运行目录**。
- 原根因保留：LLM 输出 Markdown；`result_decorate/stage.py` **不含任何 Markdown 剥离逻辑**，且全局 `t2i=false`，故 `**加粗**`、`## 标题` 会原样发到 QQ。
- **已知限制**：人设约束属 **prompt 软约束**，非结果层硬过滤——偶发星号仍可能出现。若要 100% 干净：方案 B 开 `t2i=true` 渲染成图（会改变输出形式，且与字数分流交互，须实测）；方案 C 插件层剥离（开发量最大）。**两者本轮均不立项。**
- **去向：测试 Agent 在真实群内复核「回复中不再出现 `**` / `- ` 等 Markdown 痕迹」。**

**4.6 🚨 全部 `/` 命令失效（本轮定位的根因，零代码可解）**
- 根因：`wake_prefix` **不含 `/`**。`astrbot/core/star/filter/command.py:191-192`——命令过滤器**首行**即要求 `event.is_at_or_wake_command` 为真，否则直接 `return False`：**命令完全不触发、也不给任何提示**。
- 该标志仅在 `astrbot/core/pipeline/waking_check/stage.py:102-148` 命中以下任一条件时置真：消息以 `wake_prefix` 中某项开头（L107）、@ 机器人 / @全体 / 引用机器人消息（L123-139）、私聊（L141-148）。
- 实测：`abconf` 顶层 `wake_prefix = ["丛雨","丛雨酱"]`、`cmd_config.json` 顶层 `wake_prefix = []`——**两处均不含 `/`**；故在群里直发 `/群漫画 3`（未 @ 机器人）时该标志恒为 `False` → 静默丢弃。
- 证据链闭合：开发 Agent 记录 `11:45` 用户在群 1095608283 真实触发 `/群漫画` 成功（说明**当时 `/` 尚在**）→ 配置档 mtime `15:46` 变更后失效。
- 修复：**由用户本人执行**（WebUI → 配置 → 唤醒前缀，把 `/` 加回列表，`abconf` 与 `cmd_config.json` 两处都确认），保存即热生效。修复后 `/群漫画 3`、`/群分析`、`/分析设置` 同步恢复。
**4.7 合并转发卡片标题显示为「丛雨」——需极小代码改动，本轮立项**
- **需求（用户 2026-09-15）**：卡片标题现为「AstrBot的聊天记录」（用户截图实测），需显示为「**丛雨的聊天记录**」。
- **标题来源**：转发节点 `nickname` 字段——`astrbot/core/message/components.py:697-703`（`Node.to_dict()` 输出 `"nickname": self.name`）。
- **改动点**：
  1. LLM 长回复卡片（用户截图所指）：`astrbot/core/pipeline/result_decorate/stage.py:417` 硬编码 `name="AstrBot"` → `name="丛雨"`。
  2. 群分析报告卡片：插件 `src/infrastructure/platform/base.py:190` 的 `self_name = "分析报告"` → `"丛雨"`（否则该卡片显示「分析报告的聊天记录」）。
- ⚠️ **核心补丁风险**：改动点 1 位于 AstrBot 核心，**升级核心后会被覆盖，须重新应用**（已登记 README「开发核实与本机改动」）。
- ⚠️ **不采用插件钩子方案**：`on_decorating_result`（`stage.py:163`）早于转发构造（`stage.py:415`），钩子中无法改写该 Node；若在钩子内自行构造 Node，将绕过 `reply_prefix` / `segmented_reply` / `t2i`（`stage.py:200-406`）。
- **去向：交开发 Agent**（与 4.3 / 4.4 同批交付）。



---

## 验收标准

### 1. 功能验收

- [ ] 群内可正常调用插件命令（至少 `/群分析`、`/群漫画`），管理员与普通群成员均按预期获得响应或明确的无权限提示
- [ ] `/群分析` 在消息量达标（≥200 条/日）的群内产出完整报告：话题 + 成员称号 + 金句 + 质量锐评，输出 image/text 正常展示
  - 📌 勘误（2026-09-15 规划 Agent）：`min_messages_threshold=200` **仅约束定时分析**，手动 `/群分析` 绕过该门槛（已实测在少量消息场景成功）。「≥200 条」是**定时场景**的前提，不是手动触发的前置条件。
- [ ] 群总结报告以丛雨人设口吻撰写（话题描述/锐评/金句理由符合人设）
- [ ] 短回复（≤阈值）以 1 条普通消息直接发送；长回复（>阈值）以合并转发聊天记录卡片发送，群内可正常点开查看
  - 📌 勘误（2026-09-15 规划 Agent 实测）：阈值 **当前 = 50**（原记 30 已被后续改动覆盖），真实分界「恰好 50 字直发、51 字起转卡片」；验收请按实测 50 判定，或由用户重新设定后再验。
- [ ] 长/短标准由用户自定义（修改 `forward_threshold` 后**须经对应配置档 API 保存**才热生效、无需重启；直接编辑磁盘 JSON 不会更新运行对象）
  - 📌 补充（2026-09-15 规划 Agent）：修改前须先修复 4.6 的 `/` 前缀问题（否则命令不可达）；且 `provider_settings.streaming_response` 必须为 `false`。

### 2. 文档验收

- [ ] 子项目 BRD.md / README.md / Task.md 齐全且与实测一致
- [ ] 根 CHANGELOG.md 记录**平台级**立项（子项目各轮开发/测试结论写入本子项目 `CHANGELOG.md`）
  - 📌 勘误（2026-09-15 规划 Agent）：原表述「根 CHANGELOG.md 记录立项与各轮开发/测试结论」已过期。按 2026-09-15 日志归属新规（`AGENTS.md` 重要规则第 9 条），根 log 只记平台级事项，子项目细节一律进 `Project/ALLBot部署/CHANGELOG.md`。
- [ ] 运行目录（仓库外）改动均有修复说明 + 改动摘要落库（根 BRD 协作规则第 7 条）

### 3. 质量验收

- [ ] 无敏感信息（API Key、WS token、仪表盘密码）提交到仓库
- [ ] 无未说明的 TODO/FIXME 遗留
- [ ] 依赖与环境在 README 中声明
- [ ] 日志已开启，排查链路可复现

### 4. 交接验收

- [ ] 测试 Agent 在 CHANGELOG 中签字结论（通过 / Bug 清单打回）
- [ ] Task.md 任务状态全部流转到「已关闭」（仅测试 Agent 可勾选）

---

## 不做的事情（边界）

- ❌ 不编写新的人设（丛雨人设已由用户定义，仅验证其在总结/转发场景的呈现）
- ❌ 不启用定时自动群总结（`auto_analysis_time` 保持关闭；如需改为定时，用户另行决策）
- ❌ 不在本仓库存储任何运行目录配置、密钥或聊天记录
- ❌ 不修改 SnowLuma / QQ 官方侧配置（协议端由用户维护）

---

## 已知风险与待确认项

| # | 风险/待确认 | 影响 | 处置 |
|---|---|---|---|
| 1 | 空 id 白名单语义 | 普通群友可能无法用指令 | **已实测：空白名单直接放行**（`enable_id_white_list=true` 时 `id_whitelist=[]` 不拦截）；插件命令声明为管理员命令，普通成员收到明确无权限提示，符合预期 |
| 2 | 群分析插件 `llm_provider_id=""` | 插件 LLM 可能无法调用 | **已实测：留空会回退当前会话模型**（claude-opus-5/claude-opus-5），LLM 请求成功，无需硬编码 provider |
| 3 | ~~消息量不足（<200 条）不出报告~~ | 小群无法总结 | **已实测且用户确认正常：手动触发不受 200 条限制**；定时分析仍受此限（定时任务未启用） |
| 4 | 合并转发在 SnowLuma/OneBot 的兼容性 | 长回复转发失败 | **已实测：合并转发卡片可点开，且普通 LLM 长回复已三次自动显示为 `forward`**；正式验收时再核对卡片内容与展示 |
| 5 | 旧阈值 1500 且流式回复开启 | 超阈值内容仍被流式直发 | **已修复并验证超阈值路径**：QQ 档非流式、关闭自动分段。⚠️ **勘误（2026-09-15 第二轮实测）**：阈值**当前 = 50**（原记 30 已被后续改动覆盖）→ 真实分界「恰好 50 字直发、51 字起转卡片」；边界复验按实测 50 判定 |
| 6 | `log_file_enable` 改动需重启生效 | 日志未落盘 | 配置已保存，接手 Agent 经 Launcher 正常重启后验证 `core/data/logs/astrbot.log` |
| 7 | `/群漫画` 缺绘图供应商（`drawing_provider_overrides` 为空） | 漫画无法出图 | **已解决**：配置 `openai_images` / `gpt-image-2`，供应商测试与真实群内漫画生成、发送均成功；API Key 仅保存在 AstrBot 本机配置 |
| 8 | 🚨 `wake_prefix` 丢失 `/`（2026-09-15 15:46 后） | **所有 `/` 命令静默失效**（`/群分析` `/群漫画` `/分析设置` 全部无响应、且无任何报错提示） | **由用户本人修复**：WebUI → 配置 → 唤醒前缀加回 `/`，`abconf` 与 `cmd_config.json` 两处都确认；修复后交测试 Agent 复验。详见功能 4.6 / Task.md 任务 20 |
