# ALLBot部署 任务清单（AstrBot QQ 机器人「丛雨」调教）

> 📅 产出：规划 Agent（2026-09-15，第二轮）
> 📋 状态：群分析、>30 字自动转发、群漫画均已真实群内成功；本轮定位到一处**导致所有 `/` 命令失效**的配置根因，并完成用户 5 项新需求的口径确认与立项
> 📌 任务状态流转：`待办 → 修复中 → 待复验 → 已关闭`（已关闭仅测试 Agent 可勾选）
> 📌 **规划 Agent 第三轮（2026-09-15 晚，交接核实）**：两个新插件（图片库 v1.2.0 / 纯唤醒 v1.1.0）**已加载运行**、日志落盘 ✅、群漫画 6 次真实出图 ✅（详见本文件「第三轮核实快照」）。⚠️ 但**唤醒前缀现状为 `["/"]`——名字前缀「丛雨」「丛雨酱」在恢复 `/` 时被覆盖丢失**，导致任务 18 的「叫名字触发」当前不可能生效，属**待用户修复的阻塞项**（见任务 11 / 18 / 20）。

---

## 🚨 当前最高优先级（2026-09-15 第二轮，规划 Agent 定位）

**现象**：用户实测「输入 `/群漫画 3` 没有任何反应」。

**根因（源码级，已确认，零代码可解）**：`wake_prefix` 不含 `/`。
- `astrbot/core/star/filter/command.py:191-192`：命令过滤器**首行**即要求 `event.is_at_or_wake_command` 为真，否则直接 `return False`——**命令完全不触发、也不给任何提示**。
- 该标志仅在 `astrbot/core/pipeline/waking_check/stage.py:102-148` 命中以下任一条件时置真：消息以 `wake_prefix` 中某项开头（L107）、或 @ 机器人 / @全体 / 引用机器人消息（L123-139）、或私聊（L141-148）。
- **实测现状**：`abconf_626c9487-….json` 顶层 `wake_prefix = ["丛雨","丛雨酱"]`、`cmd_config.json` 顶层 `wake_prefix = []`——**两处均不含 `/`**。故在群里直接发 `/群漫画 3`（未 @ 机器人）时 `is_at_or_wake_command` 恒为 `False` → 静默丢弃。
- **与任务 11 的冲突（重要）**：任务 11 曾要求「保留原前缀并补 `/`」并流转为待复验；本轮实测该 `/` **已不在配置中**（配置档 mtime `2026-09-15 15:46:33`，晚于任务 11 的修复）——**修复被覆盖/回退、问题复现**。
- **交叉印证**：开发 Agent 记录 `11:45` 用户在群 1095608283 真实触发 `/群漫画` 成功（任务 13），说明**当时 `/` 尚在**；`15:46` 配置变更后才失效。证据链闭合。
- **修复方式（需用户本人执行或明确授权，规划 Agent 不擅自改运行配置）**：WebUI「配置 → 唤醒前缀」把 `/` 加回列表，最终形如 `["丛雨","丛雨酱","/"]`；`abconf` 与 `cmd_config.json` **两处都要确认**。保存即热生效。
- **修复后预期**：`/群漫画 3` = 最近 3 天；`/群分析`、`/分析设置` 等同步恢复。

**✅ 已修复（2026-09-15 晚，用户本人操作）**：实测两处顶层 `wake_prefix` 均为 `["/"]`，`/` 已回到列表，`/分析设置` 等命令恢复响应。

**🔴 但产生新阻塞（规划 Agent 第三轮实测）**：保存 `/` 时**「丛雨」「丛雨酱」两个名字前缀被覆盖删除**。后果：① 群内直发「丛雨」**不再唤醒机器人**（前缀不匹配 → `is_at_or_wake_command` 恒假 → 连 LLM 都不触发）；② 任务 18「吾辈在！」的**叫名字触发失去前提**，当前仅**纯 @** 一条路径具备触发条件。**两个目标须同时满足，建议最终值 `["丛雨","丛雨酱","/"]`**，须用户在 WebUI 保存。

**🔴 口径更正（2026-09-16 凌晨，用户群内实测反馈；上两轮「把 `/` 加回前缀」的方向作废）**

- **`/` 不放进 `wake_prefix`。** 用户把 `/` 加入前缀后，**群里任何以 `/` 开头的消息都被当成唤醒**（包括群内其它 bot 的指令），造成误触发、场面尴尬，用户已移除。本文件顶部「当前最高优先级」以及任务 11 / 18 / 20 中所有"加回 `/`"的建议，**一律以本条为准作废**。
- **正确唤醒方式 = @ 机器人**（`@丛雨`）：`is_at_or_wake_command` 在「@ 机器人 / @全体 / 引用机器人消息 / 私聊」时同样置真（`waking_check/stage.py:123-148`），**与 `wake_prefix` 无关**。用户实测用法：`@丛雨` + 说「群漫画」，即可触发。
- **实测现状（2026-09-16 02:04 只读）**：`cmd_config.json` 与 `abconf_626c9487-….json` 顶层 `wake_prefix` **均为 `[]`**（mtime `00:05:55` / `00:07:13`）。
  - 后果：群内直发「丛雨」**不唤醒**（名字不在前缀里）→ 任务 18 的「叫名字触发」**当前不具备前提**；**纯 @ 触发可用**。
  - 若要恢复「叫名字」：把 `["丛雨","丛雨酱"]` 存入前缀即可（**仍然不要加 `/`**）。

---

> 🧭 **规划 Agent 第二轮摘要（2026-09-15 下午，用户已回复口径）**
> - **本轮结论**：定位到上方的 `/` 命令失效根因（见任务 20）；完成用户 5 项新需求的口径确认（任务 15–19），其中任务 15 **降级为零代码**、任务 17/18 **正式立项**、任务 19 **用户已自行解决**。**2026-09-15 16:05 更新：任务 17/18 口径已全部确认、规格定稿，可交开发 Agent 开工。**
> - **用户已回复的五项口径**：① 群漫画「最近几天够用」，**不需**指定历史某一天；② 存图 = **存群友发送的图片**（规格见任务 17）；③「吾辈在！」话术固定、@ 与「丛雨」「丛雨酱」均触发、触发词可编辑（规格见任务 18）；④ Markdown 关闭 —— **用户已自行改丛雨人设解决**（见任务 19，规划 Agent 已读库核实）；⑤ 仓库根 `data/` 敏感目录 —— 用户明确「放着不管」，**仅记录风险、不作任何处置**。
> - **⚠️ 本轮实测勘误三处**（均以实测为准）：QQ 档「丛雨丸」`forward_threshold` **实测 = 50**（非任务 5/16 原记的 30）；`log_file_enable` **实测 = false**（非任务 1/12 所记「已开启」，日志文件最后写入停在 `11:42:51`）；`wake_prefix` **实测不含 `/`**（见上方最高优先级）。
> - ✅ **用户已拍板（2026-09-15 16:05）**：任务 18 采用**方案 A**（纯唤醒才回「吾辈在！」，带内容则放行 LLM）；任务 17 多图**逐条发**、命名 / 去重由开发 Agent 自定。**两者规格定稿，可交开发 Agent。**
> - **上一轮（第一轮）摘要已并入本节**；更早的交接细节见本子项目 `CHANGELOG.md`。

> 🧭 **开发 Agent → 规划 Agent 接手摘要（2026-09-15）**
> - **可作为已完成事实规划**：`/群分析` 已真实成功；普通 LLM 长回复在新配置下已由 QQ 消息历史确认自动发送为 `forward`；`/群漫画` 已真实生成并发出 `text,image`。无需重复配置命令前缀、管理员、人设继承、30 字阈值或 GPT Image 2 供应商。
> - **仍需安排闭环**：任务 5 仅缺「恰好 ≤ 阈值」的真实群内边界展示；任务 12 需 Launcher 正常重启后检查文件日志；其余待复验项由测试 Agent 按 BRD 正式验收。开发 Agent 不勾选 `[x]`。
> - **需先问用户再规划**：任务 7/8/9（衍生转发插件、`segmented_reply`、定时群总结）；用户已明确当前不自动启用定时群分析。
> - **运行与保密边界**：实际实例目录见 BRD；API Key 只在本机运行配置，不进仓库。仓库根 `data/` 为未跟踪目录，本轮未提交，规划时不要把它当作项目来源或纳入版本控制。

## 开发阶段（按序执行）

- [ ] 任务 1：基线验证——确认 astrbot 双进程正常、与 SnowLuma 连接正常、群内普通聊天回复正常；开启 `log_file_enable=true` 并确认日志落盘（改 `cmd_config.json`）（待复验）
  - 📌 2026-09-15 规划 Agent：基线部分（Launcher PID 2456 → venv 4304 → Python 9076 双进程常驻、SnowLuma 连接正常、群内聊天正常）已有实测证据，状态由「修复中」流转为「待复验」；**「确认日志落盘」子项已由任务 12 单独承载**，不在此重复流转，避免同一件事两处挂状态。
  - 📌 勘误（2026-09-15 第二轮实测）：**`log_file_enable` 当前实测 = `false`**（`cmd_config.json` 与 `abconf` 均如此），日志文件 `core\data\logs\astrbot.log` 最后写入停在 `11:42:51`、仅 1200 字节——即该项**未保持开启**。以实测为准。
- [ ] 任务 2：插件命令链路实测——群内以 `/` 前缀依次触发 `/群分析`、`/群漫画`、`/分析设置` 等，记录每个指令的响应/报错；确认空 id 白名单语义（普通群友能否触发），必要时调整 `id_whitelist` 或权限配置（待复验）
  - 📌 前置阻塞（2026-09-15 第二轮）：本任务**依赖任务 20 修复**——在 `wake_prefix` 不含 `/` 的情况下，所有 `/` 命令均静默失效，本任务无法真实取值。
- [ ] 任务 3：群分析插件 LLM 连通——实测 `/群分析` 是否调用 LLM 产出报告；若 `llm_provider_id=""` 导致失败，配置为可用 provider（如 `claude-opus-5/claude-opus-5`）并复测（待复验）
- [ ] 任务 4：群总结内容验收——消息量达标群内 `/群分析` 产出完整报告（话题/称号/金句/锐评，image+text 正常展示），并确认报告以丛雨人设口吻撰写（`keep_original_persona=false` 时提示词自带人设视角要求，需实测确认）（待复验）
  - 📌 勘误（2026-09-15 规划 Agent）：括号内「`keep_original_persona=false` 时提示词自带人设视角要求」与 `_build_system_prompt` 实现**相反**，已实测证伪——false 时返回 None、丛雨人设**不**加载，正是任务 10 的根因；正确口径是 **`keep_original_persona=true` 才继承已有丛雨人设**（`use_plugin_specific_persona=false` 保持）。原文保留留档，验收时按勘误口径判定。
  - 📌 另注：本任务「消息量达标」为定时场景前提；手动 `/群分析` 绕过 `min_messages_threshold=200`，不得因群消息量不足判本任务失败（详见 BRD 验收标准功能项勘误）。
- [ ] 任务 5：转发聊天记录长短分流——用户明确标准为 ≤30 字直接发送 1–2 条普通消息、>30 字转合并转发卡片；实际 QQ 配置已将 `forward_threshold` 改为 30，关闭会绕过卡片转换的流式回复，并关闭可能把短内容拆成超过 2 条的自动分段（待复验）
  - 📌 勘误（2026-09-15 规划 Agent）：「（无需重启）」表述不完整，易被误读为「改完磁盘即生效」。正确口径：**改动须经对应配置档（QQ 实际档「丛雨丸」）的配置 API 保存才热生效、无需重启；直接编辑磁盘 JSON 不会更新运行对象**，与 BRD 验收标准、README「长短分流标准」一致。
  - 📌 2026-09-15 开发重开：此前"1500 字卡片已实测"不能证明用户要求的 30 字分界。运行配置原仍为 1500 且 `streaming_response=true`，流式结果会跳过 `ResultDecorateStage` 的转发判断。本轮已通过配置 API 改为阈值 30、非流式并关闭自动分段；10 项自测通过，OneBot 普通群消息与群合并转发协议实发成功。配置更新后，QQ 群消息历史进一步确认 11:23:15、11:23:46、11:27:23 三次普通 LLM 长回复均为 `forward`，证明 >30 字自动链路已生效；仅"恰好 ≤30 字直发 1–2 条"的真实群内边界仍待正式复验。
  - 📌 **勘误（2026-09-15 第二轮实测，重要）**：该档 `platform_settings.forward_threshold` **当前实测 = 50**，**不是**本条所记的 30——**当前真实分界是 50 字**（即 51 字起转卡片，恰好 50 字仍直发）。需用户确认 50 是否符合预期；若要改回 30 或其他值，按任务 16 的改法在 WebUI 保存即热生效。以实测为准。
- [ ] 任务 6：边界与异常——消息量不足时的提示、无权限时的提示、转发失败时日志可查；汇总运行目录改动摘要落库本仓库（待复验）
  - 📌 2026-09-15 规划 Agent：运行目录改动摘要已落库（见 README「2026-09-15 开发核实与本机改动」）；「转发失败时日志可查」子项**依赖文件日志落盘（任务 12）**，复验前需先完成 Launcher 正常重启。故状态由「修复中」流转为「待复验」。

## 新增需求（2026-09-15 用户提出，规划 Agent 完成源码级核查 + 口径确认）

> 📌 本节由规划 Agent 于 2026-09-15 新增。**用户已回复全部口径**（见各任务）；除任务 16/19 为文档/运行目录事项外，其余**保持「待办」、未勾选、未执行、未启用**。

- [ ] 任务 15：`/群漫画` 支持指定时间段——**结论：用户已确认「最近几天够用」，需求降级为零代码**
  - **用户口径（2026-09-15）**：**不需要**支持任选历史某一天。
  - 因此「改造 `until_ts` + 命令层日期参数 + 三适配器同步」**不再实施**；当前唯一阻塞点是**任务 20**（`/` 命令失效导致 `/群漫画 3` 根本进不来）。**修复任务 20 后，`/群漫画 N` 即可用（N = 最近 N 天）**。
  - 保留的技术事实（仅作将来若需「锁定某一天」时的备选参考）：命令定义 `main.py:809-815` 仅接受 `days: int`（最近 N 天语义，非某个历史自然日）；底层 `onebot_adapter.py` 已支持 `since_ts` 下界，但 `:264` 的过滤为 `start_timestamp <= msg_time <= now`——**只有下界、没有上界**。改造点＝① `fetch_messages` 增 `until_ts` 并透传（`message_repository.py:24` 域接口同步）② `onebot_adapter.py:264` 补上界 ③ `analysis_application_service.py:1169` 手动漫画入口透传 ④ `main.py:810` 命令层解析日期；另需同步 discord / telegram / qq_official 适配器，或明确限定仅 OneBot 支持。
  - ⚠️ 附带约束：拉取受 `max_count` 上限约束（插件配置 `basic.max_messages = 1000`），`/群漫画 3` 的消息量可能被截断，复验时留意。
  - **去向：测试 Agent 在任务 20 修复后复验 `/群漫画 3`；代码改造不再立项。**
- [ ] 任务 16：转发阈值修改位置——**结论：已答复，属文档补充，无需开发（含本轮实测勘误）**
  - 字段：`platform_settings.forward_threshold`（QQ 实际档「丛雨丸」）；生效判断 `astrbot/core/pipeline/result_decorate/stage.py:414`（`if word_cnt > self.forward_threshold`，**恰好等于阈值仍直发**）；全局默认 1500 位于 `astrbot/core/config/default.py:66`（被档内值覆盖，改它无用）。
  - **⚠️ 本轮实测勘误**：该档 **`forward_threshold` 实测 = 50**（原记 30 已被后续改动覆盖）。以实测为准。
  - 改法：WebUI → 平台设置 → 选中「丛雨丸」档 → 改 `forward_threshold` → 保存即热生效；**直接编辑磁盘 JSON 不会更新运行对象**。
  - **硬前提**：`provider_settings.streaming_response` 必须为 `false`（本轮实测 = `false` ✓），否则流式结果提前进入发送阶段、跳过该长度判断。
  - 文件位置（含敏感信息，**只引用位置、严禁入库**）：`core\data\config\abconf_626c9487-1b19-4180-8878-48a1b85b26fe.json`。
  - **去向：规划 Agent 已并入 README 说明章节。**
- [ ] 任务 17：**存图功能（群友图片库）——结论：口径已明确，本轮正式立项，交开发 Agent**（待复验）
  - **用户口径（2026-09-15）**：存的是**群友发送的图片**；之后按「关键词 + 扩展名」检索并发出。
  - **存图规则**：用户发一张图片，并在**同一条消息**或**对该图片的回复**中带 `/c 大肥鱼.jpg` → 把该图片归档到关键词「大肥鱼」。
  - **发图规则**（**必须识别到 `<关键词>.jpg` 才触发**；只出现关键词、不带 `.jpg` 时**完全无反应**）：
    | 输入示例 | 期望行为 |
    |---|---|
    | `大肥鱼.jpg` | 发 1 张 |
    | `大肥鱼.jpgx3` | 发 3 张 |
    | `大肥鱼.jpgx15`（库存仅 10 张） | 发全部 10 张，随后补一句「**一张也没有了...**」 |
    | `哦鲸鲸.jpg`（库中无此关键词） | 回「**换个关键词逝世吧...**」；**若发送者是主人**则回「**主人换个关键词逝世吧...**」 |
    | `大肥鱼`（无 `.jpg`） | **无任何反应** |
  - **技术要点 / 待确认项**：
    1. **必须避开任务 20 的唤醒陷阱**：若用 `filter.command("c")` 实现存图，同样受 `wake_prefix` 制约（`command.py:192`）；**发图触发是消息文本正则匹配**，建议用 `filter.event_message_type(GROUP_MESSAGE)` + 正则，不注册为命令，避免与 `/` 前缀机制耦合（否则又会「没反应」）。
    2. **存储结构**：建议 `core/data/meme_library/<关键词>/` 存图 + 轻量索引（JSON 或复用 `data_v4.db` 建表）；**明确不得入库仓库**（放运行目录或写入忽略规则）。
    3. **多张同名图**：用户示例「库里有 10 张大肥鱼.jpg」说明**同一关键词允许多张**；命名/去重规则需明确（如 `大肥鱼_001.jpg` 递增，或按内容 hash 去重）。
    4. **`xN` 语法**：`x` + 整数（`x3` / `x15`）；需定义非法写法的降级行为（`x0`、`xabc`、`x-1`）。
    5. **多图发送方式**：一次发 10 张时需确认是逐条发、还是走合并转发（任务 5 的 `forward_threshold` 只管纯文本字数，**不套用图片**），并留意 QQ 风控。
    6. **主人判定**：按 `admins_id`（当前实测 `["Edi", "2403232967"]`）判定「主人」身份，用于话术分叉。
    7. **管理/删除功能**：用户本轮**未提**，**不擅自加**；如需可另立任务。
  - ✅ **口径已全部确认（2026-09-15 用户回复）**：一次发多张 **逐条发**（不走合并转发）；同关键词多图**如何命名 / 去重由开发 Agent 自行决定**——用户只关注功能可用，但须满足三条底线：**不覆盖已有图、可维护、不误删**。
  - **去向：交开发 Agent**。前置：功能 4.6 的 `/` 前缀已由用户加回（否则 `/c` 存图命令不可用）；若不希望依赖前缀机制，可把 `/c` 也做成正则匹配（见要点 1）。
- [ ] 任务 18：被 @ 或叫「丛雨」时立即回复「吾辈在！」——**结论：无内置开关，需小型插件；方案 A 已定稿**（待复验）
  - **用户口径（2026-09-15）**：话术固定为 **「吾辈在！」**（三字 + 感叹号，无其他内容）；**@ 也触发**；说 **「丛雨」「丛雨酱」也触发**；**触发词用户可自行编辑**。
  - **触发词在哪改（直接答复用户）**：触发词**就是核心配置项 `wake_prefix`**——`astrbot/core/pipeline/waking_check/stage.py:103-120` 读取它判定唤醒（当前实测：`abconf` = `["丛雨","丛雨酱"]`、`cmd_config.json` = `[]`）。**WebUI「配置 → 唤醒前缀」就是可编辑位置**，日后加词/删词都在这里改。
    - ⚠️ **但 `/` 必须留在列表中**，否则所有 `/` 命令失效（见上方最高优先级 / 任务 20）。最终建议形如 `["丛雨","丛雨酱","/"]`。
  - **⚠️ 设计冲突（必须先由用户拍板，否则插件会让她无法正常聊天）**：若「只要 `is_at_or_wake_command` 为真就回『吾辈在！』并 `stop_event()`」，则**每次唤醒都被截断**——用户说「丛雨，今天天气如何」也只会得到「吾辈在！」，正常对话彻底不可用。三种可选行为：
    | 方案 | 行为 | 后果 |
    |---|---|---|
    | **A（推荐）纯唤醒才回** | 唤醒后**消息无其他内容**才回「吾辈在！」并 `stop_event()`；有内容则放行 LLM | 「@丛雨」→「吾辈在！」；「@丛雨 今天天气」→ 正常回答 |
    | B 无条件回并拦截 | 任何唤醒都回「吾辈在！」且阻止 LLM | **无法正常对话**（不推荐） |
    | C 回一句再放行 | 回「吾辈在！」后**不**阻止 LLM | 会「吾辈在！」+ 正经回答两条，略啰嗦 |
  - **技术要点**：监听 `EventType.AdapterMessageEvent`（配 `filter.event_message_type(GROUP_MESSAGE)`），判断 `event.is_at_or_wake_command`，`yield event.plain_result("吾辈在！")`，再按所选方案决定是否 `event.stop_event()`；**处理顺序需先于 LLM 对话阶段**。实现时须与任务 20 的前缀修复共存。
  - ✅ **方案已定：A（2026-09-15 用户回复「本来就是 A」）**——唤醒后**消息无其他内容**才回「吾辈在！」并 `stop_event()`；带内容则放行 LLM，不干扰正常对话。B / C 方案作废，仅上表备查。
  - 🔴 **阻塞勘误（2026-09-15 晚实测）**：插件 v1.1.0 **已加载**（日志 `23:11:40 纯唤醒回复插件已加载。`），但**当前 `wake_prefix = ["/"]`，其中已无名字**——插件按「实际会话 `wake_prefix`」判定，故**叫「丛雨」「丛雨酱」触发当前不可能生效**，仅**纯 @** 具备条件。**须先由用户恢复名字前缀**（建议 `["丛雨","丛雨酱","/"]`）方可验收本任务。
  - 📌 另注：日志中 `23:17:16` 的「好的主人！吾辈在呢！」是**引用消息场景下的 LLM 自由回复**，**不是**本插件的固定话术，不得作为插件生效证据；插件生效的真判据是群内出现与 `reply_text` 完全一致的「吾辈在！」。
  - **去向：交开发 Agent**（处理顺序须先于 LLM 对话阶段；与功能 4.6 的前缀修复共存）。
- [ ] 任务 19：关闭 Markdown 输出——**结论：用户已自行以人设约束解决，本轮只需复核**
  - **核实结果（2026-09-15 规划 Agent 读 `core\data\data_v4.db` → `personas` 表）**：丛雨人设 `system_prompt` 已于 **2026-09-15 07:28**（行更新时间）加入约束，原文：「**特别注意：回复时不要使用Markdown格式，禁用星号、md格式的"- "列表，其他不变，就和普通说话一样，用纯文本回复**」。即**方案 A 已由用户本人执行完毕**，本轮**未再改动运行目录**。
  - 原根因保留：LLM 输出 Markdown；`result_decorate/stage.py` **不含任何 Markdown 剥离逻辑**，且全局 `t2i=false`，故 `**加粗**`、`## 标题` 会原样发到 QQ。
  - **已知限制**：人设约束属 **prompt 软约束**，非结果层硬过滤——偶发星号仍可能出现。若要 100% 干净：方案 B 开 `t2i=true` 渲染成图（会改变输出形式，且与字数分流交互，须实测）；方案 C 插件层剥离（开发量最大）。**两者本轮均不立项。**
  - **去向：测试 Agent 在真实群内复核「回复中不再出现 `**` / `- ` 等 Markdown 痕迹」。**
- [ ] 任务 20：**🚨 `/` 命令全部失效（本轮定位的根因，零代码可解）**——详见本文件顶部「当前最高优先级」章节
  - **去向：由用户本人执行配置修复**（用户已于本轮明确「我自己加」）——WebUI → 配置 → 唤醒前缀把 `/` 加回列表，`abconf` 与 `cmd_config.json` 两处都确认，保存即热生效；**修复后交测试 Agent 在真实群内复验「零回复 → 正常响应」**，并连带复验 `/群漫画 3`。
  - ✅ **已修复（2026-09-15 晚实测）**：两处顶层 `wake_prefix` 均为 `["/"]`，`/分析设置` 恢复正常，`/群漫画` 亦有多次真实成功记录 → **「零回复 → 正常响应」已达成**。
  - 🔴 **但修复不完整（见任务 11 / 18）**：保存 `/` 覆盖掉了名字前缀，「丛雨」「丛雨酱」丢失。**本任务的正确终态是 `["丛雨","丛雨酱","/"]`**（命令可用 + 名字唤醒双目标），当前只完成一半，**交测试 Agent 前须先补齐名字**。

- [ ] 任务 21：合并转发卡片标题改为「丛雨」——**结论：需改代码，极小改动（1 行核心 + 1 行插件）；本轮立项，交开发 Agent**（待复验）
  - **需求（用户 2026-09-15）**：卡片标题现为「AstrBot的聊天记录」，要显示成「**丛雨的聊天记录**」。
  - **根因（源码级）**：卡片标题取自转发节点的 `nickname` 字段——`astrbot/core/message/components.py:697-703`（`Node.to_dict()` 输出 `"nickname": self.name`）。
  - **改动点 1（LLM 长回复卡片，用户截图所指）**：`astrbot/core/pipeline/result_decorate/stage.py:417` 硬编码 `name="AstrBot"` → 改 `name="丛雨"`。⚠️ **这是核心文件补丁**，AstrBot 升级后会被覆盖、需重打（已登记 README「开发核实与本机改动」）。
  - **改动点 2（群分析报告卡片）**：插件 `src/infrastructure/platform/base.py:190` 的 `self_name = "分析报告"` → 改 `"丛雨"`（插件自身代码，一并统一，否则该卡片会显示「分析报告的聊天记录」）。
  - **⚠️ 不要用插件钩子改**：`on_decorating_result`（`stage.py:163`）**早于**转发节点构造（`stage.py:415`），钩子里拿不到 Node；若在钩子内自行构造 Node，会**绕过** `reply_prefix` / `segmented_reply` / `t2i` 处理（`stage.py:200-406`），副作用更大。
  - 可选不改：`src/application/commands/template_command_service.py:102,122` 的「模板预览」节点名属调试用途，默认保留。
  - **去向：交开发 Agent**（与任务 17 / 18 同批交付；交接提示词已同步）。

- [ ] 任务 24：存图插件支持**一次存多张**（用户 2026-09-16 新增；交开发 Agent）（修复中）
  - 2026-09-16 开发认领：仅修改图片库插件与现有回归测试；新增「加图」并保留 `/c`，按实际新增成功数回复，重复与部分失败逐张保留提示；不改核心/唤醒前缀，不删除图库、不重启程序。
  - **需求**：同一条消息里带**多张图片** + `加图 测试鱼.jpg`（例：【图片】【图片】+ `加图 测试鱼.jpg`）→ **这两张（或 N 张）全部归档到关键词「测试鱼」**。
  - **回复文案分叉（已定稿）**：本次只存 **1 张** → 回「**存好了**」；一次存了 **x 张** → 回「**x 张都存好了**」。逐张重复 / 部分失败的话术沿用 4.8 已有的可编辑字段。
  - **技术要点**：① `加图` 是**新增触发关键字**，须与既有 `/c` **并存**（不得让 `/c` 回退），是否并入 `store_command` 配置项由开发 Agent 决定；② 须遍历消息段中**所有 `Image` 组件**逐个下载入库；③ 须处理「多图但部分成功」的计数与话术。
  - **去向：交开发 Agent**。规格已写入 `BRD.md` 4.10。

- [ ] 任务 25：群漫画**分辨率与文字清晰度调优**（用户 2026-09-16 提出；规划 Agent 已完成源码级归因，交开发 Agent 落地实测）（修复中）
  - 2026-09-16 开发认领：按处方单变量对照试验，先不发送群消息；原始图尺寸、文字可读性分别取证，不以请求参数或文件大小替代实际分辨率。话题上限调整会同步影响群分析报告，已告知用户。
  - **用户反馈**：出图**很糊、字看不清**。
  - **归因（硬证据，非猜测）**：
    1. **分格数 = 话题数**：`comic_analyzer.py:12` 的 prompt 强制「**每个话题一个分格**」，条数取自 `analysis_features.max_topics`（**实测 = 5**）→ 一张图里画 5 格。
    2. **实际出图分辨率偏低**：`reports/` 内 6 张漫画**实测均为 `1672×940` PNG**。请求侧 `drawing_provider_overrides.image_size = "auto"` 经 `drawing_client.py:281-282` 解析为 **1792×1008**，而**上游实际返回 1672×940**（缩水约 6.7%）→ 说明中转站对尺寸有介入。
    3. 合起来：**1672×940 里塞 5 格 ×（气泡台词 + 底部字幕条）**，横排时单格宽仅约 334px → 中文必须缩到极小才装得下 → 再经 QQ 二次压缩 → **必糊**。
  - **调参处方（按性价比排序，均可经 WebUI 保存热生效）**：
    1. **`analysis_features.max_topics` 5 → 2 或 3**（**最有效**）：格数减半 → 单格面积翻倍、字号显著变大。⚠️ 副作用：**群分析报告的话题条数会同步减少**（同一个开关，须用户知情）。
    2. **`image_size` 由 `auto` → `2k`**（解析为 2560×1440）或直接填官方尺寸 `1536x1024`；**必须实测确认上游是否真按该值返回**（`auto` 已被缩水，`2k` 未必生效）。
    3. **`aspect_ratio` 由 `16:9` → `4:3` 或 `1:1`**：多格横排在 16:9 上每格过窄；方图 / 4:3 每格面积更大。
    4. **缩短文字**：现 prompt 允许台词 15 字、底部标题 **30 字**（`comic_analyzer.py:13,16`）→ 建议底部标题压到 **12 字内**（属插件 prompt 改动）。
    5. **发送侧保真**：出图约 2.2–2.3 MB，超过 `napcat_stream_threshold_mb = 2`；QQ 群图片默认按标准画质压缩，**验收时须用「查看原图」核对**，别把 QQ 压缩误判成插件问题。
  - **去向：交开发 Agent 落地实测**（按 1 → 2 → 3 逐项对照，记录每组的实际返回尺寸与观感）。

- [ ] 任务 26：**禁言插件（「丛雨闭嘴 x 分钟」）——用户 2026-09-16 新增；规划 Agent 已完成源码级核查并定稿规格，交开发 Agent**（待办）
  - **用户原话需求**：输入某个指令（例：`丛雨闭嘴 30分钟`）→ 丛雨回「好的，接下来我会闭嘴x分钟」→ 之后**无论用什么指令都不会说话**（＝真正的禁言）→ 到时间自动恢复。**指令词与回复词都可编辑。**
  - **「astrbot 有没有原生功能」——核查结论**：核心**无禁言 / 静默配置项**。最接近的是**会话整体启停**（`session_status_check/stage.py:24` → `SessionServiceManager.is_session_enabled`，值存 `sp` 的 `session_service_config.session_enabled`，Dashboard 会话管理可手动开关），但**不能定时自动恢复、不能回台词、且会连插件一起拦**（该 stage 在 pipeline 最前）→ **须以插件实现，不使用原生会话开关**。
  - **三条可行性证据（已用源码证实）**：① 用 `@filter.event_message_type(GROUP_MESSAGE)`、**不注册为命令** → **不受 `wake_prefix` 限制**（`waking_check/stage.py:219` 因该 filter 通过而置 `is_wake=True`，故 L243-244 的 `if not is_wake: stop_event()` 不会拦掉；只有 `filter.command` 才要求 `is_at_or_wake_command`，见 `star/filter/command.py:191-192`）→ 不带 @ 的「丛雨闭嘴 30分钟」**可被捕获**；② `event.stop_event()` 可阻断后续全部 handler 与 LLM（`star_request.py:37`、`:51` 的 `if event.is_stopped(): break`）；③ 顺序按 priority **数值降序**（`star_handler.py:26`），**越大越先** → 本插件优先级须**高于** `presence_reply`（`maxsize + 1`），否则禁言期会先蹦「吾辈在！」。
  - **规格（完整版见 `BRD.md` 4.12）**：时长支持 分钟 / 秒 / 小时，上限建议 1440 分钟；禁言期**一切静默**（含「吾辈在！」、取图、`/群分析`、`/群漫画`）；期间再下指令**重置计时**；到期以「`asyncio` 定时器 + 每条消息惰性判断」双保险（**实例重启后仍能正确恢复**）；状态落盘插件数据目录；默认**按会话（群）**、可配置切全局；口令 / 文案 / 上限 / 范围全部 `_conf_schema.json` 可编辑（参照 4.8 与任务 22 做法）。
  - **不做**：不加群内管理 / 查询命令（用户未提）；**不改核心**；不调用原生会话开关。
  - **去向：交开发 Agent**。按用户指定，后续功能统一以 **AstrBot 插件**形式开发，参照官方文档 `https://docs.astrbot.app/dev/star/plugin-new.html`。

## 可选迭代（用户决策后执行）

> 📌 2026-09-15 规划 Agent 分流：任务 7/8/9 **均需用户决策**，维持「待办」状态、**不自行启用、不自行立项**；其中定时自动群总结 `auto_analysis_time` 按用户既定决策**保持关闭**。
> 📌 2026-09-15 第二轮补充实测：`auto_analysis_time = ["23:00"]` 但 `scheduled_group_list = []`（白名单为空），故**当前实际不会注册定时任务**；`segmented_reply.enable` 实测 = `false` ✓（与任务 8「当前关闭」一致）。

- [ ] 任务 7：衍生插件——编写"丛雨人设化转发"小插件：长内容按丛雨口吻分段组织为转发节点，转发记录 `uin/name` 呈现为丛雨身份
- [ ] 任务 8：`segmented_reply` 调优——开启长回复按句拆分多条间隔发送（当前关闭）
- [ ] 任务 9：定时自动群总结——启用 `auto_analysis_time`（当前按用户决策关闭）

## 维护记录（Bug 修复看板）

> 按根 BRD 协作规则：开发/测试中发现的 Bug 一律在此新建任务，状态实时流转；仓库外改动摘要必须落库。

- [ ] 任务 10：群分析人设未加载——两个人设开关均为 false 时 get_system_prompt 返回 None；按丛雨口吻目标修正继承配置并自测（待复验）。

- [ ] 任务 11：QQ 独立配置丛雨丸仅有丛雨前缀，且管理员填昵称 Edi，导致 / 插件指令不响应；保留原前缀并补 /，按用户提供 QQ 号修复管理员（待复验）。
  - 🔴 **勘误 + 复发（2026-09-15 第二轮实测）**：本任务要求的「补 `/`」**当前已不在配置中**——`abconf` 顶层 `wake_prefix = ["丛雨","丛雨酱"]`、`cmd_config.json` 顶层 `wake_prefix = []`，两处均无 `/`。**即本任务的修复被覆盖/回退，问题完整复发**，直接导致用户本轮实测 `/群漫画 3` 无反应。请与**任务 20** 合并处理：管理员部分（`admins_id = ["Edi","2403232967"]`）已修复 ✓，前缀部分**需重做**。
  - 📌 **第三轮状态（2026-09-15 晚实测）**：`/` 部分 ✅ **已由用户加回**（两处顶层 `wake_prefix` 均为 `["/"]`），管理员部分 ✅ 已修复；但名字部分 🔴 **再次丢失**——恢复 `/` 的那次保存把「丛雨」「丛雨酱」删掉了。本任务（保留原前缀 + 补 `/`）**目标仍未达成**，须一次性设为 `["丛雨","丛雨酱","/"]`。与任务 20 同源同修。
- [ ] 任务 12：文件日志开启后不会在当前进程加载日志 sink，需通过正常启动器重启并核对落盘（待办）。**去向：交开发 Agent** —— 经 Launcher 正常重启后核对 `core/data/logs/astrbot.log` 增长；前置条件为用户授权重启实例（规划 Agent 不擅自启停、不按进程名强杀）。
  - 📌 勘误（2026-09-15 第二轮实测）：`log_file_enable` **当前实测 = `false`**——即该开关**又回到关闭状态**，本任务在重启验证前需先重新开启并保存。
  - ✅ **已落盘（2026-09-15 晚实测，规划 Agent 只读核对）**：经用户授权的正常重启后，`core\data\logs\astrbot.log` 实测 **115,590 B**，时间跨度 `11:16:02 → 23:56:38`（620 行，含 `23:08` 重启后的启动与插件加载记录）。**前置条件（Launcher 正常重启）已完成，落盘有效**——本任务可交测试 Agent 复验确认后关闭。
  - 📌 注：`abconf` 内 `log_file_enable` 字段读数为 `false`、`cmd_config.json` 为 `true`，而日志实际在写入——以**日志文件实际增长**为落盘判据，不单看该字段。
- [ ] 任务 13：群漫画绘图供应商——已在 `daily_comic.drawing_provider_overrides` 配置 `openai_images`（`gpt-image-2`），供应商独立生图返回 PNG；11:45 用户在群 1095608283 真实触发 `/群漫画`，话题提取、分镜、生图、落盘与 QQ 图片发送全部成功（待复验）。**去向：测试 Agent** —— 按真实群消息 `11:47:08` 的 `text,image` 结果正式验收；不得在仓库记录 API Key。
  - 📌 2026-09-15 第二轮补充：该次成功同时**反证了当时 `/` 尚在 `wake_prefix` 中**（命令能进即前缀正常）；`15:46` 配置变更后失效，证据链闭合。
  - ✅ **第三轮证据（2026-09-15 晚实测）**：`plugin_data\astrbot_plugin_qq_group_daily_analysis\reports\` 实有 **6 张真实出图**——`comic_…_114703`（2,300,464 B）、`…_115746`（2,192,898 B）、`…_222509`（1,997,904 B）、`…_231353`（2,332,106 B）、`comic_…_231952_manual`（2,195,771 B）；另有群报告 `report_1095608283_…_005343` / `_231922` 与另一群 `report_859226771_…_232309`。用户回「群漫画好像可以了」。**最终验收仍以真实群内发送结果为准。**
- [ ] 任务 14：规划事实需勘误——实际 QQ 使用独立配置档；手动群分析绕过 200 条门槛；丛雨人设需要开启继承；磁盘编辑不等于热更新。开发仅在 README/CHANGELOG 记录证据，BRD 需求部分交规划 Agent 确认（待复验）。**规划 Agent 已于 2026-09-15 完成 BRD 四处勘误**（头部状态、功能 1「已验证结论」重写、验收标准补注阈值热生效须经配置档 API、副本路径追加勘误标注）；其中四处事实 BRD 前轮已吸收，核对无残留误述。交测试 Agent 复核文档与实测一致性。

## 本轮自测证据（2026-09-15）

- 基线：Launcher PID 2456 → venv PID 4304 → Python PID 9076，Windows session 1；工作目录为 README 中的 core，历史仍在 data/data_v4.db。未新启动聊天实例，未清理历史或终止用户进程。
- 00:49 用户发送三个 / 指令已进入事件总线，但旧配置前缀和管理员不匹配。随后通过原生管理 API 修改实际 QQ 配置，保留原前缀与管理员条目。
- 00:52 用户重发后，/分析设置正常响应；/群分析真实执行完成，结构化 trace 为 succeeded，产出话题 2、称号 2、金句 5、质量锐评 1，报告落盘；用户确认除漫画外正常。
- /群漫画旧轮次因缺少绘图供应商失败；本轮配置 `openai_images` 后于 11:47 真实生成约 2.3 MB 漫画并发送到测试群，任务 13 流转为待复验。
- 群分析与漫画均实际回退到当前会话 claude-opus-5/claude-opus-5，人设加载日志已出现；llm_provider_id 保持为空，未写死替换。
- tests/check_installed_source.py：本轮 10 项自测通过，覆盖 29/30/31 字边界、实际配置档，以及转发节点到 OneBot `send_group_forward_msg` 的映射。配置更新后的普通 LLM 长回复已在测试群三次显示为 `forward`；仅 ≤30 字的精确群内边界仍需测试 Agent 复验。
- 原有自动分析白名单为空且初始化日志确认不注册定时任务；未开启任务 7–9。
- 日志设置已保存但旧进程尚未建立文件日志，待 Launcher 正常重启；长回复卡片可点开、普通成员真实权限提示、≥200 条场景和报告人设听感仍交测试 Agent 复验。

## 第二轮实测快照（2026-09-15 下午，规划 Agent 只读采集）

> 📌 全部为**只读**采集，未改动运行目录任何文件。

| 项 | 实测值 | 与既有记录的差异 |
|---|---|---|
| `abconf.wake_prefix` | `["丛雨","丛雨酱"]` | 🔴 **不含 `/`** → 所有 `/` 命令失效（任务 20） |
| `cmd_config.wake_prefix` | `[]` | 🔴 空数组，同样无 `/` |
| `abconf.platform_settings.forward_threshold` | **50** | ⚠️ 原记 30，已变更（任务 5/16） |
| `cmd_config.platform_settings.forward_threshold` | 1500 | 兜底值，被档内值覆盖，改它无用 |
| `provider_settings.streaming_response` | `false` | ✓ 满足阈值生效前提 |
| `log_file_enable` | `false` | ⚠️ 原记已开启（任务 1/12） |
| `log_file_path` | `logs/astrbot.log` | 日志最后写入 `11:42:51`，1200 字节 |
| `admins_id` | `["Edi","2403232967"]` | ✓ 管理员已修复（任务 11 已完成部分） |
| `default_personality` | `丛雨` | ✓ |
| 丛雨人设 `system_prompt` | 已含「不要使用 Markdown」约束（行更新时间 `2026-09-15 07:28`） | ✓ 任务 19 已由用户自行解决 |
| `daily_comic.enable_daily_comic` | `true` | 漫画功能开启 |
| `daily_comic.drawing_provider_overrides` | 已配 `openai_images` / `gpt-image-2`（含密钥，**不在仓库记录**） | ✓ 任务 13 供应商已就绪 |
| `basic.max_messages` | 1000 | 影响 `/群漫画 N` 回溯截断 |
| `basic.min_messages_threshold` | 200 | 仅约束定时分析，手动 `/群分析` 绕过 |
| `analysis_features.keep_original_persona` | `false` | ⚠️ 见任务 4 勘误（false 时不继承丛雨人设） |
| `auto_analysis.auto_analysis_time` | `["23:00"]`，但 `scheduled_group_list = []` | 白名单为空 → 实际不注册定时任务 |
| `segmented_reply.enable` | `false` | ✓ 与任务 8 一致 |
| 实例 `core` 路径 | `C:\Users\WindoseII\.astrbot_launcher\instances\4450a298-f4c2-43fa-b7f7-bd645b753fc3\core` | ✓ |
| 配置文件 mtime | `abconf` / `cmd_config` 均为 `2026-09-15 15:46` | 用户在该时刻改过配置 → `/` 丢失的时间点 |

## 第三轮核实快照（2026-09-15 深夜，规划 Agent 只读采集）

> 📌 全部为**只读**采集（git / 运行目录日志与文件清单 / 配置项），**未改动运行目录任何文件**，未启停实例。

**A. 仓库与交接**
| 项 | 实测值 |
|---|---|
| 远端 HEAD（`git ls-remote origin main`） | `b925c030457ad5eaa5632664d735aea1e96c50bf` = 本地 HEAD ✓ |
| 开发 Agent 最后提交 | `b925c03`「开发 Agent：修正存图与纯唤醒并记录待群测状态」——**已推送成功**，此前「最后提交与推送尚未执行」的说法不成立 |
| 交接时未提交改动 | 4 个（`README.md` / `CHANGELOG.md` / `Task.md` / `tests/check_group_plugins.py`），本轮核实后提交 |

**B. 运行目录（只读）**
| 项 | 实测值 | 判读 |
|---|---|---|
| `astrbot_plugin_meme_library` | v1.2.0，`23:08:46` 加载成功（`群友图片库插件已加载，图库目录 …\core\data\meme_library`） | ✅ 已加载 |
| `astrbot_plugin_presence_reply` | v1.1.0，`23:11:40` 加载成功（`纯唤醒回复插件已加载。`） | ✅ 已加载 |
| 卡片标题核心补丁 | `stage.py` name=「丛雨」+ 插件 `base.py` self_name=「丛雨」，随 `23:08` 重启加载 | ✅ 已加载（**标题实际显示须人眼看 QQ**） |
| `core\data\logs\astrbot.log` | 115,590 B，跨度 `11:16:02 → 23:56:38` | ✅ **落盘有效**（任务 12 前置达成） |
| 图库实况 | `测试鱼` 4 张、`猫猫虫` 1、`vol` 1、`表情` 1；`.trash/` 已建立为空 | ✅ 取图链路真实可用（`23:47:13~18` 连发 3 次 `猫猫虫.jpg` 均进入发送阶段） |
| 漫画出图 | `reports/` 6 张真实 PNG（`114703` / `115746` / `222509` / `231353` / `231952_manual`）+ 3 张报告 JPG | ✅ 任务 13 证据充分 |
| 转发卡片发送 | `23:17:40` / `23:31:37` / `23:39:16` / `23:40:44` / `23:49:43` / `23:50:02` 共 6 次 `[ComponentType.Node]` | ✅ 链路通；标题待人工核对 |

**C. 配置项（只读）**
| 项 | 实测值 | 判读 |
|---|---|---|
| `cmd_config.json` 顶层 `wake_prefix` | `["/"]` | ⚠️ 名字缺失 |
| `abconf_626c9487-….json` 顶层 `wake_prefix` | `["/"]` | ⚠️ 名字缺失 |
| `abconf.platform_settings.forward_threshold` | **50** | 与前轮一致（改它须经配置档 API 保存） |
| `abconf.log_file_enable` / `cmd_config.log_file_enable` | `false` / `true` | 字段读数不一致，但**日志实际在写**——以文件增长为判据 |
| `provider_settings.streaming_response` | 未显式设置（非 `true`） | 阈值生效前提满足 |

**D. 🔴 本轮定位的阻塞项**
- 「丛雨」「丛雨酱」名字前缀在恢复 `/` 的保存中被覆盖丢失 → **群内发名字不再唤醒**；任务 18「吾辈在！」叫名字触发**当前不可能生效**（仅纯 @ 具备条件）。
- `23:17:16` 日志中的「好的主人！吾辈在呢！」属**引用消息场景的 LLM 自由回复**，**不是**插件固定话术 → **不得作为任务 18 生效证据**。
- 待用户将 `wake_prefix` 一次性设为 **`["丛雨","丛雨酱","/"]`**（规划 Agent 不擅自改运行配置）。

## 开发复查与真实群复测（2026-09-15 晚）

- 任务 17/18/21 仍为修复中，未勾选关闭；两个插件和标题补丁在运行目录落盘，不等于已加载。
- check_group_plugins.py：20 项隔离自测通过；check_installed_source.py：9 项通过、1 项跳过。前轮口头“22 项通过”撤回，不作验收证据。
- 任务 20：用户保存 / 后确认 /分析设置可用；当前仅 /，已提醒补回名字前缀。/群漫画 N 待本轮真实触发复验。
- 用户反馈只 /分析设置有响应；等待插件页加载确认。电脑控制工具实际调用失败，未擅自重启或绕过管理认证。
- 群内验证与证据未完成：两种存图、取图 1/x3/超量、空库存双身份、无 .jpg 静默、纯 @/叫名字、带内容正常 LLM、漫画实际出图发送、长短消息及两类卡片标题。

## 用户追加的可编辑功能与本轮交付（2026-09-15 晚）

- [ ] 任务 22：存图插件在 WebUI 编辑触发口令、取图格式和全部回复（待复验）。
  - 用户直接新增：默认成功回复“存好了”，无句号；可改为“我存好了”等。/c、.jpg、x、成功/重复/失败/普通空库存/主人空库存/超量回复均已加入 _conf_schema.json 和图库 Page 的“回复与口令”。
  - 自测：设置实际保存读回成功，恢复默认“存好了”；插件热加载 v1.2.0。交测试 Agent 做页面操作复验，规划 Agent 同步 BRD。
- [ ] 任务 23：存图随机取图 + WebUI 图库查看与编辑（待复验）。
  - 用户反例：单张取图总拿到相同图片。修复：随机选择，同批不重复；库存超过一张时连续单张避开上一张。
  - WebUI 图库已支持缩略图、原图、上传、关键词改名、图片移动、确认删除。用户明确授权 WebUI 二次确认删除；删除移入 .trash 回收目录，群内不增加管理命令。
  - 自测：27 项隔离用例通过，8 步真实管理 API 用临时图片验证通过并清理；用户原有“测试鱼”4 张均保留。页面接口已注册，浏览器画面检查因电脑工具启动失败尚未完成。
- 📌 **规划 Agent 第三轮同步（2026-09-15 晚）**：任务 22 / 23 的需求已登记进 `BRD.md` **4.8 / 4.9**（可编辑口令与回复 / 随机取图 + WebUI 图库管理）。插件 **v1.2.0 实测已加载**（日志 `23:08:46`），图库实况只读核对：`测试鱼` 4 张、`猫猫虫` 1 张、`vol` 1 张、`表情` 1 张，`.trash/` 已建立；`23:47:13~23:47:18` 用户连发 3 次 `猫猫虫.jpg` 均逐次进入发送阶段，**取图链路真实可用**。两项仍**待测试 Agent 复验页面交互**，规划 Agent 不代勾选。
- 任务 17 最新证据：用户确认同条存图、管理员/普通成员取图、无 .jpg 静默、空库存身份话术、单张库存超量尾句均正常。用户停止追加多图及回复存图群测；这两项仅隔离自测通过，仍不可勾选关闭。
- 任务 18 最新状态：用户临时暂停后重新要求实现，纯唤醒插件 v1.1.0 已安装加载，reply_text 可编辑；当前待真实纯 @ 与带内容放行验证。
- 任务 21 最新状态：用户已明确允许正常重启，核心新 start_time 已确认；报告插件也已热重载。待真实长回复/报告卡片标题确认。
- 任务 13/20：用户补回 / 后 /分析设置 正常；22:22:46 漫画 trace 成功、22:25:09 图片实际生成，用户表示漫画可用了。本轮 trace 实际 days=7，不冒充 days=3 的专项复验。


## 第四轮核实快照（2026-09-16 凌晨，规划 Agent 只读采集）

> 📌 只读采集（配置项 / 出图文件尺寸 / 仓库状态），**未改动运行目录**，未启停实例。

| 项 | 实测值 | 判读 |
|---|---|---|
| 顶层 `wake_prefix`（`cmd_config` / `abconf`） | `[]` / `[]`（mtime `00:05:55` / `00:07:13`） | 🔴 名字与 `/` 都不在前缀；**@ 唤醒不受影响**，用户用法「`@丛雨` + 群漫画」可行 |
| QQ 档 `platform_settings.forward_threshold` | 50 | 与前轮一致 |
| **漫画出图实际分辨率** | **1672×940**（6 张全部一致，PNG，2.0–2.3 MB） | 🔴 **「糊」的硬证据**；请求 1792×1008 被上游缩水 |
| 漫画分格数来源 | `analysis_features.max_topics = 5` | 🔴 5 格挤进一张 1672×940 |
| 人设参考图 | 1583×1747 | — |
| 群报告图 | 实测宽高约 2.8 万–6.2 万像素（`report_*.jpg`） | 报告图分辨率充足，**与漫画不同源**，勿一并归因 |
| 仓库 HEAD | `8fd77d0` | 第三轮提交已推送 |
| `赛马游戏Beta.exe` | 5.6 MB，PyInstaller 打包，`D:\SystemFiles\Downloads\`，mtime `2026-09-16 02:03` | 用户「未来打算做赛马功能」的参考物；**本轮仅登记，不立项、不逆向、不处置** |

## 第五轮核实快照（2026-09-16 凌晨，规划 Agent 只读采集）

> 📌 本轮为**只读**采集（核心源码 / 插件源码 / 配置项 / 仓库状态），**未改动运行目录任何文件**，未启停实例。

**A. 禁言能力核查（任务 26 的依据）**
| 项 | 实测 / 源码结论 |
|---|---|
| 核心「禁言 / 静默」配置项 | **不存在**（`config/default.py` 全文无对应 schema） |
| 最接近的原生能力 | **会话整体启停**：`pipeline/session_status_check/stage.py:24` → `star/session_llm_manager.py:162-186`，值存 `sp`（scope=`umo`，键 `session_service_config.session_enabled`）；Dashboard 可手动开关（`dashboard/schemas.py:650-653`） |
| 原生能力能否满足需求 | **不能**：无定时自动恢复、无回复台词、且在最前 `stop_event()`，插件/指令一并被拦 |
| 插件能否拦到不带 @ 的消息 | **能**：`@filter.event_message_type(GROUP_MESSAGE)` 非命令类 filter 通过 → `waking_check/stage.py:219` 置 `is_wake=True` → L243-244 不 `stop_event` |
| 插件能否阻断其它插件与 LLM | **能**：`process_stage/method/star_request.py:37`、`:51` 的 `if event.is_stopped(): break` |
| 执行顺序 | `star/star_handler.py:26` 按 priority **降序**排序 → **数值越大越先执行**；`presence_reply` 用 `maxsize + 1` |

**B. 仓库与文档**
| 项 | 实测值 |
|---|---|
| `CHANGELOG.md` 条数 | 上轮已达 **15 条上限** → 本轮**首次创建 `CHANGELOG.archive.md`** 并迁出最旧 1 条 |
| `CHANGELOG.archive.md` | **本轮首次创建**（此前从未存在） |
| 本轮修复 | `CHANGELOG.md` 上轮编辑遗留的破损行；`BRD.md` 4.6/4.7 交界处一处碎片行 |

**C. 待用户确认**
- `wake_prefix` 现为 `[]` → 任务 18「叫名字触发」**仍不具前提**（纯 @ 可用）；用户已明确**不加 `/`**。
- 禁言默认范围当前定为**按会话（群）**；是否改为**全局**待用户确认。
