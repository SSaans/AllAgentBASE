# ALLBot部署 任务清单（AstrBot QQ 机器人「丛雨」调教）

> 📅 最近更新：2026-09-15 01:10（规划 Agent，Codex 5h 限额中断后交接轮）
> 📌 任务状态流转：`待办 → 修复中 → 待复验 → 已关闭`（已关闭仅测试 Agent 可勾选）

---

## 📸 开发进度快照（Codex 交接，2026-09-15 01:00）

**证据来源**：Codex 会话截图 + 运行时实测（cmd_config.json 差异、进程状态、日志目录）。

| 项 | 状态 | 证据 / 说明 |
|---|---|---|
| 核心配置修复 | ✅ 已落地 | `cmd_config.json`：`log_file_enable=true`（原 false）；`admins_id` 增加用户 QQ 号（号码不落库） |
| 群分析手动触发 | ✅ 已实测可用 | Codex 结论：**手动 `/群分析` 不受 200 条下限限制**（与 BRD 原假设不同，待接手 Agent 复核后落库） |
| 转发卡片实测 | 🔄 中断未定 | 已发测试指令「丛雨，测试长消息：请写一篇不少于180…」，等模型 >24min 后被限额打断，**结果未知** |
| 日志落盘 | ⚠️ 待重启验证 | 配置已改但进程未重启（进程自 09-14 17:54 常驻），`logs/astrbot.log` 尚未生成 |
| 群漫画 | ⏳ 未开始 | Codex 剩余待办：漫画 API 接入 |
| 运行状态 | ✅ 正常 | astrbot-launcher + 2 python 进程常驻；SnowLuma 连接未见异常日志 |

**注意**：Codex 所述"实测差异已写入文档"未在本仓库工作区留下改动（工作区干净）；GitHub 网络当前不可达（Failed to connect github.com:443），远端是否有 Codex 提交**未知**，接手 Agent 恢复网络后先 `git pull`。

---

## 开发阶段（接手 Agent 按序执行）

- [ ] 任务 1（待复验）：基线验证收尾——重启 AstrBot（或确认热加载）后检查 `logs/astrbot.log` 落盘；确认与 SnowLuma 连接正常
- [ ] 任务 2（修复中）：插件命令链路——复核空白名单语义（当前 `enable_id_white_list=true`、`id_whitelist=[]`）；实测 `/群分析` `/群漫画` `/分析设置` 等指令响应
- [ ] 任务 3（修复中）：群分析——复核 Codex 结论"手动触发不受 200 条限制"，产出完整报告（话题/称号/金句/锐评），确认以丛雨口吻输出；将复核结论更新到 BRD
- [ ] 任务 4：转发卡片实测——向测试群重发长消息测试指令（≥180 字），确认超 `forward_threshold`（1500）时自动转合并转发卡片、短消息直发；确认 `forward_threshold` 调优即时生效
- [ ] 任务 5：群漫画——接入漫画 API（Codex 遗留项），实测 `/群漫画` 产出
- [ ] 任务 6：边界与异常——消息量不足提示、无权限提示、转发失败日志可查；汇总运行目录改动摘要落库本仓库

## 可选迭代（用户决策后执行）

- [ ] 任务 7：衍生插件——"丛雨人设化转发"：长内容按丛雨口吻分段为转发节点
- [ ] 任务 8：`segmented_reply` 调优（当前关闭）
- [ ] 任务 9：定时自动群总结（当前按用户决策关闭）

## 维护记录（Bug 修复看板）

> 开发/测试中发现的 Bug 一律在此新建任务；仓库外改动摘要必须落库。

- [ ] 任务 A：`log_file_enable` 改动需重启才生效，重启动作需在交接中说明（已由任务 1 覆盖）
