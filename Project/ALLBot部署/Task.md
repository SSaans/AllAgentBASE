# ALLBot部署 任务清单（AstrBot QQ 机器人「丛雨」调教）

> 📅 维护：规划 Agent　｜　最后更新：2026-09-16
> 📌 状态流转：`待办 → 修复中 → 待复验 → 已关闭`。**`[x]` 只能由测试 Agent 勾**，其他人不许勾。
> 📌 规格细节看 `BRD.md`（功能 4.x），运行环境看 `README.md`。**本轮已把历史层层叠加的勘误压缩，只留当前有效结论**。

---

## 🚦 现在的状态（先看这一段）

**已经能用的**：`/群分析`、`/群漫画`、长回复转卡片、卡片标题「丛雨」、存图插件、日志落盘。

**唤醒前缀现况**：QQ 档 = `["丛雨","丛雨酱"]`（用户 2026-09-16 02:18 设置）。
- ✅ **@ 机器人**唤醒 → 使用不带 `/` 的命令正文，如 `@丛雨 sid` / `@丛雨 群分析`；还须通过白名单
- ✅ **直接叫「丛雨」「丛雨酱」**唤醒 → 可用（名字在前缀里了）
- 🔴 **绝对不要往前缀里加 `/`** —— 加了之后群里**任何** `/` 开头的消息都会算唤醒，跟别的 bot 指令撞车（用户已实测踩过并移除）

**本轮（第七轮）已部署**：任务 26 禁言 v1.1.0（含手动提前开口）、27 关键词回复 v1.0.0、28 入群欢迎 v1.0.0、29 使用说明 v1.0.0。
- ✅ 2026-09-16 14:24:25–14:24:26 经官方 Dashboard 安装接口逐个加载；`/api/plugin/get` 四项均 `activated=true`，`astrbot.log` 有对应 Loading / Plugin / Installed 记录。
- ✅ `check_new_plugins.py --deploy-dir <运行插件目录>` 61/61 通过，含部署源码逐文件哈希一致；Ruff 检查通过。
- ⏳ 真实群验收：已请用户在既有授权测试群依次测试关键词、使用说明、禁言静默、手动恢复；入群欢迎需要用户拉小号。收到结果前不宣称群内通过。
- 任务 24 多图存图仍需单独热重载与复验；任务 25 清晰度调参仍为原有待办，未计入本次四项交付。

**还没验收的**：群内逐条验收（@ 触发 / 带内容放行 / 卡片标题 / 两类卡片等）—— 交测试 Agent，规划 Agent 不代勾。

---

## 开发阶段

- [ ] 任务 1：基线验证——双进程正常、SnowLuma 连接正常、群内普通聊天正常；开 `log_file_enable` 并确认日志落盘（**待复验**）
  - ✅ 基线部分：Launcher PID 2456 → venv 4304 → Python 9076 双进程常驻，连接与聊天都正常
  - ✅ 日志落盘已由任务 12 核实（**实测 115,590 B**，跨度 `11:16 → 23:56`），本任务不重复挂状态
  - 📌 注意：`log_file_enable` 字段读数在 `abconf` 是 `false`、`cmd_config` 是 `true`，但**日志确实在写** → **判据看文件有没有长大，别只看字段**
- [ ] 任务 2：插件命令链路实测——群里依次触发 `/群分析`、`/群漫画`、`/分析设置`，记录每个指令的响应；确认空 id 白名单语义（**待复验**）
  - 📌 现在的用法是 **@丛雨 + 指令**（不是靠前缀里的 `/`）
- [ ] 任务 3：群分析 LLM 连通——实测 `/群分析` 是否真调 LLM 出报告（**待复验**）
  - ✅ 已实测：`llm_provider_id=""` 会回退当前会话模型（claude-opus-5），LLM 调用成功，不用配 provider
- [ ] 任务 4：群总结内容验收——出完整报告（话题/称号/金句/锐评，图文正常），且是丛雨口吻（**待复验**）
  - 📌 老文档写「`keep_original_persona=false` 时自带人设视角」**是错的**（已实测证伪）：false 时 `_build_system_prompt` 返回 None、人设压根不加载。**正确是 `true` 才继承丛雨人设**（任务 10 的根因就是这个）
  - 📌 「消息量达标」是**定时场景**的前提；手动 `/群分析` 不受 200 条限制，别因此判失败
- [ ] 任务 5：长短分流——短回复（≤阈值）1–2 条普通消息；长回复（>阈值）转合并转发卡片（**待复验**）
  - ✅ 已实测：配置更新后，群里三次普通 LLM 长回复都自动转成了 `forward`
  - 📌 **阈值实测 = 50**（不是早期记录的 30，也不是 1500）→ 当前分界：**50 字直发、51 字起转卡片**
  - 📌 改阈值必须**经配置档 API 保存**才生效，直接改磁盘 JSON 没用；前提 `streaming_response=false`
- [ ] 任务 6：边界与异常——消息量不足的提示、无权限的提示、转发失败日志可查；运行目录改动摘要落库（**待复验**）
  - ✅ 运行目录改动摘要已落 README
  - 「转发失败日志可查」依赖日志落盘（任务 12 已完成）

## 新增需求

> 编号 15–29 对应用户陆续提的需求，规格细节在 `BRD.md` 功能 4.x。**未完成的都保持「待办」，不许自行启用。**

- [ ] 任务 15：`/群漫画` 指定时间段——**结论：不做，零代码**
  - 用户口径：「最近几天够用」。`/群漫画 N` 就是最近 N 天。用户用法：`@丛雨` + 「群漫画」+ 天数
  - 📌 附带约束：拉消息有 `basic.max_messages = 1000` 的上限，回溯太多会被截断
- [ ] 任务 16：转发阈值改哪里——**纯答疑，无开发量**
  - 字段：QQ 档「丛雨丸」的 `platform_settings.forward_threshold`（**实测 50**）
  - 改法：WebUI → 平台设置 → 选「丛雨丸」档 → 改数字 → 保存即热生效
  - 生效判断在 `result_decorate/stage.py:414`（`if word_cnt > self.forward_threshold`，**等于阈值仍直发**）；全局默认 1500 在 `config/default.py:66`，**改它没用**
  - 硬前提：`streaming_response = false`
  - 文件位置（含敏感信息，**只引用不落库**）：`core\data\config\abconf_626c9487-1b19-4180-8878-48a1b85b26fe.json`
- [ ] 任务 17：存图功能（群友图片库）——**已实现 v1.2.0**（**待复验**）
  - 规格见 `BRD.md` 4.3（含 5 行行为对照表：1 张 / x3 / 超量 / 没这个词 / 不带 .jpg）
  - ✅ 实测证据：图库有 `测试鱼` 4 张、`猫猫虫` 1、`vol` 1、`表情` 1；`23:47:13~18` 用户连发 3 次 `猫猫虫.jpg` 都逐次发出去了
  - 📌 实现要点：**正则匹配消息文本、不要注册成 `/` 命令**（否则又依赖唤醒前缀）
  - **去向：测试 Agent 复验群内链路**
- [ ] 任务 18：「吾辈在！」——**已实现 v1.1.0，方案 A**（**待复验**）
  - ✅ 插件已加载（日志 `23:11:40 纯唤醒回复插件已加载。`），`reply_text` 可改
  - 触发词 = `wake_prefix`（WebUI「配置 → 唤醒前缀」改）；前缀现在含名字，所以**叫名字这条路可以测了**
  - ✅ 开发复核（2026-09-16，对照官方文档 `plugin-new` / `simple` / `listen-message-event`）：`class PresenceReplyPlugin(Star)`、`@filter.event_message_type(GROUP_MESSAGE)` + `priority`、命中后 `stop_event()` 阻断后续 handler 与 LLM、`await event.send(event.plain_result(...))`、`metadata.yaml` 写法**全部合规**；`initialize/terminate` 官方标注「可选择实现」，不缺不算错。**代码无需改**
  - 本轮唯一改动：`_conf_schema.json` 的 `reply_text` hint 删除「并保留 /」（与「前缀绝不加 /」红线冲突，纯文案）
  - 📌 判据要严：群里出现**和 `reply_text` 一模一样**的「吾辈在！」才算生效。`23:17:16` 那句「好的主人！吾辈在呢！」是**引用消息场景下 LLM 自己发挥的**，不算证据
  - **去向：测试 Agent**（纯 @ / 叫名字 / 带内容放行，三条都要测）
- [ ] 任务 19：关掉 Markdown——**用户已自行解决**（**待复核**）
  - 用户已在丛雨人设 prompt 里加「用纯文本回复、不要 Markdown」（`data_v4.db`，2026-09-15 07:28）
  - 📌 是**软约束**，偶尔可能漏星号；想 100% 干净只能开 `t2i` 转图片（不立项）
  - **去向：测试 Agent 群里看「回复里还有没有 `**` 和 `- `」**
- [ ] 任务 20：`/` 命令全部失效的老问题——**已闭环（口径已改）**
  - 根因：命令过滤器首行要求 `is_at_or_wake_command` 为真（`star/filter/command.py:191-192`），不满足就静默丢弃
  - ✅ **最终口径**：**用 @ 机器人唤醒**，不往前缀放 `/`。@ / @全体 / 引用机器人消息 / 私聊都能满足（`waking_check/stage.py:123-148`）
  - 📌 历史留档：中间曾把 `/` 加回前缀，用户实测会误唤醒群里所有 `/` 消息，已移除
- [ ] 任务 21：转发卡片标题改成「丛雨」——**已实现**（**待复验**）
  - 改了两处：核心 `result_decorate/stage.py:417` 的 `name="AstrBot"` → `"丛雨"`；插件 `src/infrastructure/platform/base.py:190` 的 `self_name` → `"丛雨"`
  - ⚠️ **核心补丁**：AstrBot 升级会覆盖，**要重打**（已登记 README）
  - ⚠️ 不要走插件钩子改（钩子比节点构造早，拿不到）
  - **去向：测试 Agent 用眼睛看 QQ**（长回复卡片 + 群分析报告卡片标题）
- [ ] 任务 22：存图插件的口令与回复可在 WebUI 编辑——**已实现 v1.2.0**（**待复验**）
  - 可改：`/c` 口令、`.jpg` 后缀、`x` 连接符、成功/重复/失败/没这个词/主人没这个词/超量共 6 类回复
  - 位置：插件配置页 + 图库页的「回复与口令」页；保存立即生效。默认成功回复「存好了」
  - **去向：测试 Agent 做页面操作复验**
- [ ] 任务 23：随机取图 + WebUI 图库管理——**已实现 v1.2.0**（**待复验**）
  - 随机不重复；库存 > 1 张时连续单张取会避开上一张
  - 图库页：缩略图 / 看原图 / 上传 / 改关键词 / 移动 / **二次确认删除**（删除只移到 `.trash/`，能找回）
  - **去向：测试 Agent 复验页面交互**（浏览器画面检查上次因控制工具启动失败没做成）
- [ ] 任务 24：存图支持**一次存多张**（**已实现 v1.3.0，待复验**）
  - 一条消息带多张图 + `加图 测试鱼.jpg` → 全部入库
  - 回复：1 张 →「存好了」；x 张 →「x 张都存好了」
  - `加图` 是新触发词，`/c` 必须**继续能用**；要遍历所有 Image 组件；要处理部分成功
  - 规格：`BRD.md` 4.10
  - ✅ 开发完成（2026-09-16 开发 Agent）：`astrbot_plugin_meme_library` v1.2.0 → **v1.3.0**——新增 `加图` 别名（与 `/c` 并存，**不依赖唤醒前缀**，仍是消息文本匹配）、遍历全部 Image 组件、按**实际新增成功数**回复（重复/失败单独提示并带「第 N 张」序号）、新配置项 `stored_many_reply`（默认「{count} 张都存好了」）
  - ✅ 回归自测：`check_group_plugins.py` **29/29 通过**（新增「多图+加图」「混合新旧图只计新增」两用例）
  - ⚠️ **运行实例仍加载 v1.2.0**——代码已落运行目录，但需**用户在 WebUI 插件页对图库插件点「重载」**（热重载，不用重启）后才生效；群内验收须在重载之后
  - **去向：测试 Agent 群内复验**（多图存入、回复文案、`/c` 依旧可用）
- [ ] 任务 25：群漫画**文字清晰度**调优（**修复中——余额已恢复，可继续调参**）
  - 归因：格数 = 话题数（`max_topics=5`）× 出图只有 **1672×940** → 每格约 334px，中文必糊
  - 处方（按效果排序）：① `max_topics` 5→2/3（**最有效**，但群分析报告话题数会一起减少）② `image_size` `auto`→`2k`（**要实测上游是否真按这个尺寸返回**）③ `aspect_ratio` `16:9`→`4:3`/`1:1` ④ 底部字幕 30 字压到 12 字内 ⑤ 验收用 QQ「查看原图」，别把 QQ 压缩算进来
  - 规格：`BRD.md` 4.11
  - ✅ 基线复核（2026-09-16 开发 Agent）：6 张历史漫画 PNG 头逐张实测 **1672×940/941**（请求侧 `auto`→1792×1008，**上游缩水 ≈6.7%**，与规划轮结论一致）
  - ✅ 尺寸映射源码级确认（`drawing_client.py:_resolve_size`）：`auto`+16:9=1792×1008、`2k`+16:9=2560×1440、`auto`+4:3=1792×1344；每格像素推算：5 格≈334px/格 → 3 格≈557px/格 → 2 格≈836px/格（**减格数对清晰度的收益可量化成立**）
  - 🔴 **阻塞（需用户处理）**：单变量真实出图试验（2k / 4:3 / 短字幕）**全部被上游拒绝**——`rkapi.com` 返回 403 `Insufficient account balance`（**中转站账户余额不足**；最后一次成功出图是 2026-09-16 01:29）。试验脚本在旧机路径 `D:\Test\comic_trial.py`（**未随换机迁移，新机不存在**；需用时按同一思路重建），只出图不发包、不触群。余额已于 2026-09-16 12:39 实测恢复
  - ✅ **2026-09-16 12:39 实测余额已恢复**：`@丛雨` 触发群漫画全链路成功出图（`reports/comic_1095608283_20260916_123906_*.png`，2,398,998 B），403 未再出现 → 单变量调参试验可继续
  - ⚠️ `max_topics` 5→3 **等你拍板**（会让群分析报告话题数一起变少，见文末「待用户确认」第 2 条）——拍板后改配置即生效，无需出图验证分格逻辑（分格数=话题数已由源码与日志证实）
- [ ] 任务 26：**禁言插件（含手动提前开口）**——**v1.1.0 已部署（待复验）**
  - 效果：回「好的，接下来我会闭嘴x分钟」→ 期间**什么都不理** → 到点自动恢复；口令与回复可编辑
  - 核心**没有**原生禁言 → 必须写插件
  - 三条技术前提：① 用 `@filter.event_message_type(ALL)`、**不注册成命令** → 不受唤醒前缀限制 ② `event.stop_event()` 能拦住后面所有插件和 LLM ③ 优先级必须**比 `presence_reply` 高**（它用 `maxsize + 1`），否则禁言期间还会喊「吾辈在！」
  - 规格（时长上限 / 重置计时 / 惰性恢复 / 落盘 / 范围可配）：`BRD.md` 4.12
  - ✅ 开发完成（2026-09-16 开发 Agent）：`plugins/astrbot_plugin_mute/` v1.0.0（**已入库**）
    - 优先级取 `maxsize + 100`（**高于** `presence_reply` 的 `maxsize + 1`），禁言期间不会先蹦「吾辈在！」
    - 时长支持 分钟/秒/小时（`30分钟` / `5分` / `90秒` / `2小时` / `1h` / `闭嘴10`→10 分）；超过 `max_minutes` 按上限截断并记日志
    - 双保险：`asyncio` 定时器 + 每条消息惰性判断过期；状态落盘 `data/plugin_data/astrbot_plugin_mute/mute_state.json`，**实例重启后仍能正确恢复**（`initialize` 补排未过期项、`terminate` 取消任务）
    - 范围可配「仅当前群 / 全局」；口令、开工回复、到点回复、上限、是否仅管理员触发，全部 WebUI 可改（**默认任何人**都能触发，见文末待确认第 1 条）
  - ✅ **第七轮已补齐并加载（v1.1.0）：手动提前开口** —— 禁言期间发**解除口令**（默认 `丛雨说话` / `可以说话了` / `解除禁言`）→ 立刻恢复并回一句（默认「好啦，我回来了」）；口令与回复词都可配
    - ⚠️ 顺序：**先查解除口令、再查禁言口令**（否则「别闭嘴了」含「闭嘴」会被判成重新禁言）
    - ⚠️ 位置：解除口令的检查必须在**静默判断之前**（禁言期间消息会被 `stop_event()` 拦掉）
  - ✅ 2026-09-16 第七轮自测：解除先于禁言与静默判断；读取原始 Plain 防止 `丛雨` 被核心裁掉后解除口令失效；取消计时器并落盘，解除后不重复到期回复；禁言中重复口令只重设计时、无回复；时长按口令后文本解析，上限提示与实际计时一致。
  - ✅ 四插件 61/61 隔离测试通过（含仓库/运行目录一致）；实际加载时间 14:24:25–14:24:26，群内结果待用户回报。
  - **去向：测试 Agent 群内复验**（`@丛雨` 带「闭嘴 1分钟」→ 期间发别的话应完全无响应、且**不能**出现「吾辈在！」→ 1 分钟后自动恢复）
- [ ] 任务 27：**关键词自动回复插件**——**v1.0.0 已部署（待复验）**
  - 触发一句「我是笨蛋吗」→ 随机回 `是`/`不是`/`不知道`/`钝角`，或者固定回某一条；可给某个人指定**只能回固定答案**；全部可编辑
  - **不用唤醒**：官方 `star/filter/regex.py:9` 写明「正则表达式过滤器不会受到 wake_prefix 的制约」，用 `@filter.regex` 即可
  - ⚠️ 命中后**必须 `event.stop_event()`**（勘误见 BRD 4.13）：不 @ 时 LLM 本来不会接话，但消息一旦 @ 了机器人 / 命中唤醒词，LLM 就会再答一遍
  - 规格：`BRD.md` 4.13
  - ✅ 开发完成（2026-09-16 开发 Agent）：`plugins/astrbot_plugin_keyword_reply/` v1.0.0（**已入库**）
    - 规则一行一条：`触发词 => 回复1|回复2`（多个=随机挑一个）、`触发词 => =整句固定`、`/正则/ => 回复`、`qq:QQ号: 触发 => 回复` / `group:群号: 触发 => 回复`（**指定对象优先，默认规则兜底**）、`#` 开头当注释；坏行跳过并在日志里提示
    - 回复里可用 `{nickname}` / `{sender_id}` / `{group_id}`；可设群白名单；可整插件开关
    - 触发词改动**即时生效**：加载时直接替换注册表里本 handler 的 `RegexFilter` 实例，WebUI 保存→热重载后立即按新词匹配
    - 未命中时**不** `stop_event()`，正常聊天照旧
  - **去向：测试 Agent 群内复验**（群里直接发「我是笨蛋吗」**不用 @**，应只回一条、且 LLM 不再补一句）
- [ ] 任务 28：**入群欢迎插件**——**v1.0.0 已部署（待复验）**
  - 有人进群 → 自动欢迎；欢迎词可编辑
  - 入群通知是 OneBot `notice` 事件，适配器把它转成消息事件并保留原始数据在 `raw_message`（`aiocqhttp_platform_adapter.py:168-196`）
  - 🔴 **必须用 `@filter.custom_filter(自定义过滤器)`，只在 `notice_type == "group_increase"` 时通过**。**绝不能**用无条件的 `event_message_type(GROUP_MESSAGE)`——那样每条群消息都会**激活本插件的处理器**（核心不会因此调 LLM，见 BRD 4.13 勘误；但逻辑一写错就是全群刷屏）
  - 规格：`BRD.md` 4.14
  - ✅ 开发完成（2026-09-16 开发 Agent）：`plugins/astrbot_plugin_group_welcome/` v1.0.0（**已入库**）
    - 自定义过滤器 `GroupIncreaseFilter(CustomFilter)` 严格只放行 `notice_type == "group_increase"`；消息事件、退群、戳一戳、空 `raw_message` 全部拒绝（隔离测试逐项验过）
    - 默认 @ 新成员 + 欢迎词（`at_new_member` 可关）；欢迎词支持 `{nickname}` / `{user_id}` / `{group_id}`，并可按群单独配（一行 `群号 => 欢迎词`）
    - 取群名片失败时**回退成 QQ 号**，不报错、不漏欢迎；欢迎词留空则只静默 `stop_event()`、不发空消息
  - **去向：测试 Agent 群内复验**（拉个小号进群 → 应出现「@新成员 + 欢迎语」；同时确认**群里其它人正常聊天没有被刷屏**）
- [ ] 任务 29：**使用说明插件**——**v1.0.0 已部署（待复验）**
  - `@丛雨 使用说明` → 弹出一段「这个 bot 能干什么」的介绍，大白话；内容可编辑，也可以让 AI 生成一版草稿给用户看
  - 输出形式：**一段话**（用户明确不要海报/图片）
  - 建议用 `@filter.regex`（@ 或不 @ 都能触发），记得 `stop_event()`
  - 规格：`BRD.md` 4.15
  - ✅ 开发完成（2026-09-16 开发 Agent）：`plugins/astrbot_plugin_usage_guide/` v1.0.0（**已入库**）
    - 触发词列表可改（`|` 或换行分隔，默认 `使用说明` / `帮助` / `能干什么` / `会什么` / `怎么用`）；正文 `guide_text` 可改，**默认给的就是一段大白话**
    - 另有**草稿触发词**：命中后调当前会话的模型现写一版**只发不覆盖**，正文配置原样保留；模型不可用或报错时**自动回退**已配置正文
    - 正文留空时提示去插件配置里填，不会发空消息
  - **去向：测试 Agent 群内复验**（`@丛雨 使用说明` 应回一段话；`生成使用说明` 应回模型草稿）

- [ ] 任务 35：**所有自建插件统一补 README + 图形化面板**（待办，分批推进）
  - 范围：`keyword_reply` / `group_welcome` / `usage_guide` / `mute` / `chat_extractor` / `liflag` / `role_call` / `presence_reply` 补 README + 面板；`meme_library` 面板已有，按标准补齐（概览卡 / 深浅色 / 空态）
  - **交付从「三件套」变「五件」**：代码 + `metadata.yaml` + `_conf_schema.json` + **每个插件目录必须有 `README.md`** + **必须有 `pages/<页面名>/` 面板**（缺任一算未完工）
  - **规格全文在 `BRD.md`「插件交付标准（README 与图形化面板）」一节**，验收清单也在那（开发自查 15 项、测试验收 6 项）
  - 🎨 **视觉规范 = Shinsekai 同级**（第 4 节）：一个种子色 `--theme-accent` + `light-dark()` + `color-mix()` 自动出深浅两套，另用 `[data-theme=dark]` 兜底；圆角 7/10/12、间距 4/8/12/16/20/24、字号 11–18、侧栏 232px、内容最大宽 1120px 全部照抄 token 表；组件（按钮/输入框/分段导航/表格/Toast/确认弹窗）逐条有硬指标。**出处与本地副本**：上游 `RachelForster/Shinsekai` 的 `design.md` + `shared/theme/*.css`，副本在旧机 `D:\Test\_wb_plan\shin_ref\`（**未随换机迁移**，新机需从上游 `RachelForster/Shinsekai` 重新获取）
  - 技术要点（源码实测）：页面入口固定 `pages/<页面名>/index.html`，**目录名即面板标题**（用英文小写短名）；核心会把 `<html>` 改写成 `data-theme="light|dark"` → 必须同时给深浅两套；前端只用 `window.AstrBotPluginPage` 桥接（`apiGet` / `apiPost` / `upload` / `download` / `subscribeSSE`），后端 `context.register_web_api("/{插件名}/{短名}", ...)`；页面在 iframe 内、CSP 禁 `object`/`base` → **不许依赖 CDN**
  - `presence_reply` 源码目前只在运行目录，**先入库**再补文档
  - 去向：开发 Agent（**一个插件一次提交**；每批都要真实打开面板 + 群里跑一遍 README 里的指令）

- [ ] 任务 36：**聊天记录提取插件必须可用**（`astrbot_plugin_chat_extractor` v1.1.0；**修复中**）
  - 🎯 **「可用」四条判定（用户当前唯一重点，四条全过才算）**：
    1. 群里：引用一条合并转发 → 发「提取 html」→ 机器人回一个 html 文件（**现状已达成**）；
    2. 打开 html：**文件行显示真实文件名 + 大小**、**聊天记录卡片能点开收起**、图片正常；
    3. 过程**不刷日志**、不影响机器人别的功能；
    4. 拿不到文件链接 / 空记录 / 超大记录时**退化显示、不报错、不中断**。
  - 🔴 **先读这两条，别再走弯路**：
    1. **改动从未生效**——运行目录 `main.py` 是 `16:34:47` 改的，但日志里最后一次加载记录停在 `16:29:13`；**改完不重载 = 白改**。
    2. **「崩溃」是误判**——`16:59:26` 那次是 **LLM 模型通道 503（`model_not_found`: `gpt-5.6-terra`）**，进程没退出，`17:20`/`17:30` 仍在正常收消息；**与插件无关，不要往这个方向排查**。
  - 🔴 **不许再等用户提供日志**：字段口径已给全（`BRD.md` 4.17），直接改。
  - **A. 文件 / 视频字段**（`main.py` 的 `seg_type == 'file'` 分支，约 430–444 行）
    - 文件名按 `file` → `file_name` → `filename` → `name` 依次兜底（**`file` 是最常见的，现在漏了**）；大小取 `file_size`
    - 链接：先看段里的 `url`；没有就用 `file_id` 调 `get_group_file_url(file_id, group_id, busid=102)`（私聊 `get_private_file_url`），或 `get_file(file_id)` 一次拿 `file_name`/`file_size`/`url`
    - **换不到链接只隐藏链接，不丢消息、不报错中断**；视频段同理（`file` / `url` / `file_size` / `thumb`）
  - **B. 卡片渲染**（**只改提取层，别动 CSS/JS**，样式与 `toggleForward` 已经写好了）
    - 顶层被引用的那份合并转发**也要包成一条 `msg_type="forward"` 的消息**（带 `nested_messages`），不要摊平成普通气泡
    - 嵌套段两种写法都要认：`type=forward`（取 `data.id`）与 `type=node`（内容就在 `data` 里）
  - **C. 日志降噪**：`main.py:992`「聊天记录提取插件收到消息」与 `:1002`「识别到的格式」**删掉或降为 debug**；只在**命中指令后**打一条 info、出错打 error
  - **D. 改完必须重载并留证据**：部署到运行目录 → 重载 → 日志里出现**新的**「聊天记录提取插件已加载」行；**没有新加载行就等于没做**
  - **E. 自测（构造数据，不发群、不打扰用户）**：跑一次含「文件 + 图片 + 嵌套记录」的导出，检查：① html 里 `未知文件` 出现 **0 次**；② `onclick="toggleForward(` 出现次数 **≥1 且等于卡片数**；③ 浏览器里点标题能展开收起
  - **F. 交付证据**：导出文件路径 + 上面三条的检查输出（grep 结果）+ 新加载日志行号，按四段话术交回
  - 📌 **不包含**：README 与面板（归任务 35）；禁言/图库的在途改动（任务 37）
  - 去向：开发 Agent（**一次做完一次交付**）→ 测试 Agent 按四条判定复验

- [ ] 任务 37：**把在途未入库的插件源码补齐入库**
  - `plugins/astrbot_plugin_mute/{main.py,metadata.yaml}`：已改但未提交（`git status` 里是 `M`）→ 提交并推送，并在 CHANGELOG 写清「改了什么、验没验」
  - `plugins/astrbot_plugin_meme_library/`：整个目录**未跟踪**（`??`）→ 入库（图库数据 `core/data/meme_library/` **不入库**）
  - ⛔ 逐路径 `git add`，禁止 `git add .` / `git add Project`；提交前 `git diff --cached --name-only` 确认没有 `data/` 混入

## 可选迭代（需用户先决策，不许自行启用）

- [ ] 任务 7：衍生插件——长内容按丛雨口吻分段组织成转发节点
- [ ] 任务 8：`segmented_reply` 调优——长回复按句拆成多条间隔发送（**实测当前 = `false`**）
- [ ] 任务 9：定时自动群总结——启用 `auto_analysis_time`（**用户明确保持关闭**）
  - 📌 实测：`auto_analysis_time=["23:00"]` 但 `scheduled_group_list=[]` → **实际不会注册定时任务**

## 维护记录（Bug 看板）

- [ ] 任务 31：`sid` 无响应诊断及现用命令写法勘误（原因已确认，待配置稳定后群内复验）
  - 内置命令注册 `filter.command("sid")`，无 `/sid` 别名；`CommandFilter.filter` 先要求已唤醒，再精确匹配命令名。名字前缀不含 `/` 时，`@丛雨 /sid` 的斜杠保留，命令不匹配而转普通聊天；应使用 `@丛雨 sid`。
  - 真实证据：15:08:21 的 `@ /sid` 已通过白名单，15:08:45 LLM 回复「看不懂 /sid」；15:10:21 用户改发 `@ sid`，但此次被 `whitelist_check.stage:65` 拦截。
  - 配置时间线：15:05:46 磁盘管理员群豁免为 true，15:05:20 的 @ 消息已有正常回复；15:08:58 配置保存后群豁免为 false。随后管理 API 的运行配置与磁盘均为 false，管理员与白名单虽均只有用户 QQ，仍不豁免群白名单。不能把两个时段混为一次同配置失败。
  - 内置命令插件 activated=true，`disable_builtin_commands=false`、`plugin_set=["*"]`，排除内置功能被关。某群禁言已在 15:08:18 自动到期；本次 sid 的直接拦截源是核心白名单。
  - 本轮仅诊断与文档勘误，未改配置或核心。用户若只需自己这个管理员跨群使用，可开启管理员群豁免；未来普通指定 QQ 的名单需求仍见任务 30。


- [ ] 任务 30：按**发言人 QQ 号**限制触发（待办；用户已澄清需求）
  - 用户 2026-09-16 明确：白名单只填个人 QQ，只有名单内的人才能触发；不是按群号放行，也不是让管理员跨群豁免。
  - 已查清：当前 WebUI `platform_settings.id_whitelist` 是**会话白名单**。实际 `WhitelistCheckStage.process` 只检查 `unified_msg_origin` 或 `group_id`，不检查 `sender_id`；管理员例外关闭时，填管理员自己的 QQ 或昵称也不会让群消息通过。
  - 运行证据：2026-09-16 14:52:20「丛雨说话」、14:52:53 / 14:53:06「我是笨蛋吗」、14:53:59「解除禁言」对应消息均被 `whitelist_check.stage:65` 拦截，尚未进入新插件处理。插件仍全部已加载。
  - 6 项真实白名单源码隔离验证通过：名单群通过、未列群的管理员被拦、添加群号可放行、管理员例外可豁免、裸个人 QQ 不放行私聊、完整私聊会话 ID 可放行。后四项仅验证核心语义，**不是用户要求的解决方案**。
  - 去向：规划 Agent 按用户要求补齐发言人白名单方案，再交开发实现；应在业务插件和 AI 回复之前统一限制。当前仅诊断及登记，未修改运行配置、未扩大任何群或成员权限。


- [ ] 任务 10：群分析人设未加载——两个开关都 false 时 `get_system_prompt` 返回 None（**待复验**）
  - ✅ 已修：`keep_original_persona` 从 false 改成 **true**，继承丛雨人设
- [ ] 任务 11：QQ 档唤醒前缀与管理员问题（**已闭环**）
  - ✅ 管理员按 QQ 号添加完成（`admins_id = ["Edi","<QQ号>"]`）
  - ✅ 唤醒前缀最终定为 `["丛雨","丛雨酱"]`（**不含 `/`**）；命令用 @ 唤醒
- [ ] 任务 12：文件日志落盘（**已完成，待测试 Agent 确认后关闭**）
  - ✅ 经授权的正常重启后**日志确实落盘**：`core\data\logs\astrbot.log` = 115,590 B、跨度 `11:16:02 → 23:56:38`，含 `23:08` 重启后的启动与插件加载记录
- [ ] 任务 13：群漫画绘图供应商（**已解决，待复验**）
  - ✅ 配了 `openai_images` / `gpt-image-2`；`reports/` 里实有 **6 张真图**（约 2.0–2.3 MB），用户回「群漫画好像可以了」
  - ⚠️ 最终验收仍以**群里真实发送结果**为准；**API Key 不许进仓库**
- [ ] 任务 14：规划事实勘误（**已完成**）
  - ✅ 四处已勘误（QQ 用独立配置档 / 手动群分析绕过 200 条 / 人设要开继承 / 磁盘编辑不等于热更新）；BRD 与 README 已同步
  - **去向：测试 Agent 复核文档与实测是否一致**

---

## 实测快照（各轮合并，只留当前有效值）

> 全部为**只读**采集，没改过运行目录任何文件，没启停实例。

| 项 | 实测值 | 判读 |
|---|---|---|
| `abconf` 顶层 `wake_prefix` | `["丛雨","丛雨酱"]`（mtime 2026-09-16 02:18） | ✅ 名字唤醒可用；**不含 `/`（有意为之）** |
| `cmd_config.json` 顶层 `wake_prefix` | `[]` | QQ 走 `abconf`，不影响实际行为 |
| QQ 档 `platform_settings.forward_threshold` | **50** | 50 字直发、51 字起转卡片 |
| `cmd_config` 的 1500 | 兜底值，被档内值覆盖 | 改它无用 |
| `provider_settings.streaming_response` | 非 `true` | ✅ 阈值能生效 |
| `log_file_enable` | `abconf` false / `cmd_config` true，**但日志在写** | 判据看文件增长 |
| `core\data\logs\astrbot.log` | 115,590 B，`11:16:02 → 23:56:38` | ✅ 落盘有效 |
| `admins_id` | `["Edi","<QQ号>"]` | ✅ |
| 丛雨人设 `system_prompt` | 已含「不要用 Markdown」（2026-09-15 07:28） | ✅ 任务 19 用户自己解决的 |
| `daily_comic.drawing_provider_overrides` | 已配 `openai_images` / `gpt-image-2`（含密钥，**不落库**） | ✅ |
| `analysis_features.max_topics` | **5** | 🔴 漫画糊的主因之一 |
| `analysis_features.keep_original_persona` | false | ⚠️ 应为 true 才继承丛雨人设 |
| `basic.max_messages` | 1000 | 影响 `/群漫画 N` 的回溯长度 |
| `basic.min_messages_threshold` | 200 | 只管定时分析 |
| 漫画出图实际尺寸 | **1672×940**（6 张全一致） | 🔴 「糊」的硬证据；请求 1792×1008 被上游缩水 |
| 群报告图 | 宽高约 2.8 万–6.2 万像素 | 报告图**和漫画不同源**，别一起归因 |
| `segmented_reply.enable` | `false` | ✅ |
| 已加载插件 | `astrbot_plugin_meme_library` v1.2.0（`23:08:46`）、`astrbot_plugin_presence_reply` v1.1.0（`23:11:40`） | ✅ |
| 新写的 4 个插件 | 2026-09-16 14:24:25–14:24:26 逐个正式安装；禁言 v1.1.0，其余 v1.0.0；源码哈希一致 | ✅ 管理接口四项 activated=true，日志已有加载记录；群内验收待回报 |
| 新插件隔离自测 | `tests/check_new_plugins.py` **61/61 通过**（真实 core；含部署副本哈希） | ✅ 隔离自测，不冒充群内验收 |
| 运行实例日志文件 | `core\data\logs\astrbot.log`（本轮只读核对，用于判断插件有没被加载） | ✅ 读数有效 |
| 卡片标题补丁 | `stage.py` name=丛雨 + 插件 `base.py` self_name=丛雨，随 `23:08` 重启加载 | ✅ 已加载（**实际显示要人眼看 QQ**） |
| 图库实况 | `测试鱼` 4 / `猫猫虫` 1 / `vol` 1 / `表情` 1，`.trash/` 已建 | ✅ 取图链路可用 |
| 漫画与报告文件 | `reports/` 6 张漫画 PNG + 3 张报告 JPG | ✅ |
| `赛马游戏Beta.exe` | 5.6 MB，PyInstaller 打包，**旧机** `D:\SystemFiles\Downloads\`（换机后本机位置未确认） | 用户「未来想做赛马功能」的参考物；**只登记，不立项、不逆向、不处置** |

---

## 待用户确认

1. 禁言默认范围目前定的是**只禁当前群**——要不要改成**全局**（所有群+私聊一起静默）？
2. 任务 25 里「减少漫画格数」会让**群分析报告的话题数一起变少**（同一个开关），是否接受？

- [ ] 任务 32：存图插件关键词冲突自动合并、整关键词确认删除、单次出图上限（待复验）
  - 用户本轮新增：图库中改名为已有关键词时自动合并；支持删除整个关键词，沿用确认删除与可恢复回收目录。
  - 已澄清：不限量 QQ 名单内的人不受限；其余人单次最多 X 张，默认 20。名单留空则全部人受限。请求超量先发到上限，再提示「再发就刷屏啦...」。配置与提示在 WebUI 可编辑。
  - 运行 v1.4.0 已热重载；37 项隔离回归与真实图库管理 API 自测通过，真实群内数量上限交测试 Agent 复验。图库图片不入库。


- [ ] 任务 33：立flag 插件（开发自测通过，已安装，待真实群复验）
  - 用户 2026-09-16 直接新增需求：@机器人「我要学习吃饭20分钟」开始计时；计划20分钟后再超时10分钟提醒；本人「学完了」结束，返回实际分钟数；支持今日、本周、总学习时长 + 用户名查询。提醒、开始/完成回复、总结模板、触发词、累计门槛祝贺规则可在插件 WebUI 配置。
  - 源码 `plugins/astrbot_plugin_liflag/{main.py,_conf_schema.json,metadata.yaml}`；展示名「立flag」，v1.0.0 已通过正常安装接口加载。数据保存在运行目录 `core/data/plugin_data/astrbot_plugin_liflag/study.sqlite3`，不入仓库。
  - 实现口径：当前群分 QQ 计时；同一人同时只允许一个学习项目；到计划时长仍继续计时；超时10分钟提醒一次，直到本人发结束词才停止；重载恢复，发送失败重试；正在学习的时间计入统计。北京时间、周一至周日、跨日拆分，分钟保留一位小数。今日15分钟边界按「继续加油」；累计祝贺按达到的最高门槛，默认0/120，可编辑。
  - 通过群成员昵称/群名片/QQ查询，重名拒绝并提示用 QQ。无记录的已识别成员返回0；查询不填姓名默认本人。开始/今日/本周要求真正 @机器人；结束词、总计可不 @。
  - 14 项联合隔离自测通过；包含提醒边界、失败重试、重载恢复、提前完成、跨日/每周统计、祝贺门槛、成员与群隔离、动态触发词。23:53:09 真实安装日志正常，管理 API activated=true；未冒充真实群通过。
  - 已请用户在群1095608283发开始、结束、今日和本周命令，尚待反馈。计时结束方式的澄清尚未答复，先按上述口径实现；后续 Agent 先读聊天最新答复再调整。

- [ ] 任务 34：指定人员召唤插件（开发自测通过，已安装，待真实群复验）
  - 用户直接新增：`设置程序员 @甲 @乙` 建立当前群名单，`召唤程序员 有人做程序吗` 发真实 At 组件、分隔线和正文；角色名可自由填写，支持多名单。设置/召唤前缀可各配置多个，回复与分隔线可编辑。
  - 源码 `plugins/astrbot_plugin_role_call/{main.py,_conf_schema.json,metadata.yaml}`；展示名「指定人员召唤」，v1.0.0 已加载；数据 `core/data/plugin_data/astrbot_plugin_role_call/roles.sqlite3`，不入仓库。
  - 当前默认只有 AstrBot admins_id 内管理员可设置，所有人可召唤；WebUI 可关闭管理员限制。该权限澄清尚待用户答复。重新设置同名名单会替换名单，QQ去重、排除机器人/全体成员；群间隔离。普通无 @ 的「设置…」聊天不触发。
  - 联合14项自测含真实 At 组件、重复去重、替换、跨群隔离、权限、可配置触发词与无关聊天静默。已请用户群内试设置/召唤，等反馈后核对日志；不勾选任务。
