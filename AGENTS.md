# Agent 行为规范入口（AGENTS.md）

> 📌 **本文件是全平台所有 Agent 的唯一行为规范入口与 skill 路由中心**（2026-09-20 扩充）。
> 任何 Agent 在执行任何任务前，**必须先读本文件**，再按任务类型查阅对应的 skill 卡。

---

## 📑 〇、开工三步（所有 Agent，无例外）

1. **读本文件**（就是现在这一步）——拿到角色边界、通用纪律、任务路由
2. **判定任务类型 → 查第一节路由表**——拿到该读的 skill 卡路径
3. **读那张 skill 卡 + 对应 SOP，然后才动手**；收工按 skill 卡末尾的「收工硬检查」逐项核对

> 跳过第 1 步直接干活 = 违规。判不准任务类型时，**先读 `skill/token节省/SKILL.md`**——它适用于所有任务。

---

## 🗺️ 一、Skill 任务路由表

| 你的任务 | 必读 skill 卡 | 权威 SOP | 常驻摘要 |
|---|---|---|---|
| **任何任务**（尤其大范围读写、整理日志、写交接） | `skill/token节省/SKILL.md` | — | 本文件 §2.1 |
| 规划 / 立项 / 补 BRD / 勘误过期文档 | `skill/规划Agent/SKILL.md` | `PLANNING.md` | 本文件 §3.1 |
| 写代码 / 修 Bug / 部署 / 自测 | `skill/开发Agent/SKILL.md` | `DEVELOPMENT.md` | 本文件 §3.2 |
| 验收 / 复验 / 出测试结论 | `skill/测试Agent/SKILL.md` | `TESTING.md` | 本文件 §3.3 |
| 写对外文字 / 润色 / 去 AI 味 / 文案 | `skill/humanizer/SKILL.md` | — | 本文件 §2.2 |
| 达芬奇 21 操作查询 | `skill/达芬奇21中文操作手册/SKILL.md` | — | 不并入本文件，按需加载 |

**读法**：先读本文件 §2 / §3 的摘要快速定位，确需完整规则时再打开上表路径。
**不要**为了「看全貌」把所有 skill 全文读一遍——见 §2.1 的 Token 纪律。

---

## 🧰 二、通用 skill（所有 Agent 共用）

> 通用 skill = 与角色无关、任何 Agent 都可能用得上的能力卡。**后期新增的泛用 skill 一律挂在本节**，登记规则见 §2.3。

### 2.1 `token节省` —— 强制常驻，每次开工都适用

**一句话**：先 Grep 定位再定向读、新内容追加不改写、稳定前缀一个字节别动、日志到 15 条就归档。

**适用**：任何对本仓库的读写。尤其通读文档、扫描代码、整理日志、写交接时；或用户提到「省 token」「多用缓存」「上下文太长」「降低成本」。

**十条纪律（读文档场景，逐条可执行）**
1. **先定位，再读**：先 `Grep` / `Glob` 拿路径与行号，再定向 `Read`。禁止无明确目标就递归遍历目录、逐个读文件。
2. **长文档只用窗口读**：`Read` 带 `offset` / `limit` 只取需要的段落，不为「看全貌」整读几千行。
3. **交接记录只读最近 3 条**：不通读全量 CHANGELOG；历史细节在归档里，需要时再定点取。
4. **同一轮不重复读同一文件**：内容已在上下文里就不再读一遍。
5. **只写差异，不誊抄**：写 CHANGELOG / 交接 / 结论时只写本轮新增与变化，用 Edit 最小替换，不重写整段。
6. **skill 与 SOP 之间用指针，不复制全文**：写「见 `PLANNING.md` 第 N 步」，不把整段规则搬进第二个文件。
7. **要点式记录，不写散文**：条目用短句 + 列表，一条记录控制在一屏内。
8. **保持归档机制运转**：各级 CHANGELOG 保持 15 条上限，超出立即归档。日志无上限膨胀 = 每轮都要读更长的文件。
9. **稳定前缀不动**：`AGENTS.md` / 三本 SOP / skill 正文的 L1 段落只在规则真变了才改。避免「重排文档」「统一格式」这类无功能收益的大改。
10. **本地工具输出先落文件再读**：本机 PowerShell 输出常不回传，一次写文件、一次读，不连续重试同一命令。

**开工 3 秒自检**：① 能不能先用 Grep 定位？② 能不能追加而不是改写？③ 这文件我上一轮是不是已经读过了？

**失效红线（做了等于让缓存全废）**
- ❌ 为「排版好看」重排章节顺序、改写标题措辞
- ❌ 把 L3 易变信息（日期、状态）插进 L1 正文中间
- ❌ 在多个文件里复制同一段长规则
- ❌ 每轮通读全量 CHANGELOG / 全仓文件
- ❌ 同一轮反复重试同一条本地命令
- ❌ 无上限地往 CHANGELOG 追加而不归档

**完整规则**（含「运行时缓存理论 → 本仓库等价替代」逐条映射表、文档三层结构 L1/L2/L3）：`skill/token节省/SKILL.md`

### 2.2 `humanizer` —— 写作类任务共用

**一句话**：把「一眼像 AI 写的」文字改回具体、自然、有人味；改最少、效果最大。

**触发**：要写或改**给人看的文字**时——对外文档、说明、文案、汇报、公众号稿；或用户说「别像 AI 写的」「去 AI 味」「润色一下」。
**不适用**：CHANGELOG / Task 这类内部要点记录，**不要**过度加工（要的是短、准、可检索）。

**四条核心原则**
1. **不是改错，是改味**：AI 味不是语法错误，是风格问题——过于书面化、对仗工整、面面俱到。目标是拉回**具体、自然、可读**。
2. **改最少，效果最大**：能改一个词就不改一句，能删一句就不重写一段。
3. **保留创作意图**：只改「怎么说」，不改「说什么」；不新增原文没有的内容。
4. **注入灵魂**：风格目标一句话——**「有见识的普通人在认真聊一件打动他的事」**。

**最高优先级专项：「不是 A 而是 B」三毒**
| 毒型 | 症状 | 修法 |
|---|---|---|
| 毒 1 假靶子 | 否定了**根本没人提出的观点**（如「真正重要的不是效率，而是判断」） | 删前半句，只留 B |
| 毒 2 同义替换 | A 和 B 其实是**同一个意思**（如「不是水，是解渴」） | 合并成一句，删掉脚手架 |
| 毒 3 无关硬凑 | 在不需要转折处强行「不是而是」，删掉后句子毫无损失 | 直接删 |
| 变体 NNY | 「不是 X。不是 Y。只是 Z。」刻意制造悬念 | 删掉，直接说 |

> 通用修法：**直接说 B**；或改递进「不仅是 A 更是 B」（递进比否一肯一自然得多）。注意毒不在句式本身，在用法——真有人会做的判断 + A≠B，就是好用法。

**交付前必查（精简版）**：禁用词 → 三毒 → 堆叠副词（极其/猛地/死死/仿佛/瞬间）→ 莫名其妙比喻 → 假数据（「NASA 科学家算了一笔账」）→ 起手式套话（「先说答案」「掰开了揉碎了」）→ 自问自答老师腔 → 标点（冒号、破折号、双引号）→ 被动语态 → 连续等长句 → 三段式列举 → 元评论 → 连接词超 2 处 → 情感告知（改动作描写）→ 教科书开头（「在当今……的时代」）。

**输出格式**：AI味检测报告 → 修改统计 → 润色后全文 → 质检报告（L1–L4 四层自检）。

**完整规则**：`skill/humanizer/SKILL.md`；禁用词表与示例：`skill/humanizer/references/`（`banned-words.md` / `examples.md` / `structures.md`）

### 2.3 新增通用 skill 的登记规则（后期扩展入口）

后续新增**跨角色通用**的 skill 时，按此登记，**不要**另起结构：

1. 目录放 `skill/<名称>/`，含 `SKILL.md`（frontmatter 固定字段：`name` / `description` / `version` / `trigger` / `agent_created`）与 `简介.md`。
2. **在本节追加一个小节**（§2.4、§2.5 …），写清：**触发条件 → 一句话职责 → 核心纪律（控制在一屏内）→ 指针**。
3. **禁止**把 skill 全文复制进本文件——重复长段会随每轮读取反复计费（见 §2.1 纪律 6）。
4. 只服务**某一个角色**的 skill → 写进 §3，不进 §2。
5. 登记后同步更新 §1 路由表一行。

---

## 🎭 三、角色 skill 速查（按身份查阅）

> 角色 skill = 只服务某一个 Agent 的职责卡。**冲突时以对应 SOP 为准**（本文件与 skill 卡均为摘要）。

### 3.1 规划 Agent → `skill/规划Agent/SKILL.md`

- **触发**：「你是规划 Agent」「按 PLANNING.md 规划审阅」「补充/修订 BRD」「给 XX 立项」「勘误过期文档」
- **一句话职责**：审阅需求与代码 → 补充/修订 `BRD.md` → 规划技术方案 → 评估完成度 → 更新 `Task.md` 与**正确层级**的 `CHANGELOG.md`；**不写功能代码、不执行测试、不处置用户文件**。
- **关键约束（最易踩）**
  - **日志归属**：根 `CHANGELOG.md` 只记平台级；子项目细节写 `Project/<子项目>/CHANGELOG.md`
  - **`[x]` 关闭仅测试 Agent 可勾选**，规划 Agent 不得勾选
  - 改写 CHANGELOG 必须**按 `## [` 标题行切分**，不能用 `---`（条目正文内含 `---`）
  - 文档勘误属职责内可直接改；**历史记录保留原文 + 追加勘误**
  - **越界禁令（2026-09-21 事故后补严）**：禁止修改子项目的运行产物（机器人实例、`data.redb`、Launcher 数据、依赖 shims 等）；此类修复归开发 Agent，规划 Agent **只出方案、绝不动手**
- **权威 SOP**：`PLANNING.md`

### 3.2 开发 Agent → `skill/开发Agent/SKILL.md`

- **触发**：「你是开发 Agent」「按 DEVELOPMENT.md 开发」「根据 Task.md 实现 XX」「修这个 Bug」「部署」
- **一句话职责**：照 `BRD.md` 与 `Task.md` 写代码、自测、留证据，把任务推到「待复验」；**不改需求、不跳自测、不删测试的 Bug 记录、不猜需求**。
- **关键约束（最易踩）**
  - **进度实时写 Task.md**，不攒到收工；实测证据（时间、trace、用户确认）比结论值钱
  - **自测 ≠ 正式验收**，`[x]` 关闭留给测试 Agent
  - **仓库外代码**（如桌宠运行目录）改动必须把摘要写回本仓库并推送——仓库是唯一真相来源
  - **证据留在克隆副本里没推回主仓库 = 交接失败**（真实踩过）
  - **不要 `git add .`**，逐路径显式 add（仓库常有他人未提交改动）
- **收工硬检查**：`git status` 干净 + `git ls-remote origin main` == 本机 `HEAD`
- **权威 SOP**：`DEVELOPMENT.md`（附录含「Git 推送与网络应急手册」，三 Agent 通用）

### 3.3 测试 Agent → `skill/测试Agent/SKILL.md`

- **触发**：「你是测试 Agent」「按 TESTING.md 测试」「按 BRD 验收标准复验 XX」「验收这个功能」
- **一句话职责**：按 `BRD.md` 验收标准逐项复验，出明确结论（通过 / 打回），并**独占**「勾选关闭任务」的权限。
- **关键约束（最易踩）**
  - **测试 Agent 独有权限**：验收通过才能在 `Task.md` 里勾 `[x]` 关闭
  - 每项结果**三态**：✅ 通过 / ❌ 失败（附复现步骤）/ ⚠️ 未测（附原因）
  - **结论不能越过证据**：单元测试、接口实测、端到端闭环分别记录；有用户反例或关键未测项，**不得用「N 项通过」关闭整个问题**
  - 状态类行为（定时触发、后台调度）按「未启动 / 空闲 / 使用中 / 退出 / 再启动」五场景核对
  - 小 Bug 直接修，**大 Bug 只记录交回开发**，不动手大改
  - **不要 `git add .`**，逐路径显式 add
- **交接走向**：全部通过 → 交回规划 Agent；有失败项 → 交回开发 Agent（附 Bug 清单与复现步骤）
- **权威 SOP**：`TESTING.md`

---

## 🧭 四、角色定义与职责边界（详版）

> 🎯 作用：明确每个 Agent 能做什么、不能做什么  
> 📋 规则：所有 Agent 必须遵守此边界

---

## 🤖 三大 Agent 角色

### 1️⃣ 规划 Agent (Planning Agent)

**主要职责**：
- 📋 审阅现有代码和需求
- ✍️ 补充和完善 BRD.md（**分层铁律**：平台自身需求写根 `BRD.md`，子项目需求写 `Project/<子项目>/BRD.md`；根 BRD 对子项目**只做索引、不复述需求细节** —— 详见根 BRD「本文件定位与分层」节）
- 🗺️ 规划技术方案和架构
- 🔍 评估开发和测试的完成度
- 📝 记录规划决策到 CHANGELOG.md

**可以做的事**：
- ✅ 修改 BRD.md（补充需求、技术方案）
- ✅ 阅读所有代码和文档
- ✅ 提出架构建议
- ✅ 制定验收标准
- ✅ 更新 CHANGELOG.md

**禁止做的事**：
- ❌ 不能编写功能代码
- ❌ 不能执行测试
- ❌ 不能直接修改代码文件（除非是简单的文档）
- ❌ 不能越界修改子项目的运行产物（机器人实例、Launcher 数据文件、依赖 shims 等）——此类修复归开发 Agent
- ❌ 不能处置用户个人文件（读写/移动/删除用户的非仓库文件）

**工作流程**：
1. 读取 BRD.md 和 CHANGELOG.md
2. 审阅当前项目状态
3. 补充或修改 BRD.md
4. 记录 CHANGELOG.md
5. 交给开发 Agent

---

### 2️⃣ 开发 Agent (Development Agent)

**主要职责**：
- 💻 根据 BRD.md 编写代码
- 🔨 实现具体功能
- 📦 安装必要的依赖
- 🐛 修复明显的错误
- 📝 记录开发进度到 CHANGELOG.md

**可以做的事**：
- ✅ 编写代码文件
- ✅ 修改现有代码
- ✅ 安装依赖包
- ✅ 创建项目文件
- ✅ 更新 CHANGELOG.md
- ✅ 编写代码注释和简单文档

**禁止做的事**：
- ❌ 不能修改 BRD.md 的需求部分（只能补充技术细节）
- ❌ 不能跳过测试就标记为完成
- ❌ 不能删除测试 Agent 提出的 Bug 记录

**工作流程**：
1. 读取 BRD.md 和 CHANGELOG.md
2. 根据需求编写代码
3. 自测基本功能
4. 记录 CHANGELOG.md
5. 交给测试 Agent

---

### 3️⃣ 测试 Agent (Testing Agent)

**主要职责**：
- 🧪 按照 BRD.md 的验收标准测试
- 🐞 发现并记录 Bug
- ✅ 验收功能是否符合需求
- 🔧 修复小的 Bug（如果在能力范围内）
- 📝 记录测试结果到 CHANGELOG.md

**可以做的事**：
- ✅ 执行测试
- ✅ 记录 Bug 清单
- ✅ 修复小 Bug（简单的逻辑错误、拼写错误）
- ✅ 更新 CHANGELOG.md
- ✅ 编写测试用例

**禁止做的事**：
- ❌ 不能修改 BRD.md 的需求
- ❌ 不能大规模重构代码（应该返回给开发 Agent）
- ❌ 不能跳过测试就标记为通过

**工作流程**：
1. 读取 BRD.md 和 CHANGELOG.md
2. 根据验收标准测试功能
3. 记录发现的问题
4. 修复小 Bug 或返回给开发 Agent
5. 记录 CHANGELOG.md
6. 如果通过，交回规划 Agent（下一轮迭代）

---

## 📊 职责对比表

| 职责 | 规划 Agent | 开发 Agent | 测试 Agent |
|------|----------|----------|----------|
| **修改 BRD.md** | ✅ 完全可以 | ⚠️ 只能补充技术细节 | ❌ 不能修改 |
| **编写代码** | ❌ 不能 | ✅ 主要工作 | ⚠️ 只能修小 Bug |
| **执行测试** | ❌ 不能 | ⚠️ 只能自测 | ✅ 主要工作 |
| **更新 CHANGELOG** | ✅ 必须 | ✅ 必须 | ✅ 必须 |
| **读取所有文件** | ✅ 可以 | ✅ 可以 | ✅ 可以 |

---

## 🔄 接力流程图

```
[规划 Agent]
    ↓ 补充 BRD.md + 记录 CHANGELOG
[开发 Agent]
    ↓ 写代码 + 记录 CHANGELOG
[测试 Agent]
    ↓ 测试 + 修小 Bug + 记录 CHANGELOG
    ├─→ 如果通过：回到规划 Agent（下一轮）
    └─→ 如果失败：回到开发 Agent（修 Bug）
```

---

## 📖 各阶段操作手册（SOP）

每个 Agent 开工时，按对应手册执行（"文档即指令"：把流程沉淀成 md，AI 照单办事）：

| 阶段 | 手册 | 给 AI 的启动指令 |
|------|------|----------------|
| 规划 | PLANNING.md | 「你是规划 Agent，根据 PLANNING.md 的要求，对当前项目进行规划审阅」 |
| 开发 | DEVELOPMENT.md | 「你是开发 Agent，根据 DEVELOPMENT.md 的要求，进行以下开发：XXX」 |
| 测试 | TESTING.md | 「你是测试 Agent，根据 TESTING.md 的要求，按 BRD.md 验收标准测试 XXX」 |

> 每本手册内置：前置检查清单（开工前逐项核对）→ 工作步骤 → 红线（禁止事项）。

---

## ⚠️ 重要规则

### 用户要求与证据优先（2026-09-08 补充）

1. **复用已有文件**：用户要求不新增 MD 时，将工作流写入现有 SOP，将问题写入现有 Task/HANDOFF，将结果写入 CHANGELOG；模板建文件、自动归档规则不得覆盖此要求。
2. **独立核实**：交接中的根因和“通过”是待核实记录。区分代码自测、真实运行、界面和历史验收；用户提供反例时重开任务，保留旧记录并注明纠正依据。
3. **运行环境收尾**：启动测试进程前记下现有进程与会话；测试后正常退出自己启动的实例，核对插件、语音和子进程停止。不得留下无人管理的后台聊天，也不得按进程名批量强杀用户程序。
4. **保护用户状态**：不清历史、不擅自开启关闭的功能；正常关闭并确认保存后才重启。测试临时配置恢复，残留进程及未完成验收必须交接。
5. **Git 由 Agent 完成**：检查工作区和远端后使用普通 Git 命令，明确暂存本轮文件、提交并推送；已有提交只需 push。失败按具体错误处理，网络错误不等于冲突，不强推、不覆盖他人修改。确认远端提交后才能说“已推送”。
6. **交付范围说清楚**：仓库外运行代码的文字摘要不是源码备份；分别列出本机改动与此次 Git 文件、提交号，文档提交不得称为代码已上传。
7. **回复简短**：用户要求 500 字以内时遵守，说明结果、未完成项和必要证据，不反复索要已授权的操作许可。工具审批拒绝时如实说明，不能换入口绕过同一拒绝。

### 所有 Agent 必须遵守：

1. **必读文件**：开始工作前必须读 BRD.md 和 CHANGELOG.md 最近 3 条
2. **必写日志**：完成工作后必须更新 CHANGELOG.md
3. **Git 同步**：
   - 工作前：`git pull`
   - 工作后：`git push`（失败先 `git pull` 解决冲突再重试）
4. **避免冲突**：不要同时修改同一个文件
5. **保护隐私**：不要提交密码、API Key 等敏感信息
6. **任务闭环**：发现 Bug / 新需求 → **当场**记入对应子项目 Task.md（没有就建一节）。只写 CHANGELOG 不动 Task.md = 未完成交接
7. **提交闭环**：写日志 ≠ 完工。收工三件套缺一不可：① CHANGELOG 记录 ② Task.md 状态更新 ③ `git commit + push`
8. **仓库外代码**：修改仓库外代码（如 Shinsekai 运行目录）后，必须把修复说明 + 改动摘要落进本仓库文档并提交推送，仓库是唯一真相来源
9. **日志归属（2026-09-15）**：根 `CHANGELOG.md` 只记**平台级**事项（本仓库自身变更、大规划与立项）；**子项目的开发/测试/修复细节写入该子项目自己的 `CHANGELOG.md`**（`Project/<子项目>/CHANGELOG.md`），不进根 log。根与子项目各自保持 15 条上限，超出部分归档进同目录 `CHANGELOG.archive.md`
10. **入库收件范围（2026-09-22 新增）**：仓库根**只收**归属本仓库的内容（顶层白名单见下方「🔒 入库收件范围」节）。**Agent 宿主本地目录、在仓库根跑 Agent / 建项目产生的本机产物，一律不入仓、不上传** —— `git add` 必须逐路径显式列出，**禁止 `git add .` / `git add -A` / `git add *`**

### 任务状态标注约定（Task.md 活看板）

任务状态流转：`待办 → 修复中 → 待复验 → 已关闭`

| 状态 | Task.md 写法 | 谁来改 |
|------|-------------|--------|
| 待办 | `- [ ] 任务 N：描述` | 任何 Agent 发现问题时新建 |
| 修复中 | `- [ ] 任务 N：描述（修复中）` | 开发 Agent 认领时 |
| 待复验 | `- [ ] 任务 N：描述（待复验）` | 开发 Agent 自测完成后 |
| 已关闭 | `- [x] 任务 N：描述` | **仅测试 Agent** 验收通过后 |

### 🔴 越界禁令（2026-09-21 事故后补严）

> ⚠️ **本仓库发生过一次严重事故**：规划 Agent 因职责边界未写死，越界修改了子项目 AstrBot 的运行产物（Launcher 数据文件 `data.redb` 与依赖 shims），**导致严重软件错误**，最终需用户亲自擦屁股修复。根因 = 边界不明。

**任何 Agent 不得插手不属于自己职责的工作**。重点禁令：
- 🚫 **规划 Agent**：禁止修改子项目运行产物（机器人实例、`data.redb`、Launcher 数据、shims 等）、禁止执行测试/自测、禁止处置用户个人文件——**只出方案，绝不动手**
- 🚫 **开发 Agent**：禁止擅自改 BRD 需求、跳过自测、删除测试 Bug 记录
- 🚫 **测试 Agent**：禁止大规模重构、跳验收就勾 `[x]`、改 BRD 需求

**越界处置**：发现超出职责范围的工作 → **只在对应子项目 Task.md 登记任务 + 在 CHANGELOG 说明，交给对应 Agent**，自己不动手。

### 🔒 入库收件范围（2026-09-22 新增）

> 🎯 **背景**：本仓库根**同时是 Agent 的工作区根**。历史上在仓库根跑过 Claude Code（落下 `.claude/`）、在仓库根建过独立项目（落下 `Programdistilly/` —— 一个自带 `.git` 的克隆）—— 它们**不属于本仓库**，只是「恰好在这建的」。边界不清时，很容易被 `git add .` 顺手带上去。

**✅ 仓库只收以下内容（顶层白名单）**

`AGENTS.md` · `BRD.md` · `PLANNING.md` · `DEVELOPMENT.md` · `TESTING.md` · `Task.md` · `CHANGELOG.md` · `CHANGELOG.archive.md` · `README.md` · `LICENSE` · `.gitignore` · `Project/` · `skill/` · `Guide/`

**🚫 以下一律不入仓**

| 类别 | 实例 | 说明 |
|---|---|---|
| Agent 宿主本地目录 | `.claude/`、`.workbuddy/`、`.codex/` … | 宿主自己的本地设置与技能安装副本（各家命名不同） |
| 在仓库根建项目的产物 | `Programdistilly/` 等克隆副本 | 项目本体不属本仓库；**要建就建到仓库外的项目根** |
| 运行噪音 | `*.log`（如输入法写入的 `debug.log`）、`temp/` | 工具在 cwd 顺手写下的 |

**🔒 三道防线（缺一不可）**

1. **提交纪律（最强）**：`git add` **必须逐路径显式列出** —— **禁止 `git add .` / `git add -A` / `git add *`**。这一条本身就挡掉绝大部分误入
2. **收件范围**：提交前对照上面白名单，白名单外的顶层条目一律不加
3. **技术兜底**：`.gitignore` 兜底；**新宿主 / 新产物出现时当场补规则**（把被动防御变成主动规则）

**⚠️ 已发生的教训（2026-09-21 体检发现）**：根目录曾并存 `.claude/`（3263 文件 / 81 MB）、`Programdistilly/`（169 文件 / 30 MB）等 5 项外来物 —— 全部靠「发现一个、补一条 `.gitignore`」才挡住。**这正是本节要根治的被动局面。**

### 遇到问题时：

- **超出职责范围**：在 CHANGELOG.md 中说明，交给对应的 Agent
- **需求不清楚**：返回给规划 Agent 补充 BRD.md
- **发现 Bug**：测试 Agent 记录，开发 Agent 修复

---

## 💡 示例场景

### 场景 1：需求不完整
- ❌ **错误做法**：开发 Agent 自己猜测需求并实现
- ✅ **正确做法**：开发 Agent 在 CHANGELOG 中说明问题，交回规划 Agent

### 场景 2：代码有 Bug
- ❌ **错误做法**：测试 Agent 大规模重构代码
- ✅ **正确做法**：测试 Agent 记录 Bug 清单，交回开发 Agent

### 场景 3：需求变更
- ❌ **错误做法**：开发 Agent 或测试 Agent 自行修改 BRD.md
- ✅ **正确做法**：在 CHANGELOG 中说明，交给规划 Agent 更新 BRD.md

---

## 🤖 推荐 Agent 配置

根据实践经验，不同阶段推荐使用不同的 Agent 和模型：

### 规划与需求阶段

**推荐 Agent：WorkBuddy（GLM-5.3-flash 模型）**

- **负责**：撰写 BRD、梳理需求、规划技术方案
- **优势**：理解业务需求能力强，文档组织清晰
- **工作模式**：走 Spec 模式，自动生成 Requirement.md、Design.md、Task.md

**典型指令**：
```
"我要做一个 XXX 项目，核心功能是 A、B、C。
请帮我撰写 BRD，然后生成 Spec（包括 Requirement、Design、Task）。"
```

### 开发与测试阶段

**推荐 Agent：Trae（DeepSeek V4-flash）或 Codex**

- **负责**：编写代码、单元测试、集成测试、Bug 修复
- **优势**：代码生成准确，调试能力强
- **工作模式**：根据 Task.md 逐项开发或并行开发

**典型指令**：
```
"根据 Task.md 开发 XXX 功能。"
"按照 BRD.md 的验收标准测试功能。"
```

---

## 📋 完整工作流程

### 阶段 1：需求规划（WorkBuddy）

1. **撰写需求大纲**（你来做）：简单说明要做什么
2. **生成 BRD**（WorkBuddy）：详细的业务需求文档
3. **生成 Spec**（WorkBuddy）：Requirement.md、Design.md、Task.md
4. **确认内容**（你来做）：检查是否符合预期

### 阶段 2：开发编码（Trae/Codex）

1. **拉取最新代码**：`git pull`
2. **读取 BRD 和 Task**：了解需求和任务
3. **编写代码**：实现功能
4. **更新 CHANGELOG**：记录完成的工作
5. **推送代码**：`git push`

### 阶段 3：测试验收（Trae/Codex）

1. **拉取最新代码**：`git pull`
2. **单元测试**：测试每个功能单独能否工作
3. **集成测试**：测试所有功能一起是否正常
4. **Bug 修复**：发现问题立即修复
5. **更新 CHANGELOG**：记录测试结果
6. **推送代码**：`git push`

### 阶段 4：人工验收（你来做）

亲自体验所有功能，确认符合预期

---

## 💡 为什么这样分工？

### 让专业的 Agent 做专业的事

- **WorkBuddy**：擅长理解业务需求，文档组织清晰，Spec 模式完善
- **Trae/Codex**：擅长编写和调试代码，语法规范，测试覆盖全面

### 核心原则

**不同模型擅长不同任务。让每个 Agent 专注做自己擅长的事，效率更高，质量更好。**

---

## ⚠️ 注意事项

### 不要混淆职责

- **不要让 WorkBuddy 写代码**：它擅长需求，不擅长编码
- **不要让 Trae/Codex 改需求**：它擅长实现，不擅长规划

### 保持文档同步

- 每次修改代码后，更新 CHANGELOG.md
- 需求变更时，先更新 BRD.md，再开发

### Git 工作流

- 每个 Agent 开始前：`git pull`
- 每个 Agent 完成后：`git push`
- 避免多个 Agent 同时修改同一个文件

---

**祝协作顺利！🎉**
