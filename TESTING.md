# 测试 Agent 操作手册（TESTING SOP）

> 🤖 适用于：测试 Agent (Testing Agent)
> 📖 启动指令：「你是测试 Agent，根据 TESTING.md 的要求，按 BRD.md 验收标准测试 <项目/功能>」
> 🔗 配套文档：AGENTS.md（角色职责）、BRD.md（验收标准来源）、CHANGELOG.md（交接日志）

---

## 第一步：前置检查（全部通过才能开工）

- [ ] 已执行 `git pull`，与远程同步
- [ ] 已通读 BRD.md 验收标准（平台级 + 子项目级）
- [ ] 已读 CHANGELOG.md 最近 3 条记录（重点看开发 Agent 的自测结果与遗留问题）
- [ ] 已查看 Task.md 中标记「待复验」的任务——这是本轮要验收的对象

## 第二步：逐项测试

1. 把验收标准拆成一条条可核对项，逐项测试
2. 每项记录结果：
   - ✅ 通过
   - ❌ 失败（必须附复现步骤）
   - ⚠️ 未测（必须附原因）
3. **Task.md 状态更新（测试 Agent 独有权限）**：
   - 验收通过 → 任务改为 `- [x]`（已关闭）
   - 验收失败 → 任务改回「待办」并注明 Bug 与复现步骤，交回开发 Agent
   - 部分通过 → 保持「待复验」，注明剩余问题

## 第三步：处理问题

### 桌宠与后台插件的状态验收

| 场景 | 应核对的行为 | 必须保留的证据 |
|---|---|---|
| 仅打开设置、未启动聊天 | 无独立聊天实例时，不生成主动聊天或语音 | 入口、进程归属、会话日志和观察时段 |
| 正常启动聊天并空闲 | 到期触发，当前界面显示，语音与历史对应同轮 | 会话/轮次 ID、触发事件、界面与历史结果 |
| 用户正在对话或播放语音 | 心跳遵守忙碌约束，不插队、不重复入队 | 用户活动与调度时序 |
| 正常退出聊天或测试结束 | 调度停止，队列停止，历史保存，测试进程退出 | 退出前后进程、保存结果、退出后观察时段 |
| 再次启动 | 仅一个目标聊天实例，原历史可恢复 | 实例数量、会话 ID、恢复结果 |

观察时长须覆盖已知触发窗口或使用隔离的可控时钟，并说明局限；短时无声音不能证明永久不会触发。不得为了方便验收擅改用户的定时识屏开关。

### 结论不能越过证据

单元测试、接口实测、UI/语音/历史闭环分别记录通过、失败或未测。存在用户反例或关键未测项时，不得以“47 项通过”等数字关闭整个问题。回归修复可确认，端到端未完成仍须保留待复验。

- **小 Bug**（拼写错误、明显笔误、简单逻辑错）→ 直接修复，并在 CHANGELOG 说明
- **大 Bug**（架构问题、大范围逻辑错误）→ 只记录 Bug 清单，交回开发 Agent，**不动手大改**

## 第四步：记录结论

在 CHANGELOG.md 顶部新增记录，必须写清：**测试结果汇总 / Bug 清单 / 明确结论（通过 或 打回开发）**。

## 第五步：收工检查（三件套，缺一不算完工）

- [ ] **① CHANGELOG.md** 已新增记录（测试结果汇总 / Bug 清单 / 明确结论：通过 或 打回）
- [ ] **② Task.md** 已更新（通过的任务已勾 `[x]` 关闭；失败的任务已改回「待办」并注明 Bug）
- [ ] **③ 改动已落库**：本轮修复的小 Bug、更新的文档已 `git commit + push`（仓库外代码的修复说明也要落库）

```bash
git add <改动文件>
git commit -m "测试 Agent：<简述>"
git push
```

> ⚠️ 只写日志不提交 = 工作白做。未实测不得勾选「已关闭」。

---

## 交接走向

- 全部通过 → 交回规划 Agent（进入下一轮迭代）
- 存在失败项 → 交回开发 Agent（附 Bug 清单）

---

## 红线（禁止）

- ❌ 修改 BRD.md 的需求
- ❌ 大规模重构代码
- ❌ 未测试就标记通过
- ❌ 只写日志：不更新 Task.md、不 commit/push 就宣布完工
