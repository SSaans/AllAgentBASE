# 业务需求文档 (BRD)

> 📅 更新时间：2026-09-24  
> 👤 维护者：规划 Agent  
> 🎯 状态：已立项 8 个子项目 —— 状态一览见本文「子项目需求规范 → 2. 子项目索引」；**需求详情一律在各子项目自己的 `Project/<子项目>/BRD.md`**（本文件只登记索引与状态，不复述细节）

---

## 本文件定位与分层（2026-09-22 规划 Agent 补充）

> 本节回答三个问题：根 BRD 记什么、流程与标准归谁管、子项目需求写在哪。
> **判断依据只有一句：一件事只能有一处真相来源。**

平台文档分三层，各管各的、互不重复：

| 层 | 文件 | 回答什么问题 | 谁维护 |
|---|---|---|---|
| **规范层** | `AGENTS.md`、`PLANNING.md` / `DEVELOPMENT.md` / `TESTING.md`、本文件「子项目需求规范」节 | 所有项目**怎么干**（流程 / 标准 / 模板 / 红线） | 规划 Agent |
| **平台需求层** | **本文件（根 `BRD.md`）** | **平台自己**要什么（把平台当成一个产品） | 规划 Agent |
| **子项目需求层** | `Project/<子项目>/BRD.md` | **某个子项目**要什么 | 规划 Agent 起草 → 用户确认 → 开发实现 |

**层级关系**：规范层约束平台需求层与子项目需求层（三个 Agent 都必须遵守）；平台需求层对子项目需求层**只做索引**；子项目需求层套用规范层的流程与平台需求层的通用模板。

> ⚠️ **术语区分（别混）**：这里的「三层」按**责任归属**分（谁的需求装哪个文件）。`skill/token节省` 里另有一套「文档三层」（L1 稳定层 / L2 半稳定层 / L3 易变层），那是按**变动频率**分（怎么写才省 token）—— 两者不是一回事。

### 根 BRD 收录边界

**收**：
- 平台自身的需求：项目目标、核心功能、技术方案、平台自身验收标准、不做的事情
- 子项目**索引**：名称 + 一行定位 + 状态 + 指向其 BRD 的路径
- **必须跨项目统一**的少量模板：子项目 BRD 结构、验收标准四类模板、立项流程

**不收**：
- 子项目的具体需求、功能清单、验收细节（**一律写在各子项目自己的 BRD**）
- 子项目的开发 / 测试 / 修复过程（写在各子项目自己的 `CHANGELOG.md`，与根 CHANGELOG 的分层同构）
- 重复搬运 SOP 全文（只写指针：「见 `PLANNING.md` 第 N 步」）

### 划分原则（6 条，可直接当判断规则用）

1. **「做什么」进 BRD，「怎么干」进 SOP / `AGENTS.md`** —— 需求与流程分家。
2. **一处唯一**：同一内容只写一次；根 BRD 对子项目**只索引、不抄内容**。
3. **与 CHANGELOG 同构**：根 CHANGELOG 只记平台级、子项目细节下沉 —— BRD 用**同一套**分层逻辑，两处口径必须对齐。
4. **验收标准分层**：平台自己的验收在本文件；子项目的验收在子项目 BRD，套用本文件给的通用模板。
5. **规范只留一份 + 指针**：本文件只写模板与指针，不把 SOP 全文搬第二遍。
6. **新子项目走立项流程**：本文件登记一行索引，细节全部进子项目 BRD。

---

## 项目目标

建立一个 **多 Agent 协作的 GitHub 项目管理平台**，让不同的 AI Agent 通过 Git 同步，在同一个仓库中接力完成任务。

---

## 核心功能

### 1. 统一的项目结构
```
AllAgentBASE/
├── Project/             # 正式子项目存放区（立项后放入；每个含 BRD / README / Task / CHANGELOG 四件套）
├── skill/               # 可复用 Agent 技能资产（含三 Agent 执行卡）
├── Guide/               # 工作流程图文指南
├── AGENTS.md            # 全平台 Agent 统一行为规范入口 + skill 路由中心（开工必读）
├── BRD.md               # 业务需求文档（本文件；平台级需求）
├── PLANNING.md          # 规划 Agent 操作手册（SOP）
├── DEVELOPMENT.md       # 开发 Agent 操作手册（SOP）
├── TESTING.md           # 测试 Agent 操作手册（SOP）
├── Task.md              # 平台级任务看板
├── CHANGELOG.md         # 平台级变更日志（15 条上限）
├── CHANGELOG.archive.md # 超限条目归档
├── README.md            # 仓库说明
├── LICENSE              # MIT 许可证
└── .gitignore           # Git 忽略规则
```

（2026-09-21 勘误：原结构图列出的 `Readme/`、`Any/` 两个目录已移入回收站 —— `Readme/` 是与根 README 重复的旧副本、`Any/` 是空占位；当前实际顶层结构以此图为准。）

> 🔒 **上图之外，仓库根还可能出现一批「本机产物」** —— Agent 宿主本地目录（`.claude/`、`.workbuddy/`）、在仓库根跑 Agent / 建项目留下的克隆副本（如 `Programdistilly/`）、运行噪音（`*.log`、`temp/`）。它们**不属于本仓库、一律不入仓**（已由 `.gitignore` 兜底）。**顶层白名单、三道防线与强制纪律见 `AGENTS.md` 的「🔒 入库收件范围」节。**

### 2. Agent 接力工作流

每个阶段一本操作手册（SOP），开工时对 AI 下达「根据 XX.md 的要求进行操作」的指令，AI 照手册办事：

- **规划 Agent**：按 PLANNING.md 执行 —— 审阅现状、补充需求、产出 Task.md 任务清单
- **开发 Agent**：按 DEVELOPMENT.md 执行 —— 照 BRD 与 Task.md 编码实现、自测
- **测试 Agent**：按 TESTING.md 执行 —— 按验收标准逐项测试、修小 Bug、出结论

每本手册内置：前置检查清单（开工前逐项核对）、工作步骤、红线（禁止事项）。

### 3. Git 同步机制
- Agent 开始工作前：`git pull` 拉取最新内容
- Agent 完成工作后：`git push` 推送修改
- 通过 CHANGELOG.md 进行工作交接

---

## 技术方案

### 版本控制
- **工具**：Git + GitHub
- **仓库地址**：https://github.com/SSaans/AllAgentBASE（2026-09-07 勘误：原误写为 SSaann）
- **分支策略**：暂时使用 main 分支，后续可根据需要建立功能分支

### 文档管理
- **BRD.md**：**平台自身**需求的唯一真相来源（目标 / 功能 / 技术方案 / 平台验收）；**子项目需求写在各子项目自己的 BRD**，本文件只做索引 —— 分层口径见本文「本文件定位与分层」节
- **CHANGELOG.md**：Agent 交接日志，记录每次改动
- **AGENTS.md**：**全平台 Agent 的统一行为规范入口与 skill 路由中心**——所有 Agent 执行任何任务前必须先读，再按任务类型查阅 `skill/` 下对应技能卡
- **PLANNING.md / DEVELOPMENT.md / TESTING.md**：各阶段操作手册（SOP），Agent 开工的执行依据
- **skill/**：可复用技能资产，按子目录封装（`skill/<技能名>/SKILL.md` + `简介.md`；**外部原样导入**的技能按上游包结构保存，如 `skill/humanizer`，不套用这两项要求）。`SKILL.md` 的 frontmatter 写明 `name` / `description` / `trigger`，正文含触发条件、输入输出与可复用步骤；三 Agent 执行卡（`skill/规划Agent`、`skill/开发Agent`、`skill/测试Agent`）与降本纪律（`skill/token节省`）均在此

### 协作规则
1. 所有 Agent 必须先读 BRD.md 和 CHANGELOG.md 最近 3 条
2. 完成工作后必须更新 CHANGELOG.md
3. 避免多个 Agent 同时修改同一文件
4. 敏感信息（密码、API Key）禁止提交到仓库
5. **任务闭环（2026-09-08 新增）**：任何 Agent 发现 Bug 或新需求，必须**当场**记入对应子项目的 Task.md（没有就建一节）。任务状态流转：`待办 → 修复中 → 待复验 → 已关闭`。Task.md 是活看板，必须随工作实时更新；**只写 CHANGELOG 不动 Task.md 视为未完成交接**
6. **提交闭环（2026-09-08 新增）**：写日志 ≠ 完工。收工三件套缺一不可：① CHANGELOG 已记录 ② Task.md 状态已更新 ③ `git commit + push` 已执行。push 失败先 `git pull` 解决冲突再重试
7. **仓库外代码（2026-09-08 新增）**：子项目代码可能在实际运行目录而非本仓库（如 Shinsekai 位于 `H:\Program\新世界\Shinsekai`）。修改仓库外代码后，必须把**修复说明 + 改动文件与摘要**写进本仓库（子项目 HANDOFF 文档或 CHANGELOG）并提交推送，保证仓库始终是唯一真相来源
8. **入库收件范围（2026-09-22 新增）**：仓库根**同时是 Agent 工作区根**，宿主目录与本机产物会「恰好」落在里面，但**它们不属于本仓库**。入库只收顶层白名单内的内容（见 `AGENTS.md`「🔒 入库收件范围」）；`git add` **必须逐路径显式列出，禁止 `git add .`**

---

## 验收标准（平台自身）

> 2026-09-07 规划 Agent 更新：逐项核对当前完成度

- [x] 项目结构完整（核心文档 + 子项目区）— Project/ 曾缺失，已用 .gitkeep 补齐；skill/ 用于保存可复用 Agent 技能资产（2026-09-21 勘误：原 `Any/` 空占位、`Readme/` 重复副本已移入回收站）
- [x] 核心文档齐全（BRD、CHANGELOG、AGENTS、.gitignore）— 2026-09-06 开发 Agent 创建并验证
- [x] 能够成功执行 git pull/push — pull 于 2026-09-06 验证；push 由规划 Agent 2026-09-07 提交本次更新验证
- [x] 多个 Agent 能够基于 CHANGELOG 进行协作 — 已完成 开发 Agent → 规划 Agent 两棒交接
- [x] README.md 已推送到 GitHub — 根目录一份（2026-09-21 勘误：原 `Readme/` 下的重复副本已移入回收站）

---

## 不做的事情

- ❌ 不支持多个 Agent 同时编辑同一文件
- ❌ 不存储敏感信息到仓库

---

## Git 归属（2026-09-20 用户明确）

> 🔴 **Git 一律由 Agent 自行闭环，任何情况都不要求用户人工介入。**

- 拉取（`pull` / `fetch`）、合并（含**冲突解决**）、提交、推送，**全部由 Agent 自己完成**。
- 冲突不是"交给用户"的理由：自己 `pull` → 自己解冲突 → 自己重试推送，直到远端确认同步。
- 网络 / 代理 / 凭据问题同样由 Agent 自行排查处置，处置手册见 `DEVELOPMENT.md` 附录「Git 推送与网络应急手册」。
- 「我不会弄 / 你手动拉一下」这类说法**不允许出现**；确实无解时必须写清具体错误、已试过的办法与残留状态，而不是把操作甩给用户。

---

## 子项目需求规范（2026-09-07 规划 Agent 补充）

> 本节用于解决原"待补充内容"中的三个空白项：具体子项目需求、子项目验收标准、自动化测试方案

### 1. 子项目立项流程

所有正式子项目放入 `Project/<子项目名>/`，立项必须满足：

1. **用户发起**：由用户指定要做什么，Agent 不得自行立项
2. **规划 Agent 起草子项目 BRD**：包含目标、功能清单、验收标准
3. **用户确认后进入开发**：开发 Agent 才能开始编码

每个子项目的标准目录结构：

```
Project/<子项目名>/
├── BRD.md          # 子项目需求（含验收标准，唯一真相来源）
├── README.md       # 使用说明
├── CHANGELOG.md    # 子项目自身的变更日志（子项目细节写这里，不进根 log）
└── src/ 等         # 代码目录（开发 Agent 创建）
```

### 2. 子项目索引

> 本节**只做索引**：登记名称、一行定位、当前状态，并指向各自的详情文档。
> **子项目的需求细节一律不在此复述** —— 同一内容在根与子项目各写一份，必然两处过期、甚至互相矛盾。

**一、已立项（8 个）**

| 子项目 | 一行定位 | 状态 | 需求详情 |
|--------|----------|------|----------|
| Shinsekai | 桌宠部署 | 维护模式 | `Project/shinsekai项目byendcycle/BRD.md` |
| ALLBot部署 | AstrBot QQ 机器人「丛雨」 | 开发中 | `Project/ALLBot部署/BRD.md` |
| bilisum部署 | BiliSum 视频摘要工具 | ⛔ **已搁置**（2026-09-21 用户拍板「不搞了」） | `Project/bilisum部署/BRD.md` |
| distilly | 蒸馏自己 → Person Profile 技能包 | 阶段 1 环境部署完成，待提供材料进阶段 2 | `Project/distilly/BRD.md` |
| 表情包同步 | StickerSync（TIM 导出全自动 / 导入半自动，微信仅备料） | R2 规划完成，待用户放行 | `Project/表情包同步/BRD.md` |
| 万有引力 | 跨平台个人聊天记录归档（⚠️ 本仓库公开，按保密要求脱敏登记） | 规划完成，待用户拍板 | `Project/万有引力/BRD.md` |
| 中转站搭建 | 本机 new-api 面板聚合上游中转站（**自用**，不对外不收费） | 规划完成，待用户提供上游坐标 | `Project/中转站搭建/BRD.md` |
| ALLSkill | 外部技能的调研、评估与引进（一期：儿童心理辅导；二期：绘画指导已入库） | 一期待用户放行；二期已落地待复验 | `Project/ALLSkill/BRD.md` |

**二、未立项候选（等待用户拍板；立项时再写子项目 BRD，本节不预写需求）**

| 候选 | 说明 | 优先级 |
|------|------|--------|
| CHANGELOG 归档工具 | 自动归档超限条目 | 待定 |
| 文档一致性检查脚本 | 校验 BRD / CHANGELOG 格式与勾选项状态，可后续接入 CI | 待定 |

> 📌 上表为历史候选，**不代表当前授权**；新子项目由用户拍板后，规划 Agent 产出子项目 BRD，用户确认后开发 Agent 才能开始编码。
>
> 📌 各子项目的**立项过程留档**（谁指派、有无越界、边界约定）已从本节归拢至本文「立项留档与流程审计」节，本节只保留索引。

### 3. 子项目验收标准（通用模板 · 供各子项目 BRD 套用，区别于上文「验收标准（平台自身）」）

每个子项目的 BRD 必须包含以下四类验收标准，缺一不可：

1. **功能验收**：逐条列出可测试的功能点（"能够 XXX"），测试 Agent 逐项核对
2. **文档验收**：CHANGELOG 有完整的开发与测试记录；README 更新到最新用法
3. **质量验收**：无敏感信息提交；无未说明的 TODO/FIXME 遗留；依赖在 README 中声明
4. **交接验收**：测试 Agent 在 CHANGELOG 中明确签字"通过"；未通过则记录 Bug 清单并回到开发 Agent

### 4. 自动化测试方案（轻量级，分阶段）

考虑到平台当前以文档协作为主，不引入重型 CI，分三个阶段推进：

- **阶段一（当前，纯文档协作）**：不强制自动化；由测试 Agent 按验收标准人工核对
- **阶段二（首个子项目落地后）**：子项目自带最小测试集（如 Python 项目用 pytest），测试 Agent 本地执行并将结果记入 CHANGELOG
- **阶段三（可选）**：接入 GitHub Actions，push 时自动运行测试集与文档格式检查

### 5. 平台运维规则（补充）

- **CHANGELOG 归档**：按用户要求复用已有文件，保留历史证据与纠正记录；**不新建**归档文件，超限条目归档进已有的 `CHANGELOG.archive.md`。
- **CHANGELOG 归属（2026-09-15）**：根 `CHANGELOG.md` 只记**平台级**事项（本仓库自身变更、大规划与立项）；**子项目的开发/测试/修复细节写入各子项目自己的 `CHANGELOG.md`**（已在 `Project/ALLBot部署`、`Project/shinsekai项目byendcycle` 落地）。根与子项目各自保持 15 条上限，超出部分归档进同目录 `CHANGELOG.archive.md`。
- **文件夹职责（2026-09-22 勘误：删除已不存在的 `Readme/`、`Any/`）**：
  - `Project/`：正式子项目（立项后才可放入；每个含 BRD / README / Task / CHANGELOG 四件套）
  - `skill/`：可复用 Agent 技能资产（每个技能一个子目录，含 `SKILL.md`；三 Agent 执行卡与 `token节省` 纪律卡在内）
  - `Guide/`：工作流指南文档

---

## 立项留档与流程审计（历史留档，只增不改）

> 本节的用途只有一个：**留下"谁指派、有没有越界、边界怎么定"的证据**，供后续 Agent 与用户核对。
> 它不承载任何需求 —— 需求在各子项目 BRD。历史原文不改写，后续变更只追加。

> 📌 立项说明（2026-09-20）：`Project/distilly` 的 BRD 由**开发 Agent 按用户明确指派代拟**（用户指定「在 AllAgentBASE 里建一个 project，命名为 distilly，负责蒸馏自己」）。其**需求章节按 `AGENTS.md` 属规划 Agent 职责**，已在 BRD 与 `Task.md` 标注「待规划 Agent 确认」，后续复核由规划 Agent 完成。
>
> 📌 立项说明（2026-09-20）：`Project/表情包同步` 由**规划 Agent 按用户明确指派起草**（用户指定「现在在 allagentbase 里面就立个项吧，就叫表情包同步好了，你当规划 agent 履行你的职责」），需求与方案均在本职范围内，无越权问题。
>
> 📌 立项说明（2026-09-20）：`Project/万有引力` 由**规划 Agent 按用户明确指派起草**（用户指派「部署微信聊天记录提取工具 + 规划一个名为万有引力的项目」），需求与架构规划在本职范围内。
> ⚠️ 但**立项前存在流程越界**：用户直接指派「部署 + 验证」时，规划 Agent 未走本仓库流程，越界**写了功能脚本并执行了真机测试**，且规划成果先落在了私有仓库的 `docs/`。已在 `Project/万有引力/BRD.md` §6.2 全文登记，并据此确立核实口径——**该结论只是待核实记录，不构成验收通过**，须由开发 Agent 认领、测试 Agent 按验收标准复验。
> 🔴 另因本仓库为**公开**仓库，该子项目的坐标（私有仓库名/地址、账号标识、本机路径）按 `Project/万有引力/BRD.md` §七 **一律不在此登记**；是否转私有由用户拍板。
>
> 📌 立项说明（2026-09-22）：`Project/中转站搭建` 由**规划 Agent 按用户明确指派起草**（用户指派「新建 sub2api 中转站搭建（简称中转站搭建）的 project，并作出详细规划」），需求与架构规划在本职范围内，无越权问题。
> 🔀 **立项前已澄清一处需求误读**：用户原表述含「sub2api」，但经确认其**真实需求是搭 new-api 面板聚合各家中转站**；「sub2api」是其提供的调研报告里 22 家站的底座，非拟装软件。**底座确定为 new-api**，理由见 `Project/中转站搭建/BRD.md` §二；sub2api 转为延期方案（持有订阅账号时再评估）。目录名取用户给定的**简称**，未写入底座项目名。
> ⚖️ 用户曾就「这样做有无法律风险」提问，已按**自用 / 朋友白用 / 对外收费**三层作答并留档于 `Project/中转站搭建/BRD.md` §九 R5；结论：**对外收费运营不写进本子项目规划**。
> 🔴 本子项目边界：只监听本机、不对外开放、不收费；上游站点地址与 API Key 等凭据**一律不入库**。
>
> 📌 立项说明（2026-09-24）：`Project/ALLSkill` 由**规划 Agent 按用户明确指派起草**（用户指派「在 GitHub 与 skillhub.cn 等平台查找与儿童心理辅导最相似的 skill，列出候选并定出最匹配的一项，随后新建名为 ALLSkill 的项目」），需求、调研与选型论证均在本职范围内，无越权问题。
> 🔀 **立项前已澄清一处口径**：用户表述里的 `https://github.com` 与 `https://skillhub.cn/` 是**调研来源**，不是拟引进的对象；`ALLSkill` 为项目名、未限定范围。按「先调研、后立项」的上下文，本项目定为**外部技能的调研、评估与引进**，首期方向取用户点名的**儿童心理辅导**。该假设已写在 `Project/ALLSkill/BRD.md` 头部，若与用户本意不符，一句话即可纠偏。
> 🧭 **选型结论已留档**：共 10 项候选，评估维度固定为三维加权（功能定位 40% / 适用场景 30% / 目标用户 30%）；最匹配项为 SkillHub 的「青少年成长教练」（`@user_fe828a0e/youth-coach`，相似度 86），落选项一律写「延期 + 重新成立的条件」。详见 `Project/ALLSkill/BRD.md` §三、§四。
> 🔴 **本子项目边界**：一头连未成年人、一头连心理健康，八条红线写死在 `Project/ALLSkill/BRD.md` §六 —— 不诊断不治疗不开药、危机优先并转介（12355 / 12356 / 儿童心理科 / 110）、对话内容不上传公网不入仓库、不做亲子对错裁判、不承诺疗效、来源与许可证须可追溯。本仓库为公开仓库，技能只引进**本体与文档**，个人数据一律不入库。
> 🖌️ **二期留档（2026-09-25）**：引进 **Art Philosophy**（ClawHub，`@nyxur42/nyx-archive-art-philosophy`），落 `skill/绘画指导/`，已在根 `AGENTS.md` §1 路由表与 §2.4 登记。10 项候选与选型论证见 `Project/ALLSkill/BRD.md` §十。两处需留意：上游**许可不标准**（只留档、不外发、不收费分发）；该技能**不是技法教程**（教判断力与意图，不教握笔排线），优质的技法教学型技能仍属空白。一期那八条红线只服务儿童心理方向，**不随二期继承**。

---

## 历史项目状态审阅（2026-09-07，非当前状态）

- ✅ 仓库共 6 个提交，工作区干净，与远程 main 同步
- ✅ 核心文档齐全：BRD、CHANGELOG、AGENTS、.gitignore、README（根目录 + Readme/）
- ✅ 协作机制已实际运转一轮：开发 Agent 完成初始化并通过 CHANGELOG 交接
- ⚠️ `Project/`、`Any/` 文件夹缺失（Git 不跟踪空目录）→ 本次以 `.gitkeep` 占位补齐
- ⚠️ BRD 仓库地址笔误（SSaann → SSaans）→ 已修正
- ❌ 尚无已立项子项目 → 等待用户从候选清单指定第一个子项目

> 📌 **2026-09-22 勘误（保留原文不改写）**：本节是 2026-09-07 的历史快照。文中提到的 `Any/`、`Readme/` 两个目录已于 2026-09-21 移入回收站（前者为空占位、后者为与根 README 重复的旧副本）；当前实际顶层结构见本文「核心功能 §1 统一的项目结构」。

---

**历史下一步（2026-09-07，已由上述立项状态取代）**：
1. 用户确认第一个子项目（可从候选清单选择，也可提出新想法）
2. 规划 Agent 产出 `Project/<子项目名>/BRD.md`
3. 开发 Agent 在子项目 BRD 确认后接力开发
