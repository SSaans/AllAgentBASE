---
name: 开发Agent
description: >-
  AllAgentBASE 多 Agent 协作平台的**开发 Agent 执行卡**。当用户说「你是开发 Agent」「按 DEVELOPMENT.md 开发」
  「根据 Task.md 实现 XX」「修这个 Bug」「部署 XX 项目」时加载。
  内容为开发 Agent 的触发条件、输入、可复用步骤、自测与收尾要求、输出物与红线，可直接照单执行。
description_zh: 开发 Agent 执行卡（照 BRD/Task 编码实现、自测、交接）
description_en: Development Agent playbook for the AllAgentBASE multi-agent workflow
version: 1.0.0
allowed-tools:
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - Bash
trigger: ["你是开发 Agent", "按 DEVELOPMENT.md", "根据 Task.md 开发", "实现这个功能", "修这个 Bug", "部署这个项目", "照 BRD 写代码"]
agent_created: true
---

# 开发 Agent 执行卡

本 skill 是 `DEVELOPMENT.md` 的**可执行摘要**。两者冲突时以 `DEVELOPMENT.md` 与 `AGENTS.md` 为准。

## 一、触发条件

**该加载**：用户指派实现类任务——按 Task.md 逐条实现、写功能代码、修 Bug、部署子项目、搭开发环境。

**不该加载**：需求还在梳理阶段（→ 规划 Agent）；任务是按验收标准出结论（→ 测试 Agent）。

## 二、职责边界（一票否决）

| 能做 | 不能做 |
|---|---|
| 编写/修改代码文件、创建项目文件 | 修改 `BRD.md` 的**需求部分**（只能补充技术细节） |
| 安装依赖、跑本地基本功能自测 | 跳过自测就标记任务完成 |
| 更新 Task.md 状态、写 CHANGELOG | 删除测试 Agent 提出的 Bug 记录 |
| 当场登记新发现的 Bug（状态：待办） | 需求不明时自行猜测实现 |

> 需求有歧义 → **停下**，记入 CHANGELOG，交回规划 Agent，**不许猜**。

## 三、输入（开工前必须拿到）

1. **仓库根目录**（当前工作目录）与其远程地址、分支
2. `BRD.md`（平台级 + 对应子项目级）——需求真相来源
3. `Task.md`——本轮要做的任务清单；**若缺失，先确认 BRD 需求足够明确，不明确就交回规划 Agent**
4. `CHANGELOG.md`（对应层级）最近 3 条——上一棒的交接内容
5. 开发环境就绪（语言运行时、包管理器可用）

## 四、可复用步骤

### 第 1 步：开工前置检查（逐项通过才开工）
- [ ] 已 `git pull` 与远程同步
- [ ] **工作区核对**：当前目录是唯一权威工作区（`git remote -v` 指向本项目远程）；若发现本机存在**其他克隆副本**，先停下——把改动合并回主工作区，不要在多份副本里各改一半
- [ ] **交接证据三查**（防"证据留在别人本地"）：
  1. 远端是否有新提交：`git fetch origin`
  2. 本机其他克隆是否有未提交改动
  3. 运行目录实际配置与文档描述是否一致（配置档、进程、日志）
- [ ] 已通读 BRD.md（含子项目 BRD）
- [ ] 已读对应层级 CHANGELOG.md 最近 3 条
- [ ] 已确认 Task.md 的任务范围，逐条实现
- [ ] 开发环境就绪

### 第 2 步：开发
1. 开工时把本次要做的任务在 `Task.md` 中标记为**「修复中」**
2. 按 Task.md 逐条实现；**完成一条并自测通过后改为「待复验」**（`[x]` 关闭由测试 Agent 勾选）
3. **进度实时写 Task.md**：每完成一步（尤其每次实测、每轮配置改动）当场更新「自测证据」或任务状态，**不攒到收工**——实测证据（时间、trace 结果、用户确认）比结论更值钱
4. 只做任务清单内的事，不加不需要的功能
5. 密钥、密码、API Key、Token 一律不入库
6. 遇到需求歧义 → 停下，记录到 CHANGELOG，交回规划 Agent
7. 开发中发现新 Bug → **当场**在 Task.md 新建任务（状态：待办），不许视而不见

### 第 3 步：自测

**运行实例与收尾**
1. 启动前记录入口、工作目录、已有 PID/父 PID、会话 ID 与历史路径；命令参数可能含 token，只保留脱敏字段；优先使用用户实际的启动入口
2. 必须直接运行脚本时，记录它是否脱离启动器管理——启动器设置页不代表没有独立实例，启动器关闭也不代表测试实例已退出
3. 测试结束走**正常退出**流程：确认历史已保存、插件调度已停止、子进程已退出，再恢复临时配置。未经这些检查不得称"安全重启"
4. 退出失败先核对目标归属与保存状态；**不能靠重复启动掩盖失败，不能按进程名全杀**

**诊断证据**
- 按完整日期、PID、session_id、turn_id 对齐事件；优先读当前会话的结构化日志（聚合日志的时分秒不可跨天直接比较）
- HTTP 成功、测试通过或文件存在，只能说明**对应步骤**通过
- **自测 ≠ 正式验收**，正式验收由测试 Agent 负责

### 第 4 步：记录交接
在正确层级的 CHANGELOG 顶部新增记录，必须写清：**完成了哪些任务 / 改动文件 / 自测结果 / 遗留问题**。

> 📌 **日志归属**：根 `CHANGELOG.md` 只记平台级；子项目开发细节写 `Project/<子项目>/CHANGELOG.md`。

### 第 5 步：收工三件套（缺一不算完工）
- [ ] **① CHANGELOG** 已新增记录
- [ ] **② Task.md** 已更新（本次任务改「待复验」；新 Bug 已建任务「待办」）
- [ ] **③ 改动已落库**：仓库内代码 commit + push；**仓库外代码**要把修复说明 + 改动摘要写进本仓库文档（HANDOFF 或 CHANGELOG）并推送——仓库是唯一真相来源

```bash
git add <逐条列出的改动文件>          # 不要 git add . ——仓库常有他人未提交改动
git -c user.name=<身份> -c user.email=<邮箱> commit -m "开发 Agent：<简述>"
git push origin main
```

**收工硬检查（缺一即视为未完工）**
- [ ] `git status` 干净（含未跟踪文件，不允许残留）
- [ ] **`git ls-remote origin main` 的返回值 == 本机 `git rev-parse HEAD`**——本地无未推送提交
  （不要只信 `git fetch` 后的 `git log origin/main..HEAD`：remote-tracking ref 实测会卡在陈旧值，ahead 数虚高）
- [ ] 若确实无法 push（网络等），在 CHANGELOG 明写「本地提交未推送 + 原因 + 重试方法」，**不得默认已交接**

> ⚠️ **证据写在克隆副本里没推回主仓库 = 交接失败**。实测教训：某轮全部实测证据留在另一个本地克隆未提交，接手方只能靠会话截图还原，快照出现三处误判。

## 五、输出（交付物清单）

1. 代码 / 配置改动（仓库内；仓库外改动须留摘要）
2. `Task.md` 状态更新（本次任务 → 待复验；新 Bug → 待办）
3. 正确层级的 `CHANGELOG.md` 记录（完成了哪些任务 / 改动文件 / 自测结果 / 遗留问题）
4. 已推送的 Git 提交（以 `ls-remote` 核实）

## 六、红线

- ❌ 修改 `BRD.md` 的需求部分（只能补充技术细节）
- ❌ 跳过自测就标记任务完成
- ❌ 删除测试 Agent 提出的 Bug 记录
- ❌ 需求不明时自行猜测实现
- ❌ 只写日志：不更新 Task.md、不 commit/push 就宣布完工
- ❌ 按进程名批量强杀用户进程、擅自清空用户历史
- ❌ 把密钥、Token、账号、聊天记录等敏感信息写进仓库
- ❌ 强推（`--force` / `--force-with-lease`）、擅自改用户代理配置

## Token 纪律（必读）

- **先定位再读**：改代码前用 `Grep` / `Glob` 定位文件与行号，再定向读；不做全仓递归扫描、不整读大文件
- **交接记录只读最近 3 条**；同一轮不重复读同一文件；已在上下文的内容不重读
- **只写差异**：CHANGELOG 写本轮完成项与遗留，不誊抄旧记录；代码注释不复制文档原文
- **不动稳定前缀**：SOP/skill 的规则段落、章节编号、frontmatter 字段，只为真变更而改
- **保持归档**：CHANGELOG 到 15 条即归档，避免每轮都读更长的日志
- 完整机制见 `skill/token节省/SKILL.md`

## 七、权威来源

- `AGENTS.md` — 角色职责与红线（含日志归属、任务状态约定）
- `DEVELOPMENT.md` — 开发 Agent 操作手册（SOP，本文完整版）
- `DEVELOPMENT.md` 附录 — **Git 推送与网络应急手册**（三 Agent 通用，推送失败必读）
- `BRD.md` / `Task.md` — 需求与任务来源
