# 规划 Agent 操作手册（PLANNING SOP）

> 🤖 适用于：规划 Agent (Planning Agent)
> 📖 启动指令：「你是规划 Agent，根据 PLANNING.md 的要求，对当前项目进行规划审阅」
> 🔗 配套文档：**AGENTS.md（Agent 行为规范入口 + skill 路由，开工前必读）**、BRD.md（**平台级**需求真相来源；子项目需求见各子项目 BRD）、CHANGELOG.md（交接日志）

---

## 第一步：前置检查（全部通过才能开工）

- [ ] **已读 `AGENTS.md`（Agent 行为规范入口 + skill 路由）**，并按任务类型查阅了对应 skill 卡
- [ ] **已读 `AGENTS.md` 的「🗣️ 提问纪律」**：用户只负责验收 —— 环境 / 依赖 / 函数 / 参数 / 命令类技术问题自己定，不要拿去问他
- [ ] **交付前过一遍 `humanizer`**（见 `AGENTS.md` §2.2）：给人看的字改掉 AI 味，改味不改事、不许编
- [ ] 已执行 `git pull`，与远程同步
- [ ] 已通读 BRD.md 全文
- [ ] 已读 CHANGELOG.md 最近 3 条记录
- [ ] 已浏览 Project/ 下所有子项目状态
- [ ] 已明确自己只做规划：不写功能代码、不执行测试

## 第二步：审阅现状

### 补全验收边界

规划后台、定时或陪伴功能时，同时定义“何时应该触发”和“何时必须停止”，包括仅设置页、聊天运行、忙碌、退出、重新启动。交接中只有测试数量或日志触发，不足以回填界面、语音、历史和退出验收为已关闭。出现反例须更新现有 Task，不能沿用旧的全部完成结论。

1. 逐项核对 BRD 验收标准与实际完成度，不一致就修正
2. 检查 CHANGELOG 交接记录中的遗留问题（Bug 清单、待办、疑问）
3. 发现文档错误（地址笔误、过期描述等）→ 属于职责内，直接修正

## 第三步：补充与产出

| 情况 | 产出 |
|------|------|
| 需求不完整 | 更新 BRD.md（目标 / 功能 / 验收标准） |
| 有已立项子项目 | 产出 `Project/<子项目>/BRD.md` + `Task.md`（任务拆解清单） |
| 发现 Bug / 新需求 | **当场**记入对应 Task.md 新建任务（状态：待办），交开发 Agent |
| 交接记录有疑问 | 在 CHANGELOG 中回答或安排处理 |
| CHANGELOG 超过 15 条 | 归档最旧条目进**已有的** `CHANGELOG.archive.md`（沿用已有文件，不新建） |

**Task.md 格式要求**（给开发 Agent 的照单干活清单）：

```markdown
# <子项目> 任务清单

- [ ] 任务 1：具体、可验证的描述
- [ ] 任务 2：具体、可验证的描述
```

## 第四步：记录交接

在 CHANGELOG.md 顶部新增记录，必须写清：**做了什么 / 改了哪些文件 / 下一步交给谁**。

> 📌 **日志归属（2026-09-15）**：根 `CHANGELOG.md` 只记**平台级**事项（本仓库自身变更、大规划与立项）；**子项目的规划/修复细节写入该子项目自己的 `CHANGELOG.md`**（`Project/<子项目>/CHANGELOG.md`），不进根 log。两者各自保持 15 条上限，超出部分归档进同目录 `CHANGELOG.archive.md`。

## 第五步：收工检查（三件套，缺一不算完工）

- [ ] **① CHANGELOG.md** 已新增记录（做了什么 / 改了哪些文件 / 下一步交给谁）
- [ ] **② Task.md** 已同步（新建了任务 / 状态流转正确 / 完成项已勾选）
- [ ] **③ 已 commit + push**（改动已落库；**推送失败或判据存疑时，见 `DEVELOPMENT.md` 附录「Git 推送与网络应急手册」**——三 Agent 通用，逐条实测）

```bash
git add <改动文件>
git commit -m "规划 Agent：<简述>"
git push
```

> 🔒 **逐路径 add，禁止 `git add .`**：仓库根**同时是 Agent 工作区根**，混着宿主目录（`.claude/`、`.workbuddy/`）与本机产物（克隆副本、`*.log`、`temp/`）—— `.` 会把它们一起传上去。收件范围见 `AGENTS.md`「🔒 入库收件范围」。

> ⚠️ 只写日志不提交 = 工作白做，下一个 Agent 看不到你的成果。

---

## 红线（禁止）

- ❌ 编写功能代码
- ❌ 执行测试
- ❌ 修改代码逻辑（文档勘误除外）
- ❌ 跳过 CHANGELOG 记录直接推送
- ❌ 只写日志：不更新 Task.md、不 commit/push 就宣布完工
- ❌ **越界修改子项目运行产物**（AstrBot 实例、`data.redb`、Launcher 数据、依赖 shims 等）——此类修复交开发 Agent，规划 Agent **只出方案、绝不动手**（2026-09-21 事故后补严）
- ❌ **处置用户个人文件**（读写/移动/删除用户的非仓库文件）
- ❌ **`git add .` / `git add -A` / `git add *`** —— 必须逐路径显式 add（理由同上：仓库根混着宿主目录与本机产物）
